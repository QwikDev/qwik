---
'@qwik.dev/core': minor
---

feat: `useComputed$` accepts async functions, auto-tracks all reads, and provides a compute context; `useAsync$`, `createAsync$`, and `AsyncSignal` are deprecated in favor of it
