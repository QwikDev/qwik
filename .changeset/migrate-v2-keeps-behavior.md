---
'@builder.io/qwik': patch
---

feat: `qwik migrate-v2` now migrates renamed and removed APIs, `tsconfig.json`, `package.json` and Vite 8, adds options that keep the v1 behavior where v2 changed it, and lists the changes that need manual attention.
