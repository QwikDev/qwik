---
'@qwik.dev/router': minor
---

feat: 404.tsx and error.tsx render inside their layouts (with `@layout`/`!` modifiers), a route miss resolves the nearest 404.tsx, and the 404 page is prerendered for static hosts
