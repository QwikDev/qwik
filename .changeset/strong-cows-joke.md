---
'@qwik.dev/router': minor
---

FEAT: Custom `404.tsx` error pages are now shown in dev mode, and you can now also create `error.tsx` pages for other HTTP status codes.
Read the HTTP status with `useHttpStatus()`.
