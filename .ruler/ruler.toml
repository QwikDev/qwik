# Ruler Configuration File
# See https://ai.intellectronica.net/ruler for documentation.

# Default agents to enable when running `ruler apply`.
# AGENTS.md (codex output) is read natively by most other agents (Cursor, Copilot, Gemini, ...),
# so we only generate the two files that are actually distinct: CLAUDE.md and AGENTS.md.
default_agents = ["claude", "codex"]

# Keep repo guidance centralized in the root .ruler directory.
nested = false

[agents.claude]
enabled = true
output_path = "CLAUDE.md"

[agents.codex]
enabled = true
output_path = "AGENTS.md"

[skills]
enabled = true

# Generated outputs are committed, not ignored, so a fresh clone/worktree has guidance immediately.
# Never hand-edit the outputs, edit .ruler/. A CI check re-runs `ruler apply` and fails on drift in case of manual edits.
[gitignore]
enabled = false
