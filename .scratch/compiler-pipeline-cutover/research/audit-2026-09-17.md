# Audit baseline 2026-09-17 (charting session)

## Two compilers
- Legacy: `src/` (48 files) + `generators/js/{csr,ssr,shared}` (9.7k lines) + `generators/rust/ssr` (`qwik-ssr-gen`, 3.2k lines Rust, zero tests) + `conformance/layerA` (58 fixtures, harness on `src/index`). Produces `QwikSsrPlan` v0 → `q-ssr-plan.json` via `qwik-vite/src/plugins/ssr-plan.ts`. Specs 01/02/03/07/08/09 + `REQUIREMENTS.md` + `specs/TODO.md` anchored to it.
- Staged: `pipeline/` (67 source files) → `ModulePlan` → `LinkedPlan` → `generateJsCsr`/`generateJsSsr`. `generateRustSsr` promised in DESIGN.md, removed (git history to eb9287f79).
- Nothing bridges `LinkedPlan` to the old Rust generator.

## Pipeline → legacy imports (5 modules)
`src/expr-ir` (ValueIR/ValueIrKind/collectIrBindingIds, 18 sites incl. all of schema/), `src/normalization` (createOriginalRangeMapper: analyse-module, type-results, emit-chunk), `src/module-assembly` (assemble-module, emit-chunk), `src/emit-qrl` (applyReplacements: emit-chunk), `src/source-location` (compat only).

## Monorepo consumers of packages/compiler (all through src/index.ts)
- `qwik-vite/plugins/plugin.ts:4` transformModules (legacy) — dev/HMR path.
- `qwik-vite/plugins/ssr-plan.ts` linkSsrPlan + old plan types.
- `qwik-vite/plugins/test-resume.ts` extractRenderRoots + transformPipelineModules (compat).
- `qwik-vite/plugins/linked-build.ts` `pipeline.*` namespace (prod/lib whole-app complete link, no flag).
- Scripts: build.native, serve.native, playwright.native.config.ts, native-serve.ts, ci test-native.
- package.json exports only `.` → dist/index.mjs built from src/index.ts; tsconfig aliases point at src.

## Tests
- `npx vitest run --root . packages/compiler`: 1870 pass, 72 fail (all conformance/layerA), 16 todo (snapshots.unit.ts:3331-3346).
- pipeline/tests: 46 unit files, 13.5k lines; snapshots.unit.ts 3347 lines; 434 snap files (`<name>.{ssr,csr}.snap`).
- 40/67 pipeline source files without direct test import (covered via snapshots): all lower-* except lower-jsx/lower-setup/lower-hole/lower-event, js-ssr, emit-component/import/collection, link-content/hooks/qrl-dependencies/render-results/type-results, all schema files, html/names/static-subtree/words.
- No `compiler` vitest project; no package scripts.
- conformance/layer0: 6 goldens (numbers, serdes, tofixed, json-bytes, coerce, escape) — engine-agnostic, keep.

## Structure flags
>700 lines: lower-jsx 1474, js-ssr 1289, js-csr 1264, lower-setup 847, render-results 825, bindings 754, link-plans 743, lower-array 713. emit-chunk (520) exports 24 symbols incl. JS printer. Duplicates: pipeline/names.ts vs generate/names.ts; analyse/type-results.ts vs link/type-results.ts. pipeline/index.ts re-exports compat transformModules.

## Plan holes
`QrlBodyKind.Js`, `HookBodyKind.Js`, `SetupKind.Js` (14 use sites in analyse/generate/link). DESIGN rule 5 says JS text is baseline; decided 2026-09-17: no-holes wins server-side.

## Doc drift
- ModulePlan version: DESIGN says 2, LINKED-RENDER-RESULTS says library artifacts carry 3.
- DESIGN linker = folding/policies; README = "1:1 materializer"; LINKED-RENDER-RESULTS = render-result inference + TS checker (TypeScript 5.9.3 is a compiler dependency).
- Three roadmap numberings (DESIGN phases 0–5, JSX-IMPLEMENTATION groups 0–15, specs phases 0–8).
- MULTI_HEAD_SSR.md: multi-head deferred, Reveal excluded — both reopened 2026-09-17.

## JSX-IMPLEMENTATION open items (2026-09-17)
Group 4: factory-returned components. 9: locators after parser normalization, q:shadowRoot. 10: async rows. 11: behavioral regressions partly, React external projections. 12: useOn dynamic roots, visible-task skipped decisions, use-task corpus. 13: all (async, Suspense, Reveal, ErrorBoundary, streaming). 14: all. 15: all (real builds).
