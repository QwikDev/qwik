[workspace]

members = [
    "packages/optimizer/napi",
    "packages/optimizer/wasm",
    "packages/optimizer/core",
]
exclude = ["packages/optimizer/wasm"]
resolver = "2"

[profile.release]
debug = 0
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"
