import type { GetPage, GetNavItems } from './types';

export const getPage: GetPage = async (opts) => {
  opts;
  return null;
};

export const getNavItems: GetNavItems = async (opts) => {
  opts;
  return [];
};
