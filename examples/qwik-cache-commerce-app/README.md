# Qwik Cache Commerce App

Large cache-registry storefront example.

It stresses:

- repeated product inputs and request dedupe;
- shopper segment vary rules;
- product, price, inventory, and recommendation server resources;
- standalone `?qcomponent=ProductCard` rendering;
- normal client `server$` RPC reuse with cache enabled.
