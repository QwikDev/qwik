# Qwik Cache Dashboard App

Multi-tenant dashboard example for the cache-registry prototype.

It stresses private vary rules, tenant-scoped server resources, mixed dashboard widgets, and
component cache safety for personalized UI.
