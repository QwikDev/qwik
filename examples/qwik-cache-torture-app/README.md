# Qwik Cache Torture App

Stress fixture for cache-registry limits.

It intentionally mixes repeated resources, nested Suspense, local signals, request-scoped vary rules,
cacheable components, and normal non-registered SSR components.
