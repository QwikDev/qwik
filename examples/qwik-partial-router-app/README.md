# Qwik Partial Router App

SPA-like shell for testing standalone qcomponent partial navigation.

The shell can fetch:

- server-rendered standalone component HTML;
- data-plus-render-symbol envelopes;
- fallback errors for unregistered components.
