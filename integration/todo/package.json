{
  "name": "qwik-todo",
  "version": "0.0.1",
  "description": "Classic Todo app",
  "scripts": {
    "build": "npm run clean && rollup -c",
    "start": "npm run clean && rollup -c --configDev --watch",
    "serve": "node server",
    "clean": "rimraf build && rimraf server/build"
  },
  "devDependencies": {
    "@builder.io/qwik": "file:../../dist-dev/@builder.io-qwik",
    "@rollup/plugin-node-resolve": "^13.0.6",
    "@rollup/plugin-typescript": "^8.3.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.59.0",
    "typescript": "^4.5.2"
  },
  "author": "Builder.io Team",
  "license": "MIT",
  "engines": {
    "node": ">=14.14.0"
  }
}
