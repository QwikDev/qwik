# its own workspace: it sits outside the native runtime workspace
[workspace]

[package]
name = "qwik-ssr-gen"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
