==INPUT==

export default (props) => {
  return <p title={props.title}>x</p>;
};

== CSR OUTPUT ==

============================= src/component.tsx ==

import { propSource, createAttrEffect, _createElementTemplate } from '@qwik.dev/core';

const default_tmpl0 = _createElementTemplate('<p>x</p>');
export default (props, ctx) => {
  const el0 = default_tmpl0(ctx.document);
  const prop0 = propSource(props, 'title');
  const effect0 = createAttrEffect(el0, 'title', prop0, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
};
== CSR DIAGNOSTICS ==

[]