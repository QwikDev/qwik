==INPUT==

import { useSignal } from '@qwik.dev/core';
export default () => {
  const count = useSignal(0);
  return <div class={count.value}>x</div>;
};

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrNodeId,
  createSsrElementTarget,
  renderSsrAttr,
  escapeHTML,
  maybeThen,
} from '@qwik.dev/core';
export default (props, ctx) => {
  const count = useSignal(0);
  const id0 = ctx.nextId();
  ctx.addRoot(count);
  const attr0 = renderSsrAttr(createSsrElementTarget(id0), 'class', count);
  return maybeThen(attr0, (attr0) => [
    '<div q:id="',
    createSsrNodeId(id0),
    '"',
    attr0 === null ? '' : ' class' + (attr0 === '' ? '' : '="' + escapeHTML(attr0) + '"'),
    '>x</div>',
  ]);
};
== SSR DIAGNOSTICS ==

[]