==INPUT==

import { useSignal, useStore } from '@qwik.dev/core';
export default (props: { label?: string; content?: any }) => {
  const count = useSignal(1);
  return <div>{[<b>a</b>, count.value]}</div>;
};
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _last, createTextNodeEffect, _createElementTemplate } from '@qwik.dev/core';
const default_tmpl0 = _createElementTemplate('<div><b>a</b><!></div>');
export default (props, ctx) => {
  const count = useSignal(1);
  const el0 = default_tmpl0(ctx.document);
  const marker0 = _last(el0);
  const text0 = ctx.document.createTextNode('');
  marker0.replaceWith(text0);
  const effect0 = createTextNodeEffect(text0, count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
};
== CSR DIAGNOSTICS ==

[]