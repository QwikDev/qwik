==INPUT==

import { useSignal } from '@qwik.dev/core';
export default () => {
  const count = useSignal(0);
  return <div><span>{count.value}</span></div>;
};

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrOpenTag,
  createSsrNodeId,
  createSsrMarkup,
  createSsrElementTextTarget,
  escapeHTML,
  renderSsrTextNode,
  maybeThen,
} from '@qwik.dev/core';
export default (props, ctx) => {
  const count = useSignal(0);
  const id_0 = ctx.nextId();
  ctx.addRoot(count);
  const text_0 = renderSsrTextNode(createSsrElementTextTarget(id_0), count);
  return maybeThen(text_0, (text_0) => [
    createSsrOpenTag('<div', '>'),
    createSsrMarkup('<span q:id="', createSsrNodeId(id_0), '">'),
    escapeHTML(text_0),
    '</span></div>',
  ]);
};
== SSR DIAGNOSTICS ==

[]