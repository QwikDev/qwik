==INPUT==

import { component$ } from '@qwik.dev/core';
export default component$((props: { heading: string }) => (
  <div>
    <style>{'.a > b { color: red }'}</style>
    <script>{'if (a < b) { s = "</script>"; }'}</script>
    <title>{props.heading}</title>
  </div>
));

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  createSsrNodeId,
  escapeHTML,
  propSource,
  isSource,
  renderSsrTextNode,
  maybeThen,
} from '@qwik.dev/core';
export default (props, ctx) => {
  const id0 = ctx.nextId();
  const prop0 = propSource(props, 'heading');
  isSource(prop0) && ctx.addRoot(prop0);
  const text0 = renderSsrTextNode(id0, null, prop0);
  return maybeThen(text0, (text0) => [
    '<div><style>.a > b { color: red }</style><script>if (a < b) { s = "<\\/script>"; }</script><title q:id="',
    createSsrNodeId(id0),
    '">',
    escapeHTML(text0),
    '</title></div>',
  ]);
};
== SSR DIAGNOSTICS ==

[]