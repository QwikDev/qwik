==INPUT==

import { useSignal } from '@qwik.dev/core';
export default () => {
  const count = useSignal(0);
  return <p>Count: {count.value}!</p>;
};

== CSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  _first,
  _next,
  createTextNodeEffect,
  _createElementTemplate,
} from '@qwik.dev/core';
const default_tmpl0 = _createElementTemplate('<p>Count: <!>!</p>');
export default (props, ctx) => {
  const count = useSignal(0);
  const el0 = default_tmpl0(ctx.document);
  const marker0 = _next(_first(el0));
  const text0 = ctx.document.createTextNode('');
  marker0.replaceWith(text0);
  const effect0 = createTextNodeEffect(text0, count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
};
== CSR DIAGNOSTICS ==

[]