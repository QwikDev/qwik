==INPUT==

import { useSignal } from '@qwik.dev/core';
export default () => {
  const count = useSignal(0);
  return <p>{count.value}</p>;
};

== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, createTextNodeEffect, createTemplate } from '@qwik.dev/core';
const default_tmpl0 = createTemplate('<p> </p>');
export default (props, ctx) => {
  const count = useSignal(0);
  const fragment0 = default_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const text0 = _first(el0);
  const effect0 = createTextNodeEffect(text0, count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
};
== CSR DIAGNOSTICS ==

[]