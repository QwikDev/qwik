==INPUT==

import { renderItem } from './render-item';

export function App(props: { value: string }) {
  return <section><b>Before</b>{renderItem(props.value)}<i>After</i></section>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import { renderItem } from './render-item';
import { maybeThen } from '@qwik.dev/core/spark';

export function App(props, ctx) {
  const jsx0 = renderItem(props.value);
  return maybeThen(
    jsx0,
    (jsx0) =>
      '<section' +
      '>' +
      '<b' +
      '>' +
      'Before' +
      '</b>' +
      jsx0 +
      '<i' +
      '>' +
      'After' +
      '</i>' +
      '</section>'
  );
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { renderItem } from './render-item';
import { _first, _next, createTemplate } from '@qwik.dev/core/spark';

const App_tmpl0 = createTemplate('<section><b>Before</b> <i>After</i></section>');
export function App(props, ctx) {
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const text0 = _next(_first(el0));
  const jsx0 = renderItem(props.value);
  text0.replaceWith(...(Array.isArray(jsx0) ? jsx0 : jsx0 == null ? [] : [jsx0]));
  return el0;
}
== CSR DIAGNOSTICS ==

[]