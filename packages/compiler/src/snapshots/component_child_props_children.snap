==INPUT==

export function Wrapper(props: { children?: unknown }) {
  return <section>{props.children}</section>;
}

export function Parent() {
  return <Wrapper><p>Projected</p></Wrapper>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import { createComponent } from '@qwik.dev/core/spark';

export function Wrapper(props, _ctx) {
  return '<section>' + (props.children ?? '') + '</section>';
}
export function Parent(_props, ctx) {
  return createComponent({ children: '<p>Projected</p>' }, (props) => Wrapper(props, ctx));
}

null
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { createComponent } from '@qwik.dev/core/spark';

export function Wrapper(props, ctx) {
  const el0 = ctx.document.createElement('section');
  for (const child of props.children ?? []) el0.appendChild(child);
  return [el0];
}
export function Parent(_props, ctx) {
  const el1 = ctx.document.createElement('p');
  const text2 = ctx.document.createTextNode('Projected');
  el1.appendChild(text2);
  const cmp0 = createComponent({ children: [el1] }, (props) => Wrapper(props, ctx), {
    container: ctx,
  });
  return [...cmp0];
}

null
== CSR DIAGNOSTICS ==

[]