==INPUT==

import { useSignal } from '@qwik.dev/core';
export function App() {
  const classes = useSignal('active');
  return <main className="shell"><button className={classes.value}>Toggle</button></main>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrElementRecord,
  createSsrNodeId,
  createSsrElementTarget,
  renderSsrAttr,
  escapeHTML,
  createSsrRecord,
  maybeThen,
} from '@qwik.dev/core';
export function App(props, ctx) {
  const classes = useSignal('active');
  const id0 = ctx.nextId();
  ctx.addRoot(classes);
  const attr1 = renderSsrAttr(createSsrElementTarget(id0), 'class', classes);
  return maybeThen(attr1, (attr1) => [
    createSsrElementRecord('main', '<main class="shell"', '>'),
    createSsrRecord(
      '<button q:id="',
      createSsrNodeId(id0),
      '"',
      attr1 === null ? '' : ' class' + (attr1 === '' ? '' : '="' + escapeHTML(attr1) + '"'),
      '>'
    ),
    'Toggle</button></main>',
  ]);
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, createAttrEffect, createTemplate } from '@qwik.dev/core';
const App_tmpl0 = createTemplate('<main class="shell"><button>Toggle</button></main>');
export function App(props, ctx) {
  const classes = useSignal('active');
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const el1 = _first(el0);
  const effect0 = createAttrEffect(el1, 'class', classes, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
}
== CSR DIAGNOSTICS ==

[]