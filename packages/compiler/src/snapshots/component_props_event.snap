==INPUT==

import type { QRL } from '@qwik.dev/core';

export function App(props: { onClick$: QRL<() => void> }) {
  return <button onClick$={props.onClick$}>Save</button>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import { createSsrElementRecord } from '@qwik.dev/core';
export function App(props, ctx) {
  return [
    createSsrElementRecord('button', '<button', ctx.eventAttr('q-e:click', props.onClick$), '>'),
    'Save</button>',
  ];
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { _first, setEvent, createTemplate } from '@qwik.dev/core';

const App_tmpl0 = createTemplate('<button>Save</button>');
export function App(props, ctx) {
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const event0 = props.onClick$;
  if (event0) setEvent(el0, 'q-e:click', event0);
  return el0;
}
== CSR DIAGNOSTICS ==

[]