==INPUT==

import { useSignal } from '@qwik.dev/core/spark';
  export function App() {
      const count = useSignal(0);
  return <button>{count.value}</button>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  escapeHTML,
  renderSsrTextNode,
  createSsrElementTextTarget,
  maybeThen,
} from '@qwik.dev/core/spark';

export function App(props, ctx) {
  const count = useSignal(0);
  const id0 = ctx.nextId();
  ctx.addRoot(count);
  const text0 = renderSsrTextNode(createSsrElementTextTarget(id0), count);
  return maybeThen(
    text0,
    (text0) => '<button q:id="' + id0 + '"' + '>' + escapeHTML(text0) + '</button>'
  );
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, createTextNodeEffect, createTemplate } from '@qwik.dev/core/spark';

const App_tmpl0 = createTemplate('<button> </button>');
export function App(props, ctx) {
  const count = useSignal(0);
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const text0 = _first(el0);
  const effect0 = createTextNodeEffect(text0, count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
}
== CSR DIAGNOSTICS ==

[]