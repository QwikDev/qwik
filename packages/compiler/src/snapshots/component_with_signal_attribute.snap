==INPUT==

import { useSignal } from '@qwik.dev/core';
  export function App() {
      const count = useSignal(0);
  return <button test={count.value}>Click</button>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrNodeId,
  createSsrElementTarget,
  renderSsrAttr,
  escapeHTML,
  createSsrElementRecord,
  maybeThen,
} from '@qwik.dev/core';
export function App(props, ctx) {
  const count = useSignal(0);
  const id_0 = ctx.nextId();
  ctx.addRoot(count);
  const attr_0 = renderSsrAttr(createSsrElementTarget(id_0), 'test', count);
  return maybeThen(attr_0, (attr_0) => [
    createSsrElementRecord(
      'button',
      '<button q:id="',
      createSsrNodeId(id_0),
      '"',
      attr_0 === null ? '' : ' test' + (attr_0 === '' ? '' : '="' + escapeHTML(attr_0) + '"'),
      '>'
    ),
    'Click</button>',
  ]);
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, createAttrEffect, createTemplate } from '@qwik.dev/core';
const App_tmpl0 = createTemplate('<button>Click</button>');
export function App(props, ctx) {
  const count = useSignal(0);
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const effect0 = createAttrEffect(el0, 'test', count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
}
== CSR DIAGNOSTICS ==

[]