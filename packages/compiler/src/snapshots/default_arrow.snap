==INPUT==

export default () => <section>Default arrow</section>;

== SSR OUTPUT ==

============================= src/component.tsx ==

export default (props, ctx) => {
  return '<section>Default arrow</section>';
};
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { _first, createTemplate } from '@qwik.dev/core';

const default_tmpl0 = createTemplate('<section>Default arrow</section>');
export default (props, ctx) => {
  const fragment0 = default_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  return el0;
};
== CSR DIAGNOSTICS ==

[]