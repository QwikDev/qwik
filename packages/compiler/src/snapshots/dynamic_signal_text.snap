==INPUT==

import { createSignal } from '@qwik.dev/core/spark';
export function App() {
  const count = createSignal(0);
  return <p>{count.value}</p>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  createSignal,
  escapeHTML,
  renderSsrTextNode,
  createSsrElementTextTarget,
} from '@qwik.dev/core/spark';

export function App(_props, ctx) {
  const count = createSignal(0);
  const id0 = ctx.nextId();
  ctx.addRoot(count);
  const text1 = renderSsrTextNode(createSsrElementTextTarget(id0), count);
  return '<p q:id="' + id0 + '">' + escapeHTML(text1) + '</p>';
}

null
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { createSignal, createTemplate, createTextNodeEffect } from '@qwik.dev/core/spark';

const dom_App_template0 = createTemplate('<p> </p>');
export function App(_props, ctx) {
  const count = createSignal(0);
  const fragment0 = dom_App_template0(ctx.document);
  const el1 = fragment0.firstChild;
  const text2 = el1.firstChild;
  const effect3 = createTextNodeEffect(text2, count, { scheduler: ctx.scheduler });
  ctx.scheduler.notify(effect3);
  return [el1];
}

null
== CSR DIAGNOSTICS ==

[]