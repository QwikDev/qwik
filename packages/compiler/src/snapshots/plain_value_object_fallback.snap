==INPUT==

export function App() {
  const foo = { value: 'hello' };
  return <p title={foo.value}>{foo.value}<span>{'Hi ' + foo.value}</span></p>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import { renderDomPropsToString, createSsrElementRecord, escapeHTML } from '@qwik.dev/core';
export function App(props, ctx) {
  const foo = { value: 'hello' };
  return [
    createSsrElementRecord('p', '<p', ...renderDomPropsToString({ title: foo.value }).attrs, '>'),
    escapeHTML(String(foo.value ?? '')),
    '<span>',
    escapeHTML(String('Hi ' + foo.value ?? '')),
    '</span></p>',
  ];
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { _first, _next, patchAttrValue, patchTextValue, createTemplate } from '@qwik.dev/core';

const App_tmpl0 = createTemplate('<p><!----><span> </span></p>');
export function App(props, ctx) {
  const foo = { value: 'hello' };
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const marker0 = _first(el0);
  const text0 = _first(_next(marker0));
  patchAttrValue(el0, 'title', foo.value);
  const text1 = ctx.document.createTextNode('');
  marker0.replaceWith(text1);
  patchTextValue(text1, foo.value);
  patchTextValue(text0, 'Hi ' + foo.value);
  return el0;
}
== CSR DIAGNOSTICS ==

[]