==INPUT==

import { useSignal } from '@qwik.dev/core';
export function App() {
  const title = useSignal('hello');
  const classes = useSignal('active');
  const style = useSignal('color:red');
  return <div title={title.value} className={classes.value} style={style.value}>Attrs</div>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrNodeId,
  createSsrElementTarget,
  renderSsrAttr,
  escapeHTML,
  getActiveInvokeContextOrNull,
  invoke,
  createSsrOpenTag,
  maybeThen,
} from '@qwik.dev/core';
export function App(props, ctx) {
  const title = useSignal('hello');
  const classes = useSignal('active');
  const style = useSignal('color:red');
  const id_0 = ctx.nextId();
  ctx.addRoot(title);
  const attr_0 = renderSsrAttr(createSsrElementTarget(id_0), 'title', title);
  const invokeCtx = getActiveInvokeContextOrNull();
  const attr_1 = () =>
    invoke(invokeCtx, () => {
      ctx.addRoot(classes);
      return renderSsrAttr(createSsrElementTarget(id_0), 'class', classes);
    });
  const attr_2 = () =>
    invoke(invokeCtx, () => {
      ctx.addRoot(style);
      return renderSsrAttr(createSsrElementTarget(id_0), 'style', style);
    });
  return maybeThen(attr_0, (attr_0) =>
    maybeThen(attr_1(), (attr_1) =>
      maybeThen(attr_2(), (attr_2) => [
        createSsrOpenTag(
          '<div q:id="',
          createSsrNodeId(id_0),
          '"',
          attr_0 === null ? '' : ' title' + (attr_0 === '' ? '' : '="' + escapeHTML(attr_0) + '"'),
          attr_1 === null ? '' : ' class' + (attr_1 === '' ? '' : '="' + escapeHTML(attr_1) + '"'),
          attr_2 === null ? '' : ' style' + (attr_2 === '' ? '' : '="' + escapeHTML(attr_2) + '"'),
          '>'
        ),
        'Attrs</div>',
      ])
    )
  );
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, createAttrEffect, createTemplate } from '@qwik.dev/core';
const App_tmpl0 = createTemplate('<div>Attrs</div>');
export function App(props, ctx) {
  const title = useSignal('hello');
  const classes = useSignal('active');
  const style = useSignal('color:red');
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const effect0 = createAttrEffect(el0, 'title', title, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  const effect1 = createAttrEffect(el0, 'class', classes, ctx.scheduler);
  ctx.scheduler.notify(effect1);
  const effect2 = createAttrEffect(el0, 'style', style, ctx.scheduler);
  ctx.scheduler.notify(effect2);
  return el0;
}
== CSR DIAGNOSTICS ==

[]