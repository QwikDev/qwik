==INPUT==

import { useSignal } from '@qwik.dev/core';
export function App() {
  const count = useSignal(0);
  return <p>Hello {count.value} world</p>;
}

== SSR OUTPUT ==

============================= src/component.tsx ==

import {
  useSignal,
  createSsrNodeId,
  createSsrElementRecord,
  createSsrRangeTextTarget,
  renderSsrTextNode,
  escapeHTML,
  maybeThen,
} from '@qwik.dev/core';
export function App(props, ctx) {
  const count = useSignal(0);
  const id0 = ctx.nextId();
  ctx.addRoot(count);
  const text1 = renderSsrTextNode(createSsrRangeTextTarget(id0, 0), count);
  return maybeThen(text1, (text1) => [
    createSsrElementRecord('p', '<p q:id="', createSsrNodeId(id0), '"', '>'),
    'Hello <!t>',
    escapeHTML(text1),
    '<!/t> world</p>',
  ]);
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/component.tsx ==

import { useSignal, _first, _next, createTextNodeEffect, createTemplate } from '@qwik.dev/core';
const App_tmpl0 = createTemplate('<p>Hello <!----> world</p>');
export function App(props, ctx) {
  const count = useSignal(0);
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  const marker0 = _next(_first(el0));
  const text0 = ctx.document.createTextNode('');
  marker0.replaceWith(text0);
  const effect0 = createTextNodeEffect(text0, count, ctx.scheduler);
  ctx.scheduler.notify(effect0);
  return el0;
}
== CSR DIAGNOSTICS ==

[]