==INPUT==

import { useId } from '@qwik.dev/core';

export function App() {
  const id = useId();
  return <label for={id}>Name</label>;
}

== SSR OUTPUT ==

============================= src/use-id.tsx ==

import { escapeHTML, createSsrElementRecord } from '@qwik.dev/core';
export function App(props, ctx, _id = 'qApp-') {
  const id = _id + 'u0';
  return [
    createSsrElementRecord('label', '<label for="', escapeHTML(id), '"', '>'),
    'Name</label>',
  ];
}
== SSR DIAGNOSTICS ==

[]
== CSR OUTPUT ==

============================= src/use-id.tsx ==

import { _first, createTemplate } from '@qwik.dev/core';

const App_tmpl0 = createTemplate('<label>Name</label>');
export function App(props, ctx, _id = 'qApp-') {
  const id = _id + 'u0';
  const fragment0 = App_tmpl0(ctx.document);
  const el0 = _first(fragment0);
  el0.setAttribute('for', id);
  return el0;
}
== CSR DIAGNOSTICS ==

[]