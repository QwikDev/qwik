---
title: Cookbook | Overview
contributors:
  - mhevery
  - fabiobiondi
  - n8sabes
---

# Cookbook

A cookbook contains a collection of useful patterns for solving common problems in front-end development.

Examples:
- [Modal Dialog Pop-Up](./portal/)
- [Media Controller with iOS Support](./mediaController/)
## Managing Dark theme
1. In root.tsx file add this code in head tag.
```html
<script
  dangerouslySetInnerHTML={`
   (function() {
      function setTheme(theme) {
      document.documentElement.className = theme;
      localStorage.setItem('theme', theme);
      }
      var theme = localStorage.getItem('theme');
      if (theme) {
        setTheme(theme);
      } else {
        setTheme('light');
      }
      })();
  `}></script>
```
2. Then you can change the theme by adding this button.
```html
<button
    class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
    onClick$={() => {
      const theme = document.documentElement.className;
      if (theme === "light") {
        document.documentElement.className = "dark";
        localStorage.setItem("theme", "dark");
      }
      else {
        document.documentElement.className = "light";
        localStorage.setItem("theme", "light");
      }
    }}
  >
  Toggle Theme
  </button>
```
