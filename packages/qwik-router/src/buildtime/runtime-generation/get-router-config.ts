// import { menus, routes, basePathname } from '@qwik-router-config';
// import type { RouteData } from '@qwik.dev/router';

// /**
//  * Returns the client build manifest, which includes the mappings from symbols to bundles, the
//  * bundlegraph etc.
//  *
//  * @public
//  */
// export const getRoutes = (): RouteData[] => routes;
