# Qwik Framework Info
- `$QWIK_HOME` points to a local clone of the Qwik framework.
- The most current version of the Rust SWC optimizer can be found in `$QWIK_HOME/packages/optimizer` with the Rust SWC source code living under `./core`.