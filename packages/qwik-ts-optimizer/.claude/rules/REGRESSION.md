# Regression Prevention

## `pnpm vitetest convergance` Success Rate

The success rate of `pnpm vitetest convergance` should never decrease from one change to the next.

## `pnpm vitetest run` Success Rate

Must not decrease from one change to the next compared to `main`.