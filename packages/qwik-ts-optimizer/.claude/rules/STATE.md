# Project state

Snapshot of where the active workstream stands. Read at the start of a session to rehydrate context fast. **Update aggressively** as meaningful progress lands — see "Maintenance" at the bottom.

Last updated: 2026-05-21 (post PR #136 merged — OSS-404 closed (second F2 sub-issue under OSS-403 umbrella). **Convergence 187 → 188/212 (88.7%)**. **What landed**: three coupled fixes for inline self-ref components — (1) `computeRelPath` at `src/optimizer/path-utils.ts:66` preserves leading `./` from input (SWC's hash + dev-info `fileName:` field use the user-provided shape; pre-fix `normalize()` stripped it producing TS-vs-SWC hash divergence + `fileName:` divergence; new `restoreDotSlash` helper reapplies the prefix); (2) `buildDevFilePath` at `src/optimizer/dev-mode.ts:38` strips leading `./` from `inputPath` before srcDir concat (pre-fix emitted `/user/qwik/src/./node_modules/...` with stray `/./`); (3) F1c port from stale `ast-parity/F2` branch commit 534ddd4 — `assembleOutput` partitions `sCalls` by whether body references an exported componentQrl name; referencing calls go AFTER export so body's self-reference doesn't TDZ at module load. **Pickup audit found 4 distinct bugs vs umbrella's 2** (A: `file:` extra `./`, B: F1c emit ordering, C: symbol hash mismatch, D: `fileName:` missing `./`, E: `lineNumber:` offset). A+C+D share one root cause (path-norm); fix #1 cascade-fixes all three. B = F1c port. E filed separately as **OSS-410** (line-number offset). Hash divergence confirmed via debug instrumentation: SWC's expected hash requires `./`-prefixed relPath input. F1c port is a **real runtime correctness fix** — convergence test passes without it (compareAst's sortReorderableBlock normalizes ordering) but the emitted module has a TDZ bug at module load. 6 focused regression tests at `tests/optimizer/oss-404-path-norm-f1c.test.ts`: path-norm preserve/no-op behaviors, buildDevFilePath strip behavior, F1c partition for self-ref + non-self-ref. Net +206/-5 across 4 files. Target test flips → convergence 187 → 188/212 + full-suite 742 → 749. **OPTIMIZER.md audit verdict**: no update needed — `path-utils.ts` and `output-assembly.ts` are on the trigger checklist but the changes are localized correctness fixes; no phase / marker / convention change. **Pattern observation**: OSS-406 + OSS-404 both shipped under the same playbook — deep pickup audit reveals broader scope than umbrella documented → update description + ship coherent fix + file siblings for adjacent bugs. The `feedback_parity_audit_multi_root_cause` memory is now validated across 4 PRs (OSS-400/401/406/404). **F2 sub-issues remaining**: OSS-405 / OSS-407 / OSS-408 / OSS-409 + new sibling OSS-410. Previous workstream — **OSS-406 merged via PR #134** (first F2 sub-issue closed, convergence 186 → 187/212). **What landed**: `promoteEventHandlerCaptures` at `src/optimizer/transform/event-capture-promotion.ts:474-491` now branches on a new `isInlineStrategy` field threaded through `EventCaptureContext`. Under `EntryStrategy::Inline`, non-loop eventHandler captures stay in `captureNames` so the downstream pipeline (`preConsolidateRawPropsCaptures` → `transformInlineSegmentBody` → `injectCapturesUnpacking` → `replacePropsFieldReferencesInBody`) routes them through `_captures[N]` unpacking + `_rawProps.X` rewriting. Under `Hoist` and default `smart`/`segment`, existing segment-file-style positional-param padding (`['_', '_1', 'atom', 'ctx']`) keeps firing. **Key implementation moment — the initial gate `isInlineStrategy = type === 'inline' || type === 'hoist'` regressed `example_issue_33443`** (Hoist, baseline-passing) which expects `(_, _1, counter) => counter.value++` padded paramNames. Narrowed to `inline` only — Hoist needs its existing padding. **3 focused regression tests** at `tests/optimizer/oss-406-rawprops-propagation.test.ts` cover the gate behavior: positive (Inline keeps captureNames + emits `_captures[0]` / `_rawProps.X`), negative-1 (Hoist still pads), negative-2 (default smart still pads). Net +183/-5 across 3 files. Target test `example_optimization_issue_3542` flips → convergence 186 → 187/212 + full-suite 738 → 742. **OPTIMIZER.md audit verdict**: no update needed — localized correctness fix adding one branch to an existing classifier; no phase/marker/convention change. **Multi-root-cause audit confirmation in BOTH directions**: the audit during OSS-403 umbrella creation incorrectly framed OSS-406 as "emit `q_X.s(body)` blocks" — reality was TS WAS emitting them, just with wrong content (loop-cross-capture-style args, phantom `ev`, missing `.w([_rawProps])`). Re-audit during pickup correctly identified the real bug + scope. The audit-during-impl norm caught the over-broad Hoist gate before merge. This is the established pattern: F* parity tickets always re-audit during pickup; surprise findings drive sub-issue splits or scope corrections. **Of 5 remaining F2 sub-issues** (OSS-404/405/407/408/409), some may now be subsumed by this fix; pickup audit on the next sub-issue should explicitly check whether OSS-406's landing also flipped its target.)

## Goal

**Active workstream:** Parity track — F2 umbrella (OSS-403) underway, 2 of 6 sub-issues closed. **OSS-404 just merged via [PR #136](https://github.com/thejackshelton/TS-Optimizer/pull/136)** — convergence 187 → 188/212 (88.7%). Three coupled fixes for inline self-ref components: (1) `computeRelPath` preserves leading `./` (SWC's hash + dev-info use user-provided shape); (2) `buildDevFilePath` strips `./` before srcDir concat (no stray `/./` in absolute path); (3) F1c port from stale branch — sCalls partitioned so self-ref bodies emit AFTER `export const X = componentQrl(...)`. **Pickup audit found 4 bugs vs umbrella's 2**; one extra (line-number offset) filed as OSS-410. F1c port is a real runtime correctness fix — convergence passes without it via compareAst's sortReorderableBlock normalizer, but emitted module had TDZ bug. 6 focused regression tests pin all 4 fix behaviors. Previous workstream — **OSS-406 merged via [PR #134](https://github.com/thejackshelton/TS-Optimizer/pull/134)** — first F2 sub-issue closed. **What landed**: `promoteEventHandlerCaptures` gate on `isInlineStrategy === 'inline'` (NOT `'hoist'`); under Inline, non-loop captures stay in `captureNames` for the existing `_rawProps`-consolidation pipeline to consume. Initial gate `inline || hoist` regressed `example_issue_33443` (Hoist, baseline-passing); narrowed to `inline` only — Hoist needs its existing padding. Target test `example_optimization_issue_3542` flips. 3 focused regression tests pin the gate behavior. **5 F2 sub-issues remain** ([OSS-404](https://linear.app/kunai/issue/OSS-404) / [405](https://linear.app/kunai/issue/OSS-405) / [407](https://linear.app/kunai/issue/OSS-407) / [408](https://linear.app/kunai/issue/OSS-408) / [409](https://linear.app/kunai/issue/OSS-409)); some may now be subsumed by this fix — pickup audit on the next sub-issue must check first per the multi-root-cause norm. **Audit refinement during pickup**: OSS-403 umbrella initially described OSS-406 as "emit `q_X.s(body)` blocks" but pickup audit found TS WAS emitting them with wrong content. The real bug was downstream — `event-capture-promotion` clearing `captureNames` too early. Audit-during-impl norm caught the over-broad gate before merge. This is now a stable workflow expectation for F* sub-issues: re-audit at pickup; surprise findings drive scope corrections. Previous workstream — **OSS-402 merged via [PR #132](https://github.com/thejackshelton/TS-Optimizer/pull/132)** completing the 3-PR arc that closed `example_component_with_event_listeners_inside_loop`. **Convergence 185 → 186/212 (87.7%)** — first parity flip since OSS-379 closed `example_qwik_react` (back during the code-health pass). **What landed**: new fourth branch in `analyzeMemberExpression` at `src/optimizer/signal-analysis.ts:868` handling `obj[Identifier]` / `obj[NumericLit]` patterns (gate: `isKnownIdent && exprNode.computed`). Existing three branches cover `.value`, deep store access (depth ≥ 2), and static/string-literal-computed props; this branch fills the remaining case where `getPropertyName` returns null (Identifier prop or NumericLit). Mirrors SWC's `convert_inlined_fn` path: `prop_to_string` returns None for `[Identifier]`/`[NumericLit]`, `ObjectUsageChecker.used_as_object` matches when object resolves to in-scope Var. Collects deps via `collectSignalDeps` (alphabetically sorted, matching SWC's `scoped_idents` sort); routes through `generateFnSignal`. For the target test, 3 sites (`loopForI`/`loopForIn`/`loopWhile` `{results[i]}`/`{results[key]}`) dedupe to the same `_hf0 = (p0,p1)=>p1[p0]` via `SignalHoister.hoist`'s string-keyed dedup. 6 new unit tests at `tests/optimizer/signal-analysis.test.ts` (3 positive + 3 negative). Net +95/+0 across 2 files. **OPTIMIZER.md audit verdict**: no update needed — localized correctness fix; no phase/marker/convention change. **The 3-PR arc that closed F8**: OSS-400 (brace-depth hoist target, PR #128) → OSS-401 (scope-aware `classifyConstness`, PR #130) → OSS-402 (computed-prop `_fnSignal`, PR #132). All three executed cleanly because the OSS-400 audit surfaced the multi-root-cause shape upfront (memory: `feedback_parity_audit_multi_root_cause`). **F8 is now CLOSED in CONVERGENCE_FAILURES.md** — all F8a/b/c/d tests passing; only `example_qwik_react` (different root cause: migration `_auto_filterProps`) remains under the loose F8 umbrella. Previous workstream — **OSS-401 merged via [PR #130](https://github.com/thejackshelton/TS-Optimizer/pull/130)** — scope-aware `classifyConstness` for shadowed identifiers. New `ScopeAwareBindings` interface + per-name index over scope ranges; mirrors SWC's hygiene-context `decl_stack` lookup. 12 unit tests cover arrow-param vs for-of shadowing + edge cases. `alwaysConst` override field on `ScopeAwareBindingsImpl` resolved a regression from capture-name injection during impl. Net +434/-120 across 6 files. Test moved closer to passing (`loopArrowFn` flag 6→4) but didn't flip until OSS-402 also landed. Previous workstream — **CBP-rollout track substantively complete**. User rewrote `CODING_BEST_PRACTICES.md` (171 → 385 lines) into focused TS-discipline rules; **OSS-381 ([umbrella](https://linear.app/kunai/issue/OSS-381))** is the parent ticket coordinating 11 sub-issues (OSS-382 through OSS-392). Actual ordering (corrected from earlier STATE drafts): OSS-382 exhaustiveness + double-casts (done), OSS-383 brand-types foundation (done), **OSS-384 symbol-family brand propagation (done)**, OSS-385 path-family + CtxName brand propagation, OSS-386 position + source-text brand propagation, OSS-387 `readonly` discipline, OSS-388 AST utility return types, OSS-389 phased discriminated union for `ExtractionResult`, OSS-390 `TransformModule` discriminated union, OSS-391 enter/exit context split for AST walkers, OSS-392 type-relationship consolidation. **Phase 17 = OSS-399 / OSS-390 Phase 2 just merged via [PR #126](https://github.com/thejackshelton/TS-Optimizer/pull/126)** — TransformModule null-arm cleanup. Dropped `segment: null` from `TransformModuleParent` + `origPath: null` from `TransformModuleSegment` (the redundant null-arm fields Phase 1 retained); 4 construction sites updated; 18 consumer narrow checks migrated across 8 test files from `.segment != null` / `.segment?.X` patterns to `.kind === 'segment'`-discriminated narrowing. Net +56/-62 across 12 files. Convergence 185/212 + full-suite 719/769 baselines unchanged. **Closes the LAST unfiled Phase 2 sweep under the OSS-381 CBP umbrella** — with OSS-389 Phase 2 (PR #124) and OSS-390 Phase 2 (PR #126) both landed, every original Phase 2 deferral has now shipped. Only OSS-389 Phase 3 candidates (per-variant field-presence + construct-new at transitions) remain as deferred follow-ups across the entire umbrella. **9th consecutive Phase 1/Phase 2 split landing** (OSS-389 → 390 → 388 → 394/395 → 391 → 396 → 397 → 398 → 399). The pattern is now structurally definitive. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (variant-field removal); no phase / marker / convention change. Previous workstream — **Phase 16 = OSS-398 / OSS-389 Phase 2 merged via [PR #124](https://github.com/thejackshelton/TS-Optimizer/pull/124)** — ExtractionResult consumer narrowing. Pragmatic scope per planning audit: narrowed ~12 consumer signatures from `ExtractionResult` to `ConsolidatedSegment`; deferred per-variant field-presence + construct-new (the other two original Phase 2 concerns) to a future Phase 3 if ever picked up. Two new `Mutable<>` phase-flip loops at pipeline boundaries — Phase 2→3 in `transform/index.ts` (`'extracted' → 'captured'`) and Phase 4 inside `rewriteParentModule` after `resolveNesting()` (`'captured' → 'consolidated'`); the discriminator now flips correctly across the pipeline. `ParentRewriteResult.extractions` narrowed to `ConsolidatedSegment[]`; `SegmentGenerationContext` + `SegmentGenerationPrep` Phase-5 fields narrowed; 5 functions in `segment-generation.ts` + 4 in `segment-codegen.ts` narrowed. Audit revealed `rewrite/output-assembly` + `inline-body` helpers iterate `ctx.extractions: ExtractionResult[]` mid-Phase-4 — narrowing them would cascade casts on iterators, kept on the union. Net +62/-27 across 5 files. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor with 2 new documented Mutable<> cast sites; no phase / marker / convention change. Previous workstream — **Phase 15 = OSS-397 / OSS-391 Phase 2b merged via [PR #122](https://github.com/thejackshelton/TS-Optimizer/pull/122)** — applies the `walkWithProtocol` wrapper to the 517-line load-bearing extraction walker at `extract.ts:367`, completing the OSS-391 sweep across all 3 in-scope `oxc-walker` enter+leave walks. New `ContextStackForEnter` (via `Pick<ContextStack, ...>`) restricts enter to push/peek/read API; new `ExtractWalkEnterContext` (11 read-only inputs + 5 mutable buffers + naming-narrowed + pushActiveSegmentBody mutator); new `ExtractWalkExitContext extends Omit<EnterContext, 'naming'>` widens naming back to full ContextStack + adds `finaliseTopFrameIfMatches` + `popContextStackForNode` act-helpers; leave handler collapsed to two act-helper calls. Both compile-time probes verified (then reverted) — `ctx.naming.pop()` and `ctx.popContextStackForNode(node)` are compile errors in enter. Strongest type-level enforcement of any walker refactor in the sweep: the Pick-based narrow gates 16 enter-side push sites + the single leave-side ctx pop at compile time. Net +153/-60 in single file. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md drift folded into this same docs PR** — 5 extract.ts cites drifted past threshold (resolveCanonicalCalleeName +56, getDirectWrapperContextName +56, disambiguateExtractions +93, inlinedQrl detection +109, marker-call extraction block +109). All refreshed alongside STATE.md. **New design precedent**: `Omit + redeclare` pattern for property-type widening in interface extension. Previous workstream — **Phase 14 = OSS-396 / OSS-391 Phase 2a merged via [PR #120](https://github.com/thejackshelton/TS-Optimizer/pull/120)** — applies the `walkWithProtocol` wrapper to `transform/event-capture-promotion.ts:38` (`buildExtractionLoopMap`), the smaller of the two remaining enter+leave walks. New `BuildExtractionLoopMapEnterContext` (3 read-only inputs + 3 mutable buffers as `readonly LoopContext[]` for loopStack + `pushLoop()` mutator) and `BuildExtractionLoopMapExitContext extends ...` (adds `popTopLoopIfMatches(node)` as exit-only act-helper). The `loopStack.pop()` on loop-boundary nodes moved behind the act-helper. Compile-time enforcement verified by probe (inserting `ctx.popTopLoopIfMatches(node)` inside enter surfaces `Property 'popTopLoopIfMatches' does not exist on type 'BuildExtractionLoopMapEnterContext'`). Wrapper now applied to 2 of 3 in-scope walkers (JSX + buildExtractionLoopMap); Phase 2b = `extract.ts:367` 517-line load-bearing extraction walker remains unfiled. Net +64/-19 in a single file. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — `event-capture-promotion.ts` is on the trigger checklist but the doc has zero internal citations of it; type-internal refactor; no phase/marker/convention change. **6th consecutive Phase 1/Phase 2 split** (OSS-389 → OSS-390 → OSS-388 → OSS-394/OSS-395 → OSS-391 → OSS-396 as Phase 2a) — pattern now strong enough to expect from any future "Phase 1" ticket. **Workflow pattern locked**: filed Phase 2a as a NEW sibling Linear ticket (OSS-396) rather than re-opening OSS-391, matching the OSS-388/394/395 precedent. Sibling-ticket-when-picked-up is the established norm. Also landed same-day: **PR #119 (no-ticket OPTIMIZER.md cumulative-drift refresh)** — ~30 file:line citations swept including the famous `segment-generation.ts:761 → :1048` (287-line drift flagged in PR #118 wrap-up); ExtractionResult fields table rewritten to anchor at OSS-389 discriminated-union variants + cite the marker-call extraction block at `extract.ts:716-751`; Tier 3 under-threshold drift also swept for completeness — every cite in OPTIMIZER.md now matches current source. Previous workstream — **Phase 13 = OSS-392 just merged via [PR #117](https://github.com/thejackshelton/TS-Optimizer/pull/117)** — type-relationship consolidation. Previous workstream — **Phase 12 = OSS-391 Phase 1 just merged via [PR #115](https://github.com/thejackshelton/TS-Optimizer/pull/115)** — introduced `walkWithProtocol` wrapper at `src/optimizer/utils/walk-with-protocol.ts` (~60 LOC) that enforces `EnterContext` ⊂ `ExitContext` type split via TS generics, applied as pilot to the JSX walker at `transform/jsx.ts:620` (new `JsxWalkEnterContext` / `JsxWalkExitContext` interfaces; `needsFragment` flip moved behind `setNeedsFragment()` act-helper; `isInSkipRange` widened to `ReadonlyArray<...>`); compile-time enforcement verified by probe (removing the `@ts-expect-error` directive surfaces `Property 's' does not exist on type 'JsxWalkEnterContext'`); 7 new wrapper unit tests pin behavioural parity + context delivery + OSS-368-style buffered-state pattern + the compile-time `@ts-expect-error` check; Phase 2 sweep (`extract.ts:367` OSS-368 buffered-frame walker + `event-capture-promotion.ts:38` symmetric push/pop) deferred — unfiled, file when picked up. Net +382/-24 across 3 files. Convergence 185/212 + full-suite 712/762 baselines unchanged. **OPTIMIZER.md audit verdict**: file:line refs updated alongside (jsx.ts citations drifted: `classifyConstness` 301-435 → 334, `JsxKeyCounter` 437-457 → 470, `transformAllJsx` return 557 → 736; the third exceeds the 50-line threshold while the other two are at it — fixed all three for consistency); no phase / marker / convention change, type-internal refactor adding compile-time enforcement to an existing walker. **5th consecutive Phase 1 / Phase 2 split** (OSS-389 → OSS-390 → OSS-388 → OSS-394/OSS-395 → OSS-391) — pattern locked. Previous workstream — **Phase 11 = OSS-395 (OSS-388 Phase 3) merged via [PR #113](https://github.com/thejackshelton/TS-Optimizer/pull/113)** — eliminated defensive narrowing casts in `rewrite/index.ts` (import processing), `segment-codegen/body-transforms.ts` (the `.w(...)` walker), and `utils/ast.ts` (`getPatternProperties` return type tightening). Two key audit findings shaped the scope: (1) **Phase 1's iterator tightening propagated further than expected** — `rewrite/const-propagation.ts` (originally a Phase 3 target) was already clean because PR #109's visitor tightening forced downstream `parentNode` types to narrow, which had surfaced the dead `'ObjectProperty'` / `'StaticMemberExpression'` checks (removed in #109); (2) **2 of 3 signal-analysis.ts cast removals turned out to be load-bearing**, not defensive — restored with documenting comments. **What landed**: `rewrite/index.ts` import processing — 6 casts in 3 sites collapsed into a single new `importedSpecifierName(spec)` helper that narrows on `imported.type === 'Identifier'` and handles the ES2022 `import { "foo" as bar }` (StringLiteral imported) case by falling through to the local binding name; stale `IdentifierName` / `StringLiteral` runtime-type imports dropped. `body-transforms.ts` `.w(...)` walker — 3 casts at lines 337/340/342 replaced by proper discriminated-union narrowing on `node.callee?.type === 'MemberExpression'`; surfaced + removed the dead `'StaticMemberExpression'` branch (same territory as PR #44 / PR #109). `utils/ast.ts` `getPatternProperties` — return type tightened from `AstCompatNode[]` to `AstNode[]` via the brand-constructor pattern (single internal cast validated by `isAstNode`, matching the OSS-388 Phase 1 `forEachAstChild` pattern). `signal-analysis.ts` (3 load-bearing casts restored with comments): line 98 in `containsJsx` — `Array.isArray` doesn't narrow `ReadonlyArray` (which is what `AstNodeList` is), so the cast bridges that TS quirk; lines 289/381 — predicate-narrowing-exclusion chain (the preceding `isDeepStoreAccess` predicate also returns `n is MemberExpressionNode`, so TS has already excluded MemberExpression from `n`'s type by these points, narrowing again produces `never`; the cast overrides that). **Cast count delta**: 146 → **142** by grep (-4); -8 actual code casts removed; +1 added in `getPatternProperties` boundary; +3 restored in signal-analysis.ts as load-bearing. **Cumulative OSS-388 trajectory**: 163 → 151 (Phase 1, PR #109) → 146 (Phase 2 / OSS-394, PR #111) → 142 (Phase 3 / OSS-395, PR #113). **Important re-framing**: the original OSS-388 ≤60 acceptance target was based on a misleading grep count. `grep -rn "\bas " src/optimizer/` matches comment text (`as a grep target`), template-literal interpolations (`* as ${name}`, `${importedName} as ${localName}`), and `as const` declarations — not just type assertions. After Phase 3, remaining `as` matches are dominated by: 14 brand-constructor casts in `types/brands.ts` (legit per CBP), documented FFI casts (`as Mutable<ConsolidatedSegment>` from OSS-389), template-literal `as` keyword in import-emit code, and a handful of remaining narrowing casts. **OSS-388's principled-cast-reduction work is effectively complete across Phases 1-3**; subsequent reductions (if any) would be small scattered Phase 4 cleanup, not concentrated clusters worth a dedicated ticket. Net +44/-17 across 4 files. Convergence 185/212 + full-suite 712/762 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed (3 of 4 touched files on the trigger checklist; type-internal cast removal + helper extraction; no phase / marker / convention change). **Pattern observation**: this PR breaks the Phase 1 / Phase 2 split streak (was 4 consecutive: OSS-389 → OSS-390 → OSS-388 → OSS-394) but for a healthy reason — OSS-395 *is* Phase 3, no further phase deferred. The cast trajectory is closed; future work (if any) is scattered Phase 4 cleanup rather than a sibling-ticket-deserving cluster. Previous workstream — **Phase 10 = OSS-394 (OSS-388 Phase 2) merged via [PR #111](https://github.com/thejackshelton/TS-Optimizer/pull/111)** — eliminated structural-narrowing casts in `utils/simplify.ts`. Next-biggest cast cluster after Phase 1 (10 casts in one 147-line file) split into two categories during audit: **Category A** (6 structural-narrowing casts inside `switch (node.type)` arms — defensive workarounds not trusting TS's discriminated-union narrowing); **Category B** (4 `as never` arithmetic-bridging casts — load-bearing TS-vs-JS arithmetic-gap bridges, matching SWC's runtime simplification semantics). Phase 2 scope tackled Category A only. **What landed**: inside each `switch (node.type)` arm, accessed the typed fields of the narrowed `node` directly (`node.value`, `node.operator`, `node.argument`, `node.left/right`, `node.test/consequent/alternate`). **Surfaced an oxc quirk**: `Literal` splits into 6 variants (`BooleanLiteral | NullLiteral | NumericLiteral | StringLiteral | BigIntLiteral | RegExpLiteral`) with `bigint` only on `BigIntLiteral`. The original explicit `bigint` check was filtering out BigInt — but the existing `typeof v === 'string'/'number'/'boolean'` (plus null/undefined) guard already rejects bigint (`typeof bigint === 'bigint'`) and RegExp (`typeof RegExp === 'object'`), so the explicit check was redundant. Dropped with a comment documenting the equivalence. Category B kept with a clarifying comment explaining the TS-vs-JS arithmetic-gap rationale. **Cast count delta**: 151 → **146** (-5 grep matches; -6 code casts; +1 grep noise from new "`as never`" mention in the documenting comment). **Cumulative OSS-388 trajectory**: 163 → 151 (Phase 1) → 146 (Phase 2). 86 to go for the ≤60 target. **What's deferred (Phase 3+)**: custom walk function signatures in `rewrite/const-propagation.ts` / `rewrite/index.ts` / `signal-analysis.ts`; `getPatternProperties` return type tightening. **New ticket convention surfaced**: OSS-394 is the first sub-issue *filed in-flight* (vs OSS-389 Phase 2 / OSS-390 Phase 2 which were left "unfiled" in STATE.md). File when picked up — keeps the Linear board honest about scope while preserving the Phase 1 / Phase 2 split pattern. Net +26/-19 across 1 file. Convergence 185/212 + full-suite 712/762 baselines unchanged. **OPTIMIZER.md audit verdict**: non-trigger-touching, skipped (`utils/simplify.ts` is not on the checklist). Previous workstream — **Phase 9 = OSS-388 Phase 1 ([PR #109](https://github.com/thejackshelton/TS-Optimizer/pull/109))** — tightened AST iterator visitor types. The iterator utilities `forEachAstChild` + `someAstChild` had visitor callbacks typed `(child: AstCompatNode, ...)` (the duck-typed shape), forcing every caller to do `(child) => walkNode(child as AstNode)` because local walk functions accept the strict `AstNode` union. Per CBP rule "Casts are reserved for two narrow purposes," tightened the visitor signatures to `(child: AstNode, ...)` with single internal cast at dispatch (validated by `isAstNode` — brand-constructor pattern). **What landed**: visitor signature tightened on both iterators; `forEachChildNode` wrapper in `signal-analysis.ts:35-42` (existed solely to retype the visitor params) dropped, 7 internal call sites rewired to `forEachAstChild` directly; 12 defensive `child as AstNode` / `parent as AstNode` casts swept across 8 files (`segment-codegen.ts`, `rewrite/inline-body.ts`, `rewrite/raw-props.ts`, `rewrite/const-propagation.ts` x8, `rewrite/index.ts`, `utils/props-field-rewrite.ts`, `transform/jsx.ts`); `parentNode?: AstCompatNode` → `parentNode?: AstNode` in 3 callees consuming iterator output; **surfaced 3 dead-branch checks for `'ObjectProperty'` / `'StaticMemberExpression'` runtime types** (same territory as PR #44 — runtime emits `'Property'` / `'MemberExpression'`) — removed cleanly; restructured the `parentType === '...'` checks into inline `parentNode?.type === '...'` narrowings so `.computed` / `.shorthand` access type-checks against the strict union. **Cast count delta**: 163 → **151** (-12 in src/optimizer/). **Pragmatic-pivot scope**: this PR is *one* utility-tightening slice — the iterators. The ticket's ≤60-cast acceptance target is **not** hit here; per the OSS-389/OSS-390 precedent, OSS-388 closed as Done with Phase 2+ deferred as an unfiled follow-up (next-biggest cluster: `utils/simplify.ts` 10 casts in one file; then custom walk function signatures in `rewrite/const-propagation.ts` / `rewrite/index.ts` / `signal-analysis.ts`; then `getPatternProperties` return type). **Test sweep**: 1 fixture in `tests/optimizer/utils/ast.test.ts` (`mkNode` returns synthetic types `'A'`/`'B'`/`'Foo'` that don't satisfy the strict union — 2 predicate comparisons gained `(child.type as string)` casts; 1 local var retyped to `AstNode | null`). Net +80/-79 across 10 files (9 production + 1 test). Convergence 185/212 + full-suite 712/762 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (visitor signature narrowing + defensive cast removal); no phase/marker/convention change. **Pattern-locked observation**: this is the **third consecutive Phase 1 / Phase 2 split** (OSS-389 → OSS-390 → OSS-388). The pattern — ship the principled foundational slice, defer the consumer-side mechanical sweep — is now a stable workflow norm worth memory'ing on the next session if it lands a fourth time. Previous workstream — **Phase 8 = OSS-390 Phase 1 ([PR #107](https://github.com/thejackshelton/TS-Optimizer/pull/107))** — discriminated union for `TransformModule`. Two perfectly-correlated discriminators existed before (`origPath: string | null` and `segment: SegmentAnalysis | null`); replaced with a `kind: 'parent' | 'segment'` discriminator per CBP "Illegal states unrepresentable" rule. **What landed**: `TransformModule = TransformModuleParent | TransformModuleSegment`; parent variant has `kind: 'parent'`, `isEntry: false`, `segment: null`, `origPath: string`; segment variant has `kind: 'segment'`, `isEntry: true`, `segment: SegmentAnalysis`, `origPath: null`; `isEntry` now a variant-appropriate literal type rather than `boolean`; 4 construction sites updated (`transform/index.ts:530`, `module-cleanup.ts:578`, `segment-generation.ts:453` + `:1099`); stale JSDoc claim about stripped-segment placeholders setting `segment: null` removed — code always emits `segment: segmentAnalysis`, no third runtime state. **Pragmatic-pivot scope**: both variants retain the redundant null-arm fields (`segment: null` on parent, `origPath: null` on segment) so the **runtime object shape is unchanged** — only addition is the `kind` literal. Consumer narrowings of the prior form (`m.segment != null`, `m.segment?.name`) continue to work unchanged because the union's `segment` field remains `SegmentAnalysis | null` — structurally identical to the prior flat interface. Test sweep limited to 1 fixture in `types.test.ts` (was `origPath: null` + `isEntry: false`, illegal under the new union — now a valid parent shape; original test name preserved per REGRESSION.md's renamed-tests caveat, new variant-specific test added alongside). **0 `src/` consumer reads** on `.segment` / `.origPath` — internal code never branched on the discriminator, so no production sweep was needed. **What's deferred (Phase 2 follow-up, unfiled)**: drop redundant `segment: null` from parent / `origPath: null` from segment; mechanical sweep of ~30 consumer patterns in tests to `kind`-based narrowing; add NAPI/CLI serialization adapter if needed. Net +75/-22 across 5 files (4 production + 1 test). Convergence 185/212 + full-suite 711/762 baselines unchanged. **OPTIMIZER.md audit verdict: no update needed** — type-internal refactor of a result type that OPTIMIZER.md doesn't describe in detail (one mention at line 753 as a field-landing point; still accurate). Previous workstream — **Phase 7 = OSS-389 Phase 1 ([PR #105](https://github.com/thejackshelton/TS-Optimizer/pull/105))** — discriminated union for `ExtractionResult`. **Pragmatic-pivot scope**: during planning the audit underestimated the construct-new refactor required for "Full architectural" (53+ in-place mutation sites across the pipeline → 4-6 hours focused work + non-trivial regression risk). Mid-execution, surfaced the reality to the user; pivoted to ship the discriminator + variant types as Phase 1 with Phase 2 (consumer narrowing + construct-new + per-variant field-presence) deferred as a follow-up. **What landed**: new `phase` discriminator with three variants (`'extracted'` / `'captured'` / `'consolidated'`); `ExtractionBase` carries truly-immutable fields (readonly per OSS-387 preserved); `ExtractionPhaseFields` (mutable in this PR) carries cross-phase fields (`captureNames`, `paramNames`, `captures`, `parent`, `propsFieldCaptures`, `constLiterals`); `extractSegments` returns `readonly ExtractedSegment[]` as FFI-boundary contract; orchestrator widens via a single documented cast at the entry point; 4 in-place mutation sites that survived OSS-387 (prod rename, transpile-downgrade, stripped-segment loc, parent-rewrite raw-props consolidation) now use `Mutable<ExtractionResult>` / `Mutable<ConsolidatedSegment>` casts with FFI-boundary comments; new exported helpers `Mutable<T>` / `ExtractedSegmentBuilder` / `CapturedSegmentBuilder` / `ConsolidatedSegmentBuilder`; test fixtures updated with `phase: 'extracted'` discriminator + one cast at rewrite-parent test boundary. **NAPI byte-identical**: `SegmentAnalysis` constructed via explicit field picks at `segment-generation.ts:436` / `:1082` — `phase` does NOT propagate. **What's deferred (Phase 2 follow-up)**: consumer phase narrowing (most consumers still take the union; could narrow now), per-variant field-presence enforcement (`?: never` pattern from ticket not used), construct-new at phase transitions (in-place mutation preserved). Net +195/-98 across 7 files (5 production + 2 test). Convergence 185/212 + full-suite 711/762 baselines unchanged. Previous workstream — **Phase 6 = OSS-387 ([PR #103](https://github.com/thejackshelton/TS-Optimizer/pull/103))** — `readonly` discipline pass. **Public-API readonly coverage complete.** `readonly` token density in `src/` jumped 18 → 130 (~7×), matching the ticket's "~60 function signatures + ~50 interface fields" target. **All public NAPI types fully readonly**: `TransformModulesOptions` / `TransformModuleInput` / `TransformOutput` / `TransformModule` / `SegmentAnalysis` / `SegmentMetadataInternal` / `Diagnostic` / `DiagnosticHighlightFlat`. **Producer types readonly**: `ImportInfo`, `CustomInlinedInfo`, `MigrationDecision`, `ModuleLevelDecl` (minus `isExported` two-pass-mutated; documented), `InlineStrategyOptions`. **`ExtractionResult` is the load-bearing case**: 19 fields readonly + **14 left mutable with per-field comments** documenting where each mutation happens (disambiguation, prod rename, Phase 4-5 transforms, JSX-detection, transpile-downgrade, stripped-segment fallback, two-pass export-marking, etc.) — those 14 documented mutables are the natural surface for **OSS-389 (phased discriminated union for ExtractionResult)** to consume. **Function-parameter arrays** widened to `readonly Type[]` via the typecheck cascade: `matchesRegCtxName`, `isStrippedSegment`, `hasUnderscorePlaceholderParams`, `stripExportDeclarations`, `transformInlineSegmentBody.regCtxName`, `RewriteContext.stripExports`. **Constants** annotated `readonly string[]` rather than `as const` after the `as const` literal-union narrowing broke `.includes(arbitraryString)` callers; documented in commit. **Test fixtures**: 3 minor updates — `findModule` helpers widened to accept `readonly TransformModule[]`; one fixture mutation refactored to spread-construct a new options object. **Context types deferred** — the cascade-driven changes covered the high-leverage cases; the ticket's "flag for discussion mid-PR if it gets messy" carve-out applied. Net +181/-127 across 13 files (10 production + 3 test). Convergence 185/212 + full-suite 712/762 baselines unchanged. Previous workstream — **Phase 5 = OSS-386 ([PR #100](https://github.com/thejackshelton/TS-Optimizer/pull/100))** — third and final brand propagation (position + source-text). **Brand-propagation phase complete.** Position brands threaded through 6 `ExtractionResult` byte-offset fields (`callStart`/`callEnd`/`calleeStart`/`calleeEnd`/`argStart`/`argEnd`) + `loc: [ByteOffset, ByteOffset]` + 6 `DiagnosticHighlightFlat` fields (`lo`/`hi` as `ByteOffset`, `startLine`/`endLine` as `LineNumber`, `startCol`/`endCol` as `ColumnNumber`). Source-text brands: `TransformModuleInput.code: SourceText` (NAPI public surface) + `ExtractionResult.bodyText: BodyText`. **Pre-existing semantic bug surfaced + fixed**: `loc` is documented as `[byteStart, byteEnd]` and snap fixtures emit byte offsets, but 2 of 3 extraction emission sites populated it with `[line, col]` — hidden because convergence's strict-compare explicitly skips `loc`. The `ByteOffset` brand made it a compile error; aligned sites 2 and 3 to emit byte offsets. **New `buildHighlight(source, lo, hi)` helper** in `diagnostic-detection.ts` consolidates 3 identical diagnostic-emitter patterns into one — eliminates 18 inline brand wraps. **3 targeted regression tests** at `tests/optimizer/extract.test.ts` pin the byte-offset contract across all three emission paths (marker call, JSX attr, inlinedQrl); each asserts `loc[0] === argStart && loc[1] === argEnd` *and* validates the slice matches expected segment-body content. **Internal-vs-boundary strategy** (same as OSS-385's path-utils decision): 273 internal `.start`/`.end` AST reads stay as `number` (they're arithmetic operands); brand fires at the `ExtractionResult` contract boundary. Two `let body: string` / `let bodyText: string` annotations in `rewrite/inline-body.ts` + `segment-codegen.ts` opt internal mutation locals out of inferred `BodyText`. Net +298/-209 across 18 files (7 production + 11 test/fixture). **Linear auto-flip mystery resolved — there is no GitHub→Linear integration installed on this repo.** Confirmed by the user post-merge. Both prior hypotheses (uppercase OSS-XXX convention in PR #96, closing-keyword `Closes OSS-XXX` in PR #99) were based on the false premise that an integration existed but was misconfigured. Six consecutive "auto-flip misses" (OSS-382/383/384/393/385/386) were all the expected behavior — there's no integration to fire. **Cleanup needed in follow-up PRs**: (1) revise LINEAR.md's "Ticket references" section to remove the auto-flip-driven justification for uppercase/closing-keyword (both are still preferable for readability/grep-ability, but they don't gate auto-flip because there is no auto-flip); (2) revise the `/wrap-up` skill Step 3 to expect manual `issueUpdate` flip as the *norm*, not the fallback. The wasted-debug cost was real but the resolution is clean — manual flip via `issueUpdate` is the correct path going forward, full stop. Previous workstream — **Phase 4 = OSS-385 ([PR #97](https://github.com/thejackshelton/TS-Optimizer/pull/97))** — second brand propagation (path-family + CtxName). New module additions: zero — pure threading through existing types. Producers branded: `path-utils.ts` high-level functions (`computeRelPath`, `computeParentModulePath`, `isRelativePathInsideBase`) take/return `FilePath`/`RelativePath`; low-level string manipulators (`getBasename`, `getDirectory`, `getExtension`, `stripExtension`, `getFileStem`) intentionally stay `string`-typed because they work on any path-shaped input and branding them would force casts at every consumer; `extract.ts` ctxName resolution + origin construction branded via `mkCtxName` / `mkOrigin`. Type definitions: `ExtractionResult.{origin: Origin, ctxName: CtxName}`; `SegmentAnalysis.{origin, ctxName}` (cascades to `SegmentMetadataInternal`); `TransformModulesOptions.srcDir: FilePath`; `TransformModuleInput.path: FilePath`; `TransformModule.path: RelativePath`; `RewriteContext.relPath: RelativePath`; `buildPassthroughModule`/`rewriteParentModule` `relPath` params branded. **Call-site sweep**: ~17 production-error sites + ~130 test-fixture sites (delegated to a general-purpose agent for the bulk test-file mkFilePath/mkOrigin/mkCtxName wrapping). **Three in-flight OSS-383 brand-validation refinements** (same pattern as OSS-384): (1) `computeRelPath` fallback tightened to strip leading `/` so the output always satisfies `RelativePath`'s no-leading-slash contract — new unit test pins the OSS-385 semantics; (2) `mkCtxName` regex loosened to accept bare `$` standalone (the bare-`$()` marker's ctxName is literally `'$'` per `tests/optimizer/extract.test.ts:55`); (3) `mkCtxName` regex further loosened to accept hyphens in the body (JSX accepts dashed attribute names — `on-cLick$` in `example_jsx_listeners` and `should_convert_jsx_events` would have thrown without this). Each loosen documented at the brand constructor with the production fixture that justifies it. Net +263/-172 across 22 files; baselines preserved (convergence 185/212 + full-suite 705/735 → 709/739 after baseline absorbs +4 new tests). **The uppercase-OSS-XXX convention test failed** — PR title was `refactor(OSS-385): ...` (uppercase) and the body had `Refs: OSS-385`, but Linear still didn't auto-flip In Review → Done. Real finding: the convention isn't about case sensitivity; the GitHub→Linear integration likely needs `Closes OSS-XXX` / `Fixes OSS-XXX` (the closing-keyword convention) in the PR body, not `Refs: OSS-XXX`. Hypothesis: switch future PR bodies from `Refs:` to `Closes` for ticket references. To verify on the next OSS-XXX merge. **Phase 3 = OSS-384 merged earlier via [PR #91](https://github.com/thejackshelton/TS-Optimizer/pull/91)** — first brand propagation. Symbol-family brands (`SymbolName`, `Hash`, `CanonicalFilename`, `DisplayName`) threaded through producer chain (`qwikHash`, `buildDisplayName`, `buildSymbolName`, `context-stack.ts:getDisplayName`/`getSymbolName`) and canonical type definitions (`ExtractionResult`, `SegmentAnalysis`, `SegmentMetadataInternal` via `extends`); `parent: SymbolName | null` branded as an in-scope extension; `preRenameSymbolName: Map<SymbolName, SymbolName>` typed both sides. **Call-site sweep surprise**: expected ~170 edits, hit ~17 — brand→string covariance preserved ~150 read sites unchanged; only construction sites needed explicit smart-constructor calls. **OSS-383 brand-validation refinements landed in-flight**: three regexes loosened to match real production values rather than the over-specified strict shapes from OSS-383: (1) `DisplayName` / `CanonicalFilename` regex tightened to non-empty-only (production values include `[[...slug]].tsx_slug_component` Qwik catch-all routes, `404.tsx__404_component` digit-leading filenames, `test.spec.tsx_Foo` multi-dot filestems); (2) `Hash` regex loosened from `{11}` strict to `[A-Za-z0-9_]+` (the inlinedQrl extraction parser at `extract.ts:424` falls back to short alphanumeric names like `"task"` and whole-symbol-name fallbacks like `"Foo_component_bbb"` for peer-tool input we don't control — brand matches what the parser already accepts; still catches whitespace, `.`, `-`, `/` at the boundary; TypeScript brand-marker distinction provides swap-prevention safety regardless of regex shape). Code comments at each constructor document the reasoning so future readers don't try to "fix" the looseness. Original-named tests preserved alongside new tests for the loosened cases (keeps the CI gate green per REGRESSION.md's "renamed tests count as missing" caveat). Net +194/-76 across 12 files. **Three Linear auto-flip misses in a row** (OSS-382, OSS-383, OSS-384) — the GitHub→Linear integration's PR-title parser is case-sensitive on the ticket prefix, and the project's commit-message convention uses lowercase (`refactor(oss-XXX): ...`). Pattern is now strong enough to warrant either (a) switching to uppercase `OSS-XXX:` in PR titles or (b) hardening the `/wrap-up` skill to skip the probe and just flip directly. Next up in Phase 4: **OSS-385 (path-family + `CtxName` brand propagation)** — applies `Origin`, `RelativePath`, `FilePath`, `CtxName` brands to consumer types. Previous workstream — **Phase 2 = OSS-383 ([PR #89](https://github.com/thejackshelton/TS-Optimizer/pull/89))** — brand-types foundation. New module `src/optimizer/types/brands.ts` (179 lines) defining 13 branded primitive types in 4 sections (identifier: `SymbolName`, `Hash`, `CanonicalFilename`, `DisplayName`, `CtxName`; path: `Origin`, `RelativePath`, `FilePath`; source-text: `SourceText`, `BodyText`; position: `ByteOffset`, `LineNumber`, `ColumnNumber`) with one shape-validating smart constructor per brand. Validation strategy is shape-specific not uniform: identifier brands enforce strict `/^[a-zA-Z_$][a-zA-Z0-9_$]*$/`; `Hash` enforces the documented Qwik 11-char base64url-with-safety-rewrite shape (`/^[A-Za-z0-9]{11}$/`); `CtxName` enforces `/^[a-zA-Z_][a-zA-Z0-9_:]*\$?$/` admitting both marker callees (`component$`, `componentQrl`, `component`) and namespaced JSX attrs (`onClick$`, `document:onScroll$`, `bind:value$`); position brands enforce `Number.isInteger` + non-negative; source-text brands are pass-through. Failures throw per CBP "errors reserved for defects" rule — invalid brand construction is a defect, not a recoverable condition. 20 runtime accept/reject tests at `tests/optimizer/types/brands.test.ts`; compile-time enforcement at `tests/optimizer/types/brands.compile-test.ts` using `@ts-expect-error` to assert that cross-brand assignment, plain-string-to-brand, and swapped-arg function calls all fail to compile (also pins `SymbolName → string` direction as still working so brands remain usable in concatenation/logging/JSON). **Foundation-only — zero existing files touched**; propagation through `SegmentAnalysis`/`ExtractionResult`/consumer signatures lives in OSS-384 / OSS-385 / OSS-386. **Next up in Phase 3: OSS-384 (`readonly` discipline)** — promote params, fields, and arrays to default-`readonly`; MagicString accumulator as the explicit local exception. Previous workstream — **Phase 1 = OSS-382 ([PR #87](https://github.com/thejackshelton/TS-Optimizer/pull/87))**. Two coordinated cleanups: **Part A** eliminated 4 `as unknown as Record<string, unknown>` double casts in AST-iteration loops (`segment-codegen.ts`, `signal-analysis.ts`, `rewrite/raw-props.ts`, `utils/props-field-rewrite.ts`) by delegating to the existing `forEachAstChild` from `utils/ast.ts`; added new short-circuit sibling `someAstChild` for `raw-props.ts`'s "any return statement?" predicate (sat in the negative space next to `forEachAstChild` the whole time — once the four hand-rolled loops collapsed into the same shape, the short-circuit version named itself). **Part B** narrowed 3 sites from `any` to real closed unions with `_exhaustive: never` defaults: new exported `BindingPatternLike` union (`BindingPattern | BindingRestElement | FormalParameterRest | TSParameterProperty`) in `utils/binding-pattern.ts` — `visitBindingNames` + all 3 exported wrappers exhaust it; `collectBindingPositions` + 2 local wrappers in `variable-migration.ts` migrated to the same union; `processJsxTag` in `transform/jsx.ts` narrowed to `JSXElementName | null | undefined` with the same `never` default. Both narrowed switches gain the previously-missing `TSParameterProperty` arm (was reachable in TS class fixtures but fell through to a `// no-op`). 11 new unit tests at `tests/optimizer/utils/ast.test.ts` pin `forEachAstChild` + `someAstChild` contracts (short-circuit, meta-key skip, key/parent threading). Per the new policy from OSS-378: foundational refactors ship with focused unit tests alongside the snap suite. Audit identified 8 additional "wide-input filter" switches (different shape — filter `any`-typed input against a small known set) deferred to a follow-up. **Established workflow norm this session: planning-first** — audit → ticket draft in chat → user approval → code (memory: `feedback_plan_before_implement`). Net +236/-86 across 9 files; baselines preserved (convergence 185/212 + full-suite 670/720). **Next up in Phase 2: OSS-383 (brand types foundation)** — `SymbolName`, `Hash`, `CanonicalFilename`, `DisplayName`, `Origin`, `CtxName`, etc. as the next-biggest leverage item in the OSS-381 arc. Previous workstream — three landings since the code-health pass closed: **OSS-378 → OSS-379 → OSS-380**. **`example_qwik_react` is now passing** (convergence 184 → 185 / 212) — the three-ticket arc completed the fix. **OSS-379 ([PR #83](https://github.com/thejackshelton/TS-Optimizer/pull/83))** shipped the `jsx() → _jsxSorted()` peer-tool transform, the third and final bug behind `example_qwik_react`. `qwik-react` codegen emits `jsx(Tag, propsObj)` calls directly (pre-processed from JSX syntax) instead of leaving JSX for the optimizer to rewrite — the existing JSX-syntax transform skipped these because its regex gate only matches `<JSX/>` and its Phase-5 gate requires `.tsx`/`.jsx` extensions (qwik-react ships as `.mjs`). New module `src/optimizer/transform/jsx-call-transform.ts` (~190 LOC) mirrors SWC's `handle_jsx` at `swc-reference-only/transform.rs:1163`: `collectJsxFunctionNames` derives the JSX-runtime callable set from `importContext.moduleImports` (any local from `@qwik.dev/core/jsx-runtime` / `jsx-dev-runtime`, plus named `jsx`/`jsxs`/`jsxDEV` from `@qwik.dev/core`); `transformJsxCalls` walks bottom-up via `leave` (so nested calls rewrite inside-out — `jsx(Host, { children: jsx(Inner, {}) })` works correctly) and emits `_jsxSorted(tag, varProps, constProps, children, flags, key)`. New Phase 5b in `generateSegmentCode` runs unconditionally with a cheap fast-path skip when no JSX-runtime imports are present. 11 regression tests in `tests/optimizer/jsx-call-transform.test.ts` (5 unit on `collectJsxFunctionNames`, 6 integration through `transformModule`); 4 of the 6 integration tests fail on `main` (verified by stash). PR review feedback collapsed 5 sites of adjacent `if (cond) return X;` chains into combined-condition guards (new memory: `feedback_fold_adjacent_if_return_chains` — same pattern as OSS-363 PR-review feedback). **Scope intentionally narrow** — matches SWC's skip-and-leave behavior for spread props inside propsObj, non-ObjectExpression args, and signal analysis on prop values; none of the affected fixtures need those. Previous workstream — **OSS-380 ([PR #82](https://github.com/thejackshelton/TS-Optimizer/pull/82))** stopped tracking `ts-output/` in git. 209 stale snap files removed; the user-supplied recommendation B (untrack entirely) won over option A (regenerate) because `ts-output/` is purely a write-target of `pnpm vitest convergence` for human inspection — the convergence test reads from `match-these-snaps/`, not from `ts-output/`. Audit confirmed no code or scripts read from the directory. README updated; `.gitignore` updated. Small follow-up landed as PR #84 — gitignored the two other transient vitest outputs the workflows / `REGRESSION.md` write to (`ci-full.json` + `local.json`); no-Linear-ticket chore since it's a 2-line `.gitignore` change. Previous workstream — **OSS-378 ([PR #80](https://github.com/thejackshelton/TS-Optimizer/pull/80))** addressed 2 of 3 bugs blocking `example_qwik_react` convergence. **Bug 1**: `transform/index.ts:324` was filtering `extractions.filter((e) => !e.isInlinedQrl)` before passing to `computeSegmentUsage`, so module-level decls referenced inside `inlinedQrl` bodies (e.g. peer-tool `filterProps`) were attributed to `rootUsage` instead of segment usage — migration produced `KEEP_UNUSED`, the `_auto_` re-export was never emitted, and segment files imported the raw name. Fixed by passing the full `extractions` list (and generalizing the captureNames augmentation loop alongside). **Bug 2**: `segment-codegen/import-collection.ts:addQrlCalleeImports` had a regex-scan path that emitted `import { <X>Qrl } from "@qwik.dev/core"` for *any* identifier ending in `Qrl` (the `getQrlCalleeName(name.slice(0,-3)+'$') === name` gate is trivially true since the function literally re-suffixes `Qrl`). User-named locals like `function qwikifyQrl(reactCmpQrl)` got a spurious `import { reactCmpQrl } from "@qwik.dev/core"` emitted — which would have crashed at runtime. Fixed by gating on `importContext.moduleImports.some(m => m.localName === qrlName)` (which already includes the legitimate runtime-imports list). New `tests/optimizer/inlined-qrl-regressions.test.ts` (4 cases — 2 pin the fixes, 2 are neighboring guards). Per the new policy this session: **parity fixes ship with focused regression/unit tests alongside the snap suite** (memory: `feedback_parity_fixes_need_regression_tests`); snap suite is coarse + fragile, the unit tests pin the contract. Net +165/-8 across 3 files. `example_qwik_react` remains baseline-failing pending **Bug 3 = OSS-379 (Backlog)**: the `jsx() → _jsxSorted()` transform for `@qwik.dev/core/jsx-runtime` calls inside `inlinedQrl` bodies (~200-400 LOC new transform; comparable scope to a moderate F8 fix). Also surfaced & filed separately: **OSS-380 (Backlog)** — committed `ts-output/*.snap` files are stale relative to current main's optimizer output (e.g. dead `import { $ } from "@qwik.dev/core"` lines that current code emits but the committed snap doesn't have). Recommendation in OSS-380: stop tracking `ts-output/` in git (it's a transient artifact of `pnpm vitest convergence`; the convergence test reads from `match-these-snaps/`, not from `ts-output/`). Previous workstream — **OSS-377 ([PR #78](https://github.com/thejackshelton/TS-Optimizer/pull/78))** consolidated all three q:p-delivery orchestration helpers in `src/optimizer/transform/event-capture-promotion.ts` via a new shared `EventCaptureContext` (10-field plumbing cluster: `extractions`, `closureNodes`, `bodyScopeIds`, `moduleScopeIds`, `importedNames`, `enclosingExtMap`, `extractionLoopMap`, `allScopeEntries`, `loopBodyVarDecls`, `repairedCode`). All three sibling functions collapsed to `(ctx, globalDeclPositions)` — `promoteEventHandlerCaptures` 12→2 args (also dropping the dead `_program: AstProgram` parameter that the orchestrator was silently passing into an unused slot), `unifyParameterSlots` 5→2 args, `buildElementCaptureMap` 5→2 args. Design decision: `globalDeclPositions` stays as an explicit second argument rather than going into the context because it's the mutation target linking the three calls (`promote` writes; `unify` + `buildElement` read), and the write/read/read sequence is structurally clearer when visible at the call site. Stale `ImportInfo` import dropped; `ScopeEntry` exported so the context can reference it. Net +51/-40. Baselines unchanged. **This closes the "Reduce function parameters" queue** — the parameter-explosion sub-thread of the code-health pass is now complete (back-to-back-walk sub-thread closed earlier with OSS-373). Previous workstream — **OSS-376 ([PR #76](https://github.com/thejackshelton/TS-Optimizer/pull/76))** consolidated `processChildren` (`src/optimizer/transform/jsx-children.ts:119`) from **8 → 3 args** (`ctx, children, opts`) via the shared `JsxTransformContext` plus a new `ProcessChildrenOptions` (`neededImports: Set<string>` required, `enableSignalAnalysis?: boolean` defaulting to true). Both internal helpers (`processOneChild`, `processExpressionChild`) migrated to the same shape. **The wrinkle** that made this trickier than OSS-374/375: the `transformJsxElement` call site historically passed `undefined` for `importedNames` and `signalHoister` whenever `childSignalsEnabled` was false (text-only HTML tags, components opting out via `enableChildSignals: false`), and `processChildren` used `if (importedNames && signalHoister)` and `if (importedNames)` as twin gates. Resolved by collapsing both gates into a single `if (!enableSignalAnalysis) return early` at the top of `processExpressionChild` — safe because the call site always toggled both flags together as a unit. Dead `s?` optional in the helpers removed (`JsxTransformContext.s` is non-optional and the `source.slice` fallback was unreachable in practice). **JSX-family parameter-explosion queue now complete** — `promoteEventHandlerCaptures` (13 args, `event-capture-promotion.ts`) is the only remaining offender for the rule, but it operates in a different domain (event-handler / loop-capture analysis) and needs its own bespoke context type. Net +75/-72. Baselines unchanged. Previous workstream — **OSS-375 ([PR #75](https://github.com/thejackshelton/TS-Optimizer/pull/75))** consolidated `transformJsxFragment` from **9 → 2 args** (`ctx, node`) — the cleanest of the parameter-explosion landings. Every arg except the dead `_isSoleChild` mapped onto `JsxTransformContext`; production call site was silently passing `isChildFragment` into the unused slot. Unused `SignalHoister` + `MagicString` imports in `jsx-elements-core.ts` cleaned out as a side-effect. 2 test call sites migrated via the existing `makeCtx` helper. Net -18 lines. Previous workstream — **OSS-374 ([PR #73](https://github.com/thejackshelton/TS-Optimizer/pull/73))** opened a new sub-thread targeting the "Reduce function parameters" rule. `transformJsxElement` (`src/optimizer/transform/jsx-elements-core.ts:274`) — the worst offender in the codebase at **15 positional args** — reduced to **3 args** via a new shared `JsxTransformContext` (transform plumbing + scope sets — `source`, `s`, `importedNames`, `keyCounter`, `signalHoister`, `constIdents`, `allDeclaredNames`, `paramNames`, `qrlsWithCaptures`) and `JsxElementOptions` (per-element `passiveEvents`, `loopCtx`, `isSoleChild`, `enableChildSignals`, `qpOverrides`). `processProps` (`jsx-props.ts:66`) similarly went from 12 → 3 args via the same `JsxTransformContext` plus `ProcessPropsOptions`. The `_paramNames` parameter inside `processProps` (underscore implying unused, but actually read in const-deps classification at line 289) renamed to `paramNames`. Single external call site for each refactored function. 13 unit-test call sites in `tests/optimizer/jsx-transform.test.ts` migrated via a local `makeCtx` helper. Subtle pre-existing bug surfaced by the refactor: the old positional call wired `willUseCreateElement` (12th arg) to `skipSignalAnalysis` (12th param name) — same wire, different names. Resolved by keeping the function's internal name (which describes behavior) and setting `skipSignalAnalysis: willUseCreateElement` at the call site (which describes the caller's reason); JSDoc on `ProcessPropsOptions.skipSignalAnalysis` documents the linkage. Net +109/-89. Baselines unchanged. The same `JsxTransformContext` is intended to thread through the remaining queue (`transformJsxFragment` 9 args, `processChildren` 8 args) — subsequent tickets become mechanical context-threading. Previous workstream — **OSS-373 ([PR #71](https://github.com/thejackshelton/TS-Optimizer/pull/71))** unified the two `walk(program, ...)` calls inside `computeSegmentUsage` (`src/optimizer/variable-migration.ts`) into a single walk + buffered post-pass. The prior shape ran `collectAllLocalDeclarations` first (gather per-extraction declared names from `DECLARATION_TYPES` nodes), then a second walk classifying every `Identifier` / `JSXIdentifier` against those locals. Pass 2 has an output-dependency on Pass 1 — and unlike OSS-372's source-order DFS trick, classification cannot happen inline in `enter` here because DFS visits hoisted-decl-referenced identifiers *before* the declaration itself (e.g. `function f() { g(); function g() {} }` — the `g` Identifier is entered before `function g` is visited and adds `g` to locals). The merge buffers identifier visits during the walk and runs the classification logic in a post-walk linear pass once the locals map is fully populated. Total work identical to the two-pass version; one full-tree traversal eliminated. `collectAllLocalDeclarations` removed (no remaining callers). Net -10 lines (-60/+50). Baselines unchanged. STATE.md had flagged this file as "warrants extra care" because migration policy (MIG-01 through MIG-05a) is load-bearing; the buffered shape preserves bit-for-bit semantic equivalence with the two-pass version. Closes the last walk-merge candidate from the post-OSS-371 recon; the "back-to-back walks" sub-thread of the code-health pass is now complete. Previous workstream — **OSS-372 ([PR #69](https://github.com/thejackshelton/TS-Optimizer/pull/69))** unified the two-pass `walk(program, ...)` in `src/optimizer/utils/flatten-destructures.ts` into a single source-order DFS walk. Different shape from the prior side-by-side merges (OSS-369/370): Pass 2 (identifier rewriting) legitimately depends on Pass 1's output (`FlattenableDecl[]`), so the merge relies on DFS ordering — when the walker enters a `component$()` CallExpression, a synchronous one-level-deep `body.body` iteration collects decls + overwrites pattern ranges + registers substitutions BEFORE the walker descends into the body where identifiers are visited. `collectFlattenableDecls` (the standalone Pass 1 walker) replaced by non-walking helper `collectAndApplyDeclsForComponentCall(callNode, s, decls, subsByScope)`. Three load-bearing invariants preserved exactly: the OSS-365 BENCH-01 pattern-range skip (shorthand-Property value-side Identifier collision); outermost-scope-wins for nested `component$()` (decls iterated in source order, first scope match returns); manual one-level-deep `body.body` iteration (not piggybacking on the walker's VariableDeclaration visits). Net +16 lines (-93/+109). Baselines unchanged. Previous workstream — **OSS-371 ([PR #67](https://github.com/thejackshelton/TS-Optimizer/pull/67))** unified the per-statement `walk(stmt, ...)` loop in `buildPassthroughModule` (`src/optimizer/transform/module-cleanup.ts:534-546`) into a single `walk(program, ...)` with `this.skip()` gated on `parent?.type === 'Program'` for top-level `ImportDeclaration` + `VariableDeclaration`. Same loop-walk shape as OSS-364 (`event-capture-promotion.ts`) and OSS-366 (`diagnostic-detection.ts`): N walks (one per non-import / non-VarDecl top-level statement) collapsed to 1. The parent-Program guard preserves the original semantics exactly — identifiers inside nested VariableDeclarations within top-level `FunctionDeclaration` / `ExportNamedDeclaration` bodies are still collected, because only **top-level** Import/VarDecl get skipped. Smallest of the eight code-health PRs (+14/-12 = net +2 lines). Baselines unchanged. Previous workstream — **OSS-370 ([PR #65](https://github.com/thejackshelton/TS-Optimizer/pull/65))** unified `collectConstBindings` + `collectAllLocalNames` in `src/optimizer/transform/jsx.ts` into a single `collectConstAndLocalNames(program): { constBindings, allLocalNames }` walker. Same shape as OSS-369: two functions visiting the same `VariableDeclaration` nodes back-to-back, merged into one walk producing both result sets. `collectConstBindings` filtered on `kind === 'const'` then per-declarator did pattern-init classification; `collectAllLocalNames` filtered on every binding-introducing node and extracted leaves — both now fire at the same `VariableDeclaration` walk site in the unified version. `transformAllJsx`'s `constIdents` param replaced with `precomputedConstAndLocal?: ConstAndLocalNames`, reducing the internal back-to-back calls to one (or zero, when precomputed). `segment-codegen.ts:297` dropped its standalone `collectConstBindings` walk and augments the bundle's `constBindings` with `captureInfo.captureNames` in place. The standalone `collectConstBindings` export was removed (no remaining external callers). Every JSX path drops from 2 walks → 1 walk per call. Net -7 lines (-55/+48). Baselines unchanged. Previous workstream — **OSS-369 ([PR #63](https://github.com/thejackshelton/TS-Optimizer/pull/63))** consolidated `collectReactiveRoots` + `collectAllDeps` in `src/optimizer/signal-analysis.ts` into a single `collectSignalDeps(node, importedNames, localNames?)` returning `{ roots, allDeps }`. Both functions had been called back-to-back on the same expression tree at three call sites (ObjectExpression / ArrayExpression / `tryBuildFnSignal`); the unified walker eliminates the duplicate traversal. Three semantic divergences were resolved in the merge: (1) `localNames` is now applied consistently to store-checks — names outside localNames flow through bare-ident collection instead of store-root collection but land in the sorted `allDeps` the same way; (2) complex signal-value objects like `(a || b).value` use the `collectIdentifiersFromExpr` fallback for both outputs (old `collectAllDeps` silently produced empty output — latent gap fixed); (3) `Property.key` and non-computed `MemberExpression.property` are skipped during descent for both outputs (safe because signal/store detection happens at the MemberExpression level). `collectReactiveRoots` retained as a thin wrapper for the 2 single-output call sites in `analyzeMemberExpression`; `collectAllDeps` removed. Net +77/-70 lines. Baselines unchanged; one debug-aggregator test (`segment-only-debug.test.ts`) flipped passing as a neutral side-effect of the snapshot count. Previous workstream — **OSS-368 ([PR #61](https://github.com/thejackshelton/TS-Optimizer/pull/61))** eliminated per-extraction sub-tree walks in `src/optimizer/extract.ts`. The old shape ran a `collectIdentifiers(bodyNode)` walk for *every* marker call detected — files like `example_qwik_router_client` (dozens of segments) paid this cost N times. The new shape extends the existing `activeSegmentBodies` stack (already a lever for JSX detection per OSS-351 / OSS-352) with a `bodyIds: Set<string>` field. The outer extraction walk's `enter` handler now tags every Identifier name into all currently-active bodies' `bodyIds` (O(activeStack.length) per Identifier — typically 1–2 due to nesting depth); the `leave` handler finalises `result.segmentImports` via a new `filterImportsByIds` helper. Three push-sites (inlinedQrl branch, marker-call branch, JSX-attr branch) build with `segmentImports: []` and a fresh `bodyIds` set; the cold path (inlinedQrl with non-function arg0 — no fixture exercises it) preserves the inline `collectSegmentImports` call. Net +51/-11. Baselines unchanged. Previous workstream — **OSS-367 ([PR #59](https://github.com/thejackshelton/TS-Optimizer/pull/59))** consolidated the two walks in `collectBodyIdentifiers` (`src/optimizer/segment-codegen/import-collection.ts:33`) into a single pass via a `bareIds` side-set. Walk #1 (always-run) tracked funcNode + JSXIdentifier-Uppercase; walk #2 (fallback when no function detected) collected all plain Identifier names. Now: one walk accumulates funcNode + JSXIdentifier-Uppercase into `ids` AND all plain Identifiers into `bareIds`. Post-walk, the funcNode discriminator picks the right merge — common path runs `getUndeclaredIdentifiersInFunction` and discards `bareIds`; fallback path merges `bareIds` into `ids`. Smallest of the four code-health PRs (+13/-5) because the fallback is the rare path, but mechanically the right shape and the bare-id collection is trivial cost on the common path. Recon during OSS-367 surfaced a much larger out-of-scope opportunity (documented in the ticket): the same segment body gets parsed 5–7 times across `generateSegmentCode`'s 9 phases, each phase mutating between parses. Trickier than OSS-365's post-process cache and deserves a discovery ticket once a concrete perf signal motivates it. Previous workstream — **OSS-366 ([PR #57](https://github.com/thejackshelton/TS-Optimizer/pull/57))** collapsed per-name walks in `diagnostic-detection.ts` to single-pass collection helpers — `findCallSites` (1 walk per `$`-suffixed export target) became `collectCallSitesByName` (1 walk total, keyed by Set lookup); `findIdentifierReferenceSite` (1 closure-subtree walk per fn/class reference) became `collectIdentifierReferenceSites` (1 walk per extraction with early-exit preserved via `remaining`-counter + `out.has` guard). The 7 if-statement declaring-position exclusion list folded into a single switch helper (byte-equivalent). Net +111/-50; baselines unchanged; all 19 `diagnostics.test.ts` cases green. **OSS-365 ([PR #56](https://github.com/thejackshelton/TS-Optimizer/pull/56))** eliminated substring re-parse in `module-cleanup.ts` and threaded a shared parse-cache across the 4-helper post-process pipeline (`applySegmentConstReplacement` / `applySegmentDCE` / `applySegmentSideEffectSimplification` / `injectUseHmr` / `removeUnusedImports`). `applySegmentSideEffectSimplification` now parses the **full** code and filters its walk by `node.start >= exportStart` instead of parsing a substring (which fed bad source positions into diagnostics). `postProcessSegmentCode` keeps a `cachedProgram` invalidated by string-identity check on each helper's return value — no helper mutates and you parse at most once for all four steps. PR #56 also pinned an F8c regression caught by `BENCH-01`: `flatten-destructures.ts` was crashing on two flattenable decls in the same scope (`Cannot split a chunk that has already been edited (45:10 – "{ url }")`) because the per-decl id-range "skip if inside pattern" check only saw the current loop iteration's decl. The fix hoists the check out of the per-decl loop; new `tests/optimizer/flatten-destructures.test.ts` (7 cases) pins the regression. Convergence tests didn't surface this — only the real Qwik monorepo bench did, which is why we added a functional repro now.

Previous workstream — [OSS-364](https://linear.app/kunai/issue/OSS-364) merged via PR #54: `event-capture-promotion.ts` reduced from **5 → 2** `walk(program, ...)` calls. Three per-extraction walks (inside `promoteEventHandlerCaptures`'s for-loop, giving O(extractionCount × programSize) work) replaced with O(1) lookups against pre-collected data. Two extensions to setup-time helpers: `ScopeEntry.bindings` now carries a `kind: 'param' | 'let' | 'const' | 'var' | 'function' | 'class'` field (`collectAllScopeEntries` walks once anyway, just tags during traversal); `buildExtractionLoopMap` also produces a `LoopBodyVarDeclMap` keyed by `${loopBodyStart}-${loopBodyEnd}` (same single walk; the loop stack maintained for extraction-tagging now also feeds var-decl bucketing). The two remaining walks (`buildExtractionLoopMap` line 39, `collectAllScopeEntries` line 127) could theoretically be combined into a single walk — listed as optional follow-up; out of scope because they're independent helpers with different return shapes. Behaviour preserved exactly — F1b / F8b / generic q:p delivery all still work. Recon-pass identified the next worst offenders for follow-up tickets: `module-cleanup.ts` (5 parses + 4 walks), `diagnostic-detection.ts` (3 walks). Previous workstream — F8c ([OSS-363](https://linear.app/kunai/issue/OSS-363)) merged via PR #52 — `should_wrap_prop_from_destructured_array` flipped. The largest F8 fix yet: three SWC-specific optimizations from `swc-reference-only/props_destructuring.rs` ported to production per user preference ("port the optimizations, not paper over in compareAst"). (1) `useTask$` rawProps over-eager gate tightened — `analyzeRawPropsTransform` now requires the arrow body to be a direct CallExpression OR a BlockStatement with at least one ReturnStatement (matches SWC's `visit_mut_arrow_expr` heuristic at `props_destructuring.rs:288-307`). Restores source destructure for `useTask$(({track}) => {track(...); track(...);})` while preserving consolidation for component$ + useAsync$. (2) Compound-destructure const-binding classification — `collectConstBindings` (`transform/jsx.ts`) now walks pattern + init in parallel; for `const [store, math] = [useStore(...), Math.random()]`, `store` is classified const (init element is `useStore(...)`), `math` is not. Incidentally fixed the button q:p/q-e:click bag split since the const classification propagates through JSX prop classification. (3) Object-destructure flattening — new module `src/optimizer/utils/flatten-destructures.ts` ports `transform_component_body`'s `const { X } = use*()` → `const flat = use*()` rewrite. Naming rule: strip `use` prefix, lowercase first remaining char (`useForm2` → `form2`). New Phase 0.5 step in `transform/index.ts` between repair and extract; re-parses if changes applied. The `_fnSignal` over-dedup sub-bug fell out naturally — once the source becomes `form2.store5.errors.test`, the signal-analysis body diverges from the `p0.errors.test` body the other errors share. Two PR-review-driven follow-up commits collapsed two `if-chain` smells: an 11-way parent-type guard in `flatten-destructures.ts` extracted as `isDeclaringIdentifierPosition` switch; three consecutive `if/continue` guards in `walkPatternInit` folded into single combined conditions plus a `staticPropKeyName` helper. Previous workstream — F8b ([OSS-362](https://linear.app/kunai/issue/OSS-362)) merged via PR #50 — `should_transform_three_nested_loops_handler_captures_outer_only` flipped. The bug was misframed in CONVERGENCE_FAILURES.md ("capture analysis fails to detect outer-loop var") — capture analysis was actually correct; the real bug was downstream loop-cross-capture wiring not firing for handlers with cross-scope captures but no `_, _1` loop-iter padding (because the handler doesn't reference immediate-loop iter vars). Five coordinated production changes: (1) `transform/segment-generation.ts` `hasLoopCrossCaptures` now fires on a second arm — "in any loop AND has cross-scope captures" — via `extractionLoopMap`; (2) `segment-codegen.ts` `buildQrlsWithCapturesSet` includes QRLs with `hoistedSymbolName` (not just `loopLocalParamNames`); (3) `transform/jsx-elements-core.ts` `injectQpProp` accepts `qrlsWithCaptures` and suppresses the iterVars q:p fallback when an event handler's QRL has hoisted captures; (4) `transform/jsx-props.ts` `isRewrittenEventProp` branch now sets `hasVarEventHandler=true` like the source-form branch (was missing); (5) `transform/module-cleanup.ts` `extractBinaryOperandIdentifiers` (regex on source text — would match identifier-like substrings inside string literals like `'p'`) replaced with AST-aware `extractBinaryOperandIdentifiersFromAst`. Previous workstream — F8a ([OSS-361](https://linear.app/kunai/issue/OSS-361)) merged via PR #48 — `example_getter_generation` flipped via a new `src/optimizer/utils/simplify.ts` module. The fix is named after SWC's `simplify::simplifier` pass (explicitly invoked from `swc-reference-only/parse.rs:360`) which the Qwik Rust optimizer's authors intentionally run after `qwik_transform` to evaluate compile-time-constant subtrees. PR feedback redirected an initial compareAst-only attempt back to production code: SWC's folding is an intentional optimization (shrinks emitted code), not parity noise, so the right home is the optimizer. New module exports `simplifyExpression(node)` + `formatSimplifiedLiteral(value)`; covers BinaryExpression / UnaryExpression / LogicalExpression / ConditionalExpression with primitive Literal operands; conservative about BigInt / divide-by-zero / exotic coercions. Wired into `transform/jsx-props.ts` so JSX prop expressions get simplified before slicing source text. Scope is narrow (JSX prop values only) — broader expression-level coverage matching SWC's full simplifier is future work. Previous workstream — F1b ([OSS-360](https://linear.app/kunai/issue/OSS-360)) merged via PR #46 — mutual-recursion component migration flipped. `example_self_referential_component_migration` now passes; convergence **180 → 181 / 212**. Six coordinated changes: (1) marker calls treated as side-effect-free in `isInitializerSafe` so MIG-04 stops trapping single-segment marker decls; (2) `tryBuildMarkerDeclMove` synthesises the `componentQrl(q_*)` + supporting qrl-ref pair when a marker decl is moved into a sibling segment file (raw source would re-emit the body that was already extracted as its own segment); (3) parent emits bare `qrl(...);` for moved marker symbols since the `q_*` binding has no remaining parent consumer; (4) `_fnSignal(...)` const-classification through function parameters on non-HTML tags (with `ext.paramNames` newly threaded through `SegmentJsxOptions`); (5) Babel/React's `cleanJSXElementLiteralChild` JSX whitespace algorithm replacing the naive `trim().filter().join(' ')` form that was dropping the trailing space in `"Level "`. PR review feedback split the parity-only output-ordering tweak into a separate compareAst-only normalizer (`stripBareQrlPreloadCalls`) so production code stays clean of parity scaffolding — when SWC stops being the oracle, `ast-compare.ts` can be deleted wholesale and the optimizer keeps working. Previous workstream — PR #44 (no Linear ticket — small tech-debt cleanup) replaced ~27 `any`-typed function parameters across 6 production walker / transform files (`rewrite/{const-propagation,inline-body,index,output-assembly}.ts`, `transform/{jsx,jsx-children}.ts`) with proper oxc-typed parameters; broadened `AstCompatMaybeNode` so walkers can use either the strict `Node` union or the loose `AstCompatNode` shape; and removed three categories of dead branches the type system surfaced (`'StringLiteral'`/`'NumericLiteral'`/`'BooleanLiteral'`/`'NullLiteral'` cases — runtime emits `'Literal'`; `'StaticMemberExpression'`/`'ComputedMemberExpression'` cases — runtime emits `'MemberExpression'`; `'ObjectProperty'` case — runtime emits `'Property'`). Net **+158 / −167 lines**. Convergence + full-suite baselines unchanged. F4 ([OSS-359](https://linear.app/kunai/issue/OSS-359)) merged via PR #42 — first parity feature flipped post-refactor-track-v2. **Refactor track v2 ([OSS-343](https://linear.app/kunai/issue/OSS-343)) is fully closed and now in Done state in Linear** — every sub-issue is Done. OSS-344 (PR #12, predicates consolidation), OSS-345 (PR #13, immutable field maps), OSS-346 (PR #14, generateSegmentCode 9-phase sequencer), OSS-347 (PR #30, generateAllSegmentModules SPEC), OSS-356 (PR #32, Sub-A — Prep + inline-strategy + shared rawProps helper), OSS-357 (PR #34, Sub-B — migration-wiring + nested call-sites + nested QRL decls), OSS-358 (PR #35, Sub-C — final orchestrator shape). The `generateAllSegmentModules` orchestrator went from **580 → 459 → 214 → 34 lines (~94% reduction)** while preserving every test baseline. Doc/types/perf/process side-tracks shipped this session: OPTIMIZER.md walkthrough + Maintenance governance ([OSS-348](https://linear.app/kunai/issue/OSS-348), PR #15), REGRESSION.md mechanism rewrite (PR #16, no ticket), types.ts JSDoc parity ([OSS-349](https://linear.app/kunai/issue/OSS-349), PR #17), `extractSegments` `preParsedModule` plumbing ([OSS-350](https://linear.app/kunai/issue/OSS-350), PR #18), `extract.ts` JSX-extension fold-in ([OSS-351](https://linear.app/kunai/issue/OSS-351), PR #20), `extractSegments` closure-node threading + per-extraction body re-parse drop ([OSS-353](https://linear.app/kunai/issue/OSS-353), PR #22), `resolveConstLiterals` closure-form rewrite + prod-rename sync ([OSS-354](https://linear.app/kunai/issue/OSS-354), PR #23), portable benchmark harness ([OSS-355](https://linear.app/kunai/issue/OSS-355), PR #24), METHODOLOGIES post-merge cleanup routine codified (PR #26, no ticket — adds the four-step routine + STATE.md auto-merge carve-out), and `BENCHMARKS.md` perf-history doc bootstrapped with a 9-point backfill (PR #28, no ticket — see [`BENCHMARKS.md`](../../BENCHMARKS.md) at repo root). One backlog item filed during the OSS-351 review: [OSS-352](https://linear.app/kunai/issue/OSS-352) (`inlinedQrl` extension JSX-detection investigation).

**Long-term project goal:** 100% snapshot test parity between the TypeScript optimizer (this repo) and the SWC reference (`./swc-reference-only`), verified by `pnpm vitest convergence --run`. The refactor track is a side-track that pauses parity feature work to make subsequent feature work easier.

## Current measurements

These are the baselines the refactor track must not regress (`REGRESSION.md`). The CI gate now enforces them automatically on every PR — see "CI infrastructure" below.

| Metric | Value |
|---|---|
| Convergence failing | **24 / 212** |
| Convergence passing | **188 / 212** (88.7%) |
| Full suite failing | 47 / 769 |
| Full suite passing | 749 / 769 |
| Last verified | 2026-05-21 on `main` (post PR #136 merged, head `f861d3e`) — OSS-404 path-norm + F1c emit ordering; flips `root_level_self_referential_qrl_inline`; second F2 sub-issue closed |

## CI infrastructure (live)

Landed via [OSS-341](https://linear.app/kunai/issue/OSS-341) and unblocked via [OSS-342](https://linear.app/kunai/issue/OSS-342):

- **`.github/workflows/test.yml`** — runs on every PR to `main`. Steps: typecheck → full vitest → name-based regression check against `.ci/baseline.json`. Fails the PR if any baseline-passing test ID is now failing.
- **`.github/workflows/update-baseline.yml`** — runs on push to `main`. Regenerates `.ci/baseline.json` from a fresh test run; commits via `github-actions[bot]` with `[skip ci]` if the passing set changed.
- **Node version requirement: `>=22`** (encoded in `package.json` `engines.node`). `oxc-parser`'s `experimentalRawTransfer` throws on Node 20 — was the root cause of the apparent macOS/Linux divergence in OSS-342.
- **End-to-end smoke-tested** red-and-green via the throw-away PR #9 (closed unmerged): regression check correctly fails on intentional break, passes on revert.
- **Validated on real PRs**: every source-touching PR through PR #44 has run the gate green — PR #10 (OSS-339), PR #11 (OSS-340), PR #12 (OSS-344), PR #13 (OSS-345), PR #14 (OSS-346), PR #17 (OSS-349 docs-only), PR #18 (OSS-350), PR #20 (OSS-351), PR #22 (OSS-353), PR #23 (OSS-354), PR #24 (OSS-355), PR #32 (OSS-356), PR #34 (OSS-357), PR #35 (OSS-358), PR #40 (OSS-352), PR #42 (OSS-359), PR #44 (no ticket, de-any). Doc/planning-only PRs (PR #15 OSS-348, PR #16 REGRESSION, PR #19 + PR #21 + PR #25 + PR #27 + PR #29 + PR #31 + PR #33 + PR #36 + PR #39 STATE refreshes, PR #26 METHODOLOGIES post-merge routine, PR #28 BENCHMARKS.md bootstrap, PR #30 OSS-347 SPEC, PR #37 BENCHMARKS post-track-v2 update, PR #38 OPTIMIZER.md catch-up) also ran clean since they don't change source.

Helpful local commands:

- `pnpm typecheck` — runs `tsc --noEmit`
- `pnpm ci:baseline:check <vitest-json>` — local regression check against the stored baseline
- `pnpm ci:baseline:update <vitest-json>` — regenerate baseline locally (rare; auto-update on main is preferred)
- `node scripts/diff-platform-results.mjs <vitest-json>` — diff a vitest JSON against the baseline (cross-environment investigation tool)

## Branches in flight

| Branch | Head | Pushed | Tests | Notes |
|---|---|---|---|---|
| `main` | `f861d3e` (post PR #136 merge — OSS-404 path-norm + F1c emit ordering; flips `root_level_self_referential_qrl_inline`; convergence 187 → 188/212) | ✅ | baseline | **CBP-rollout track substantively complete + all unfiled Phase 2 sweeps shipped** under [OSS-381](https://linear.app/kunai/issue/OSS-381) umbrella (11 sub-issues OSS-382 → OSS-392 + OSS-394 / 395 / 396 / 397 / 398 / **OSS-399** siblings). OSS-399 closed (OSS-390 Phase 2: dropped `segment: null` from `TransformModuleParent` + `origPath: null` from `TransformModuleSegment` — the redundant null-arm fields Phase 1 retained for migration safety; 4 construction sites updated; 18 consumer narrow checks migrated across 8 test files from `.segment != null` / `.segment?.X` patterns to `.kind === 'segment'`-discriminated narrowing. The runtime shape of parent/segment objects changes slightly — parent loses `.segment` property; segment loses `.origPath` property — but no convergence snap or current consumer depended on these (verified via grep against `match-these-snaps/`). 1 test fixture in `types.test.ts` updated; test name preserved per REGRESSION.md's renamed-tests caveat. Net +56/-62 across 12 files. **Closes the last unfiled Phase 2 sweep under OSS-381**; only OSS-389 Phase 3 candidates (per-variant field-presence + construct-new at transitions) remain as deferred follow-ups across the entire umbrella. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (variant-field removal); no phase / marker / convention change.); OSS-398 closed (OSS-389 Phase 2 — ExtractionResult consumer narrowing: two new `Mutable<>` phase-flip loops at pipeline boundaries — Phase 2→3 `'extracted' → 'captured'` in `transform/index.ts` after capture analysis, Phase 4 `'captured' → 'consolidated'` moved INSIDE `rewriteParentModule` right after `resolveNesting()` — the discriminator now flips correctly across the pipeline where it previously always stayed `'extracted'` from construction; `ParentRewriteResult.extractions` typed `ConsolidatedSegment[]`; `SegmentGenerationContext` + `SegmentGenerationPrep` Phase-5 fields narrowed; 5 functions in `segment-generation.ts` + 4 in `segment-codegen.ts` narrowed to take `ConsolidatedSegment` directly; stripped-segment loc reset Mutable cast updated; 2 test fixtures updated to `phase: 'consolidated'`. **Pragmatic scope**: original 3-concern Phase 2 ticket narrowed to consumer-narrowing only per planning audit; per-variant field-presence (`?: never`) + construct-new at phase transitions deferred to future Phase 3. Audit revealed `rewrite/output-assembly` + `inline-body` helpers iterate `ctx.extractions: ExtractionResult[]` mid-Phase-4 — narrowing them would cascade casts on iterators, kept on the union. Net +62/-27 across 5 files. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor with 2 new documented Mutable<> cast sites; no phase / marker / convention change.); OSS-397 closed (OSS-391 Phase 2b: `walkWithProtocol` applied to the 517-line extract.ts:367 extraction walker; new `ContextStackForEnter` via `Pick<ContextStack, ...>` restricts enter to push/peek/read API; new `ExtractWalkEnterContext` carrying 11 read-only inputs + 5 mutable buffers + naming-narrowed-to-push-only + pushActiveSegmentBody mutator; new `ExtractWalkExitContext extends Omit<EnterContext, 'naming'>` widens naming back to full ContextStack + adds `finaliseTopFrameIfMatches` + `popContextStackForNode` act-helpers; leave handler collapsed from ~20 lines to 2 act-helper calls; local `const ctx = new ContextStack(...)` renamed to `const naming = ...` so wrapper parameter doesn't shadow. Both compile-time probes verified — `ctx.naming.pop()` and `ctx.popContextStackForNode(node)` are compile errors in enter. The Pick-based narrow gates 16 enter-side push sites + the single leave-side pop at compile time — strongest type-level enforcement of any walker refactor in the sweep. **OSS-391 walker sweep structurally complete** — wrapper applied to all 3 in-scope enter+leave `oxc-walker` walks (JSX via Phase 1 / buildExtractionLoopMap via Phase 2a / extract.ts via Phase 2b). The `Omit + redeclare` pattern for property-type widening in interface extension is now precedent for future walker Enter/Exit splits needing a narrowed-then-widened field. Net +153/-60 in single file. OPTIMIZER.md extract.ts cites refreshed alongside (5 cites drifted past threshold: resolveCanonicalCalleeName +56, getDirectWrapperContextName +56, disambiguateExtractions +93, inlinedQrl detection +109, marker-call extraction block +109). OSS-396 closed (OSS-391 Phase 2a: `walkWithProtocol` wrapper applied to `buildExtractionLoopMap` at `event-capture-promotion.ts:38`; new `BuildExtractionLoopMapEnterContext` + `BuildExtractionLoopMapExitContext` interfaces; `loopStack.pop()` on loop-boundary nodes moved behind a `popTopLoopIfMatches` act-helper; compile-time enforcement verified by probe; net +64/-19 in a single file; wrapper now applied to 2 of 3 in-scope walkers — JSX walker via OSS-391 Phase 1 + this one). PR #119 (no-ticket OPTIMIZER.md cumulative-drift refresh — sibling to PR #94 / OSS-393; swept ~30 file:line citations across the doc including the 287-line drift on the postProcessSegmentCode call cite; rewrote ExtractionResult fields table to anchor at OSS-389 discriminated-union variants; Tier 3 under-threshold drift also swept). OSS-392 closed (new `WithManualEntryMap` interface in `types.ts` consolidates the 5 manual-bearing `EntryStrategy` variants via intersection — preserves illegal-states-unrepresentable while dropping the 5x `manual?:` duplication to one site; new `hasManualEntryMap(s): s is EntryStrategy & WithManualEntryMap` predicate replaces an `as Exclude<EntryStrategy, { type: 'inline' } | { type: 'hoist' }>` cast at `segment-generation.ts:1080` per CBP "Casts are reserved for two narrow purposes"; `getManualEntryMap` helper dropped — it existed solely to encapsulate the awkward `Exclude<...>` type expression the predicate now replaces. Audit narrowed the original 3-target ticket to 1 actionable target: SegmentMetadataInternal consolidation deferred to OSS-389 Phase 2 (where it belongs — per-variant field-presence enforcement); DiagnosticHighlightFlat factoring skipped with rationale (flat fields `startLine`/`startCol`/`endLine`/`endCol` clearer than the consolidated form; `buildHighlight(source, lo, hi)` from OSS-386 already constructs from one site). **Critical audit finding**: the ticket's *original* `EntryStrategyBase<T>` proposal would have violated CBP's illegal-states-unrepresentable by making `{ type: 'inline', manual: ... }` representable (silently ignored at runtime — the silent-failure class the rules warn against); the mixin-via-intersection approach preserves both DRY *and* illegal-states. Net +44/-16 across 3 files); OSS-391 Phase 1 closed (new `src/optimizer/utils/walk-with-protocol.ts` wrapper enforces `EnterContext` ⊂ `ExitContext` type split at compile time via TS generics; applied as pilot to `transform/jsx.ts:620` JSX walker with new `JsxWalkEnterContext` / `JsxWalkExitContext` interfaces — Enter has `source` / `loopStack` / `childJsxNodes`; Exit adds `s: MagicString` / `ranges` / `jsxCtx` / `enableSignals` / `qpOverrides` / `neededImports` / `getDevSourceSuffix` / `setNeedsFragment()`; `needsFragment` flip moved behind the act-helper; **compile-time enforcement verified by probe** — removing the `@ts-expect-error` directive surfaces `Property 's' does not exist on type 'JsxWalkEnterContext'`; `isInSkipRange` widened to `ReadonlyArray<...>` as side-effect of threading `ranges` through the readonly ExitContext field; 7 new wrapper unit tests pin the contract — behavioural parity vs raw walk (visit order, parent threading, `this.skip()`), context delivery, OSS-368-style buffered-state pattern, and the compile-time `@ts-expect-error` check; **Phase 2 sweep deferred** — applies the wrapper to `extract.ts:367` (OSS-368 buffered-frame: `bodyIds` collected in enter, consumed in leave at frame pop) and `event-capture-promotion.ts:38` (symmetric push/pop). The wrapper API was designed with both in mind — one of the new wrapper unit tests exercises the buffered-state pattern specifically, so the Phase 1 API is confirmed to fit before Phase 2 builds on it. Audit during planning surfaced narrower-than-stated scope: ticket said "3-5 walks affected," actual `oxc-walker` calls with non-trivial enter+leave count to 3 (jsx.ts + extract.ts + event-capture-promotion.ts). Out of scope: 11+ enter-only `oxc-walker` sites (no protocol to encode), `event-capture-promotion.ts:151` (empty `leave() {}`), `jsx-call-transform.ts:92` (leave-only walk), 4 custom recursive walkers in `signal-analysis` / `rewrite/*` — different pattern, not `oxc-walker`); OSS-395 closed (OSS-388 Phase 3: 8 defensive narrowing casts removed across rewrite/index.ts + body-transforms.ts; new `importedSpecifierName` helper; getPatternProperties return tightened to `AstNode[]`; 3 signal-analysis.ts casts surfaced as load-bearing and restored with documenting comments; dead `'StaticMemberExpression'` branch removed; 146 → 142 grep; -8 code casts; principled-cast-reduction trajectory closed); OSS-394 closed (OSS-388 Phase 2: 6 structural-narrowing casts in utils/simplify.ts replaced with TS discriminated-union narrowing on `node.type`; surfaced redundant `bigint` check on oxc's 6-variant `Literal` union; 4 `as never` arithmetic-bridging casts retained as load-bearing; 151 → 146); OSS-388 Phase 1 closed (tightened `forEachAstChild` + `someAstChild` visitor types from `AstCompatNode` → `AstNode`; dropped `forEachChildNode` wrapper in `signal-analysis.ts`; swept 12 defensive `child as AstNode` casts (163 → 151); ≤60-cast acceptance target not yet hit, Phase 2+ deferred — `utils/simplify.ts` 10 casts is next-biggest cluster, then custom walk function signatures, then `getPatternProperties` return type); OSS-390 Phase 1 closed (discriminated union for `TransformModule`: `kind: 'parent' \| 'segment'` discriminator + 2 variants; both retain redundant null-arm fields for runtime-shape parity; `isEntry` typed as variant-appropriate literal; 0 `src/` consumer reads, 1 test fixture sweep; Phase 2 deferred — drop redundant null-arm fields + mechanical consumer sweep + NAPI adapter); OSS-389 Phase 1 closed (discriminated union for ExtractionResult: `phase` discriminator + 3 variants `extracted`/`captured`/`consolidated`; `Mutable<T>` helper for in-place mutation FFI-boundary; Phase 2 deferred — consumer narrowing + construct-new + per-variant field-presence); OSS-387 closed (Phase 6 readonly discipline: 18 → 130 readonly tokens across public NAPI types + producers + function-param arrays + constants; `ExtractionResult` mixed-mutability with 19 readonly + 14 documented-mutable fields tracing each mutation site); OSS-386 closed (Phase 5 brand propagation: position + source-text brands; pre-existing `loc` byte-offset semantic bug surfaced + fixed; new `buildHighlight` helper consolidates 3 diagnostic-emitter patterns; 3 OSS-386 regression tests pin loc contract); OSS-385 closed (Phase 4 brand propagation: path-family + CtxName brands threaded through `ExtractionResult` / `SegmentAnalysis` / `TransformModulesOptions` / `TransformModuleInput` / `TransformModule` / `RewriteContext` + path-utils high-level producers; three in-flight OSS-383 refinements — `computeRelPath` fallback strips leading `/`, `mkCtxName` regex accepts bare `$` + hyphens); OSS-384 closed (Phase 3 brand propagation: symbol-family brands threaded through `ExtractionResult` + `SegmentAnalysis` + producers; `parent` + `preRenameSymbolName` Map types branded; OSS-383 regex loosens for `DisplayName`/`CanonicalFilename`/`Hash` to match real production values); OSS-383 closed (Phase 2 brand-types foundation: new `src/optimizer/types/brands.ts` with 13 branded primitive types + smart constructors; 20 runtime tests + compile-time `@ts-expect-error` assertions); OSS-382 closed (Part A: 4 `as unknown as` double casts → `forEachAstChild`/new `someAstChild`; Part B: 3 sites narrowed from `any` to `BindingPatternLike` / `JSXElementName` closed unions with `_exhaustive: never` defaults; +11 unit tests at `tests/optimizer/utils/ast.test.ts`); OSS-379 closed (parity flip: jsx() → _jsxSorted() peer-tool transform; `example_qwik_react` 184→185); OSS-380 closed (chore: stop tracking `ts-output/` in git); OSS-378 closed (parity foundation: 2 of 3 `example_qwik_react` bugs fixed); OSS-377 closed (code-health: `event-capture-promotion.ts` shared `EventCaptureContext`); OSS-376 (`processChildren` 8→3 args); OSS-375 (`transformJsxFragment` 9→2 args); OSS-374 (parameter-explosion `transformJsxElement` 15→3 args + `processProps` 12→3 args via shared `JsxTransformContext`); OSS-373 (back-to-back-walk merge in `computeSegmentUsage`); OSS-372 (two-pass walk in `flatten-destructures.ts` unified into single source-order DFS); OSS-371 (loop-walk in `buildPassthroughModule` collapsed); OSS-370 (`collectConstAndLocalNames` unified); OSS-369 (`collectSignalDeps` unified); OSS-368 (per-extraction sub-tree walks eliminated in `extract.ts`); OSS-367 (2→1 walk in `collectBodyIdentifiers`); OSS-366 (per-name walks → single-pass collection in `diagnostic-detection.ts`); OSS-365 (substring re-parse elimination + shared post-process AST cache + F8c crash hotfix); OSS-364 (walks 5→2 in `event-capture-promotion.ts`); F8c / F8b / F8a / F1b parity wins; refactor track v2 + F4. Code-health pass is fourteen deep against the prior `CODING_BEST_PRACTICES.md`; both major sub-threads complete (back-to-back-walk + "Reduce function parameters"). CBP rollout is the new active track. |
| `oxc-port` | `073a11d` (post napi 3 bump) | ✅ | n/a (Rust) | Long-lived branch for the Rust/OXC port. Subtree-imported `qwik-optimizer` as `oxc/`; oxc bumped 0.94 → 0.129; napi bumped 2 → 3. 31/31 cargo tests passing. Continued TS work on `main` flows through without conflict. |
| `ast-parity/F2` | `a644c16` (stale) | ❌ local-only | parked | F2 cluster paused; will need rebase onto current `main` (refactor track v2 + F4 + F1b landed since) before resuming. |
| _(no active workstream branch)_ | — | — | — | **Parity-track active — F2 umbrella in progress, 2 of 6 sub-issues closed.** [OSS-403](https://linear.app/kunai/issue/OSS-403) parents the F2 cluster (Hoisted/Inline/Lib entry strategies). **OSS-406 closed (PR #134)** + **OSS-404 closed (PR #136)** — convergence 186 → 188/212. **4 F2 sub-issues remain + 1 new sibling**: [OSS-405](https://linear.app/kunai/issue/OSS-405) skip `_captures` injection on `useLexicalScope()` bodies; [OSS-407](https://linear.app/kunai/issue/OSS-407) Hoist + regCtxName → `_regSymbol`; [OSS-408](https://linear.app/kunai/issue/OSS-408) peer-tool `inlinedQrl(...)` re-emission; [OSS-409](https://linear.app/kunai/issue/OSS-409) `_fnSignal` dedup ordering; **[OSS-410](https://linear.app/kunai/issue/OSS-410)** JSX dev-info `lineNumber:` offset (filed during OSS-404 pickup audit). **Pickup audit on each remaining sub-issue must check whether OSS-406+OSS-404's landings subsumed it** before re-investigating from scratch. Other open parity work: F3 lightweight-inline coordinated rewrite (4 tests); F5/F6/F7/F9/F10 still open per CONVERGENCE_FAILURES.md; `example_qwik_react` (one-off, migration `_auto_filterProps`). Three OSS-381 Phase 3 candidates remain deferred follow-ups — low-value/high-risk per the OSS-398 audit. Established policies: planning-first workflow (`feedback_plan_before_implement`); parity fixes ship with focused regression/unit tests alongside snap suite (`feedback_parity_fixes_need_regression_tests`); F* parity audits must check for multiple root causes during pickup (`feedback_parity_audit_multi_root_cause` — validated again by OSS-406 where pickup audit correctly identified the real bug shape vs the umbrella's initial framing); fold adjacent `if (cond) return X;` chains into combined-condition guards (`feedback_fold_adjacent_if_return_chains`); `/wrap-up` skill auto-fires on phrases like "wrap it up" / "ship it"; no GitHub→Linear integration so Linear flips are manual via `issueUpdate` (codified in LINEAR.md). |

## Refactor track v2 ([OSS-343](https://linear.app/kunai/issue/OSS-343))

| Ticket | Title | Branch | Status |
|---|---|---|---|
| [OSS-343](https://linear.app/kunai/issue/OSS-343) | Refactor track v2 — segment generation cleanup *(parent)* | (no branch) | Backlog (auto-rolls up) |
| [OSS-344](https://linear.app/kunai/issue/OSS-344) | Consolidate `isStrippedSegment` + `isAnyComponentCtx` into `rewrite/predicates.ts` | `refactor/predicates-followup` (merged) | **Done** (PR #12) |
| [OSS-345](https://linear.app/kunai/issue/OSS-345) | Pre-compute field maps in `segment-generation.ts` | `refactor/precompute-field-maps` (merged) | **Done** (PR #13) |
| [OSS-346](https://linear.app/kunai/issue/OSS-346) | Refactor `generateSegmentCode` 8-phase sequencer | `refactor/generate-segment-code-phases` (merged) | **Done** (PR #14) |
| [OSS-347](https://linear.app/kunai/issue/OSS-347) | Discovery + plan for `generateAllSegmentModules` refactor *(planning — produced SPEC + 3 sub-tickets)* | `refactor/generate-all-segment-modules-spec` (merged) | **Done** (PR #30) |
| [OSS-356](https://linear.app/kunai/issue/OSS-356) | Sub-A — Extract `Prep` + inline-strategy from `generateAllSegmentModules` | `refactor/segment-gen-prep-inline` (merged) | **Done** (PR #32) |
| [OSS-357](https://linear.app/kunai/issue/OSS-357) | Sub-B — Extract migration-wiring + nested call-sites | `refactor/segment-gen-default-helpers` (merged) | **Done** (PR #34) |
| [OSS-358](https://linear.app/kunai/issue/OSS-358) | Sub-C — Extract `buildDefaultStrategySegment` sequencer; final orchestrator shape | `refactor/segment-gen-orchestrator-shape` (merged) | **Done** (PR #35) |

**Track v2 fully closed.** All seven sub-issues of OSS-343 are Done. The `generateAllSegmentModules` orchestrator at `src/optimizer/transform/segment-generation.ts` is now a 34-line sequencer over named helpers, down from 580 lines pre-refactor (~94% reduction). Per-PR commit messages followed the four-question format from `METHODOLOGIES.md` "Refactoring" section; Sub-A/B/C scope documented in `.planning/specs/segment-generation-refactor.md`.

## Refactor track v1 ([OSS-337](https://linear.app/kunai/issue/OSS-337)) — closed

All sub-issues merged and closed. Worth recording the shape because v2 follows the same playbook.

| Ticket | Outcome |
|---|---|
| OSS-338 | Extracted `MIG_REASON` const + `usingSegmentsOf` helper + named MIG-05a post-pass with JSDoc preconditions in `variable-migration.ts`. Renamed keys to action-prefixed style in a fixup commit. |
| OSS-339 | Named `OUTERMOST_BODY_THRESHOLD` magic constant + `formatWCall` helper + `spliceWithinBody` helper in `body-transforms.ts`. |
| OSS-340 | New module `rewrite/predicates.ts` consolidating `matchesRegCtxName` (3 byte-identical duplicates), `isEventHandlerOrJsxProp`, `hasUnderscorePlaceholderParams` across 4 files. |

## Parity feature status (refactor track v2 closed; parity work ready to resume)

Snapshot from before the refactor track started. Numeric features track the original `CONVERGENCE_FAILURES.md` grouping. Suffix letters (F1b, F1c) are sub-features that emerged from in-flight rescoping.

| Feature | Status | Test(s) | Brief |
|---|---|---|---|
| F1 — `_ref` indirection | ✅ CLOSED | `component_level_self_referential_qrl` | shipped via PR #5; const-declarator fix added during review |
| F1b — mutual-recursion migration | ✅ CLOSED | `example_self_referential_component_migration` | shipped via PR #46 ([OSS-360](https://linear.app/kunai/issue/OSS-360)); marker-aware `isInitializerSafe`, marker-decl move synthesis, bare-qrl emission for moved markers, `_fnSignal` const-classification through params on non-HTML tags, Babel JSX whitespace |
| F1c — inline-strategy emit ordering | LANDED (foundation only) | `root_level_self_referential_qrl_inline` | statement-order fix lives on `ast-parity/F2`; test won't flip until F2 also fixes path/hash/key-prefix bugs |
| F2 — hoisted / inline / lib strategies | PARTIAL (2/6 sub-issues closed) | 4 remaining tests + OSS-410 sibling | F2 umbrella [OSS-403](https://linear.app/kunai/issue/OSS-403) active. **OSS-406 closed** (PR #134) — `example_optimization_issue_3542` flipped via `promoteEventHandlerCaptures` gate on `isInlineStrategy === 'inline'`. **OSS-404 closed** (PR #136) — `root_level_self_referential_qrl_inline` flipped via 3 coupled fixes (path-norm preserves `./`, buildDevFilePath strips `./`, F1c emit ordering ported). **Remaining sub-issues**: OSS-405 (`useLexicalScope()` bodies), OSS-407 (Hoist + regCtxName → `_regSymbol`), OSS-408 (peer-tool `inlinedQrl(...)` re-emission), OSS-409 (`_fnSignal` dedup), OSS-410 (JSX dev-info `lineNumber:` offset — new sibling filed during OSS-404 pickup audit). |
| F3 — `_rawProps` lightweight components | OPEN | 4 tests | 8 coordinated changes; bigger than original 1-pass scoping. Will reuse `isComponentCtx` + `isAnyComponentCtx` from OSS-344 once that lands. |
| F4 — MIG-05a shared destructure | ✅ CLOSED | `example_invalid_references` | nested-segment migration wiring shipped via PR #42 ([OSS-359](https://linear.app/kunai/issue/OSS-359)); `wireMigration` now applies to all non-`inlinedQrl` segments, not just top-level. |
| F5 — server-marker stripping → null body | OPEN | 3 tests |  |
| F6 — JSX runtime preservation | OPEN | 3 tests |  |
| F7 — inner-function extraction discipline | OPEN | 4 tests + `fun_with_scopes` from F8 |  |
| F8 — diverse semantic bugs | ✅ CLOSED (loop-listener test) | 1 remaining test (`example_qwik_react`, different root cause) | F8a / PR #48 / [OSS-361](https://linear.app/kunai/issue/OSS-361) — `simplify.ts`; F8b / PR #50 / [OSS-362](https://linear.app/kunai/issue/OSS-362) — loop-cross-capture wiring; F8c / PR #52 / [OSS-363](https://linear.app/kunai/issue/OSS-363) — `flatten-destructures.ts` + rawProps gate + compound-destructure const classification; **F8d = `example_component_with_event_listeners_inside_loop` CLOSED via 3-PR arc** — [OSS-400](https://linear.app/kunai/issue/OSS-400) (PR #128, hoist target depth) + [OSS-401](https://linear.app/kunai/issue/OSS-401) (PR #130, scope-aware classify) + [OSS-402](https://linear.app/kunai/issue/OSS-402) (PR #132, computed-prop `_fnSignal`). Remaining: `example_qwik_react` (different root cause — migration `_auto_filterProps`) |
| F9 — spread / var / const splitting | OPEN | 2 tests |  |
| F10 — import-aware naming | OPEN | 2 tests |  |

Full feature analysis: `CONVERGENCE_FAILURES.md`.

## Most recent meaningful progress

Most recent first. Trim entries older than ~10 to keep this file from bloating.

- **2026-05-21** — [OSS-404](https://linear.app/kunai/issue/OSS-404) merged via PR #136. **Second F2 sub-issue closed** (under OSS-403 umbrella) — flips `root_level_self_referential_qrl_inline`; convergence 187 → 188/212 (88.7%). **What landed**: three coupled fixes for inline self-ref components. (1) `computeRelPath` at `src/optimizer/path-utils.ts:66` preserves leading `./` from input — SWC's hash + JSX dev-info `fileName:` use the user-provided path shape; pre-fix `normalize()` stripped it producing TS-vs-SWC divergence. New helper `restoreDotSlash` reapplies the prefix if input had one. (2) `buildDevFilePath` at `src/optimizer/dev-mode.ts:38` strips leading `./` from `inputPath` before srcDir concatenation — pre-fix emitted `/user/qwik/src/./node_modules/...` (stray `/./`); post-fix matches SWC's `/user/qwik/src/node_modules/...`. (3) F1c port from stale `ast-parity/F2` branch commit `534ddd4` — `assembleOutput` at `src/optimizer/rewrite/output-assembly.ts:520-559` partitions `sCalls` by whether body references any name exported via `export const NAME = ...componentQrl(...)`. Referencing sCalls go AFTER the export so the body's self-reference doesn't TDZ at module load. Word-boundary regex distinguishes `Tree` from `q_Tree_xxx`. **Pickup audit found 4 distinct bugs vs the umbrella's 2-bug framing**: A (`file:` extra `./`), B (F1c emit ordering), C (symbol hash mismatch), D (`fileName:` missing `./`), E (`lineNumber:` offset). A+C+D share one root cause (path-norm); fix #1 cascade-fixes all three. B = F1c port. E filed separately as **OSS-410** with proper scope notes. Hash divergence confirmed via debug instrumentation: SWC's expected `XMEiO6Rrd3Y` requires `./`-prefixed relPath input vs TS's pre-fix `eqIvhrnmoqI` from stripped input. **F1c port is a real runtime correctness fix** — convergence test passes with just the path-norm fix because `compareAst`'s `sortReorderableBlock` normalizer reorders top-level statements to match SWC's canonical order before comparing; but the emitted module has a TDZ bug at module load (`q_Tree_X.s((props) => /*references Tree*/)` runs before `Tree` is initialized → runtime `ReferenceError` on first invocation). F1c shipped for runtime correctness even though it wasn't strictly needed for convergence pass. 6 focused regression tests at `tests/optimizer/oss-404-path-norm-f1c.test.ts`: path-norm preserve-when-present + leave-unchanged-when-absent; buildDevFilePath strip + no-op; F1c partition for self-ref body emits AFTER export + non-self-ref body keeps BEFORE position. Net +206/-5 across 4 files. Convergence 187 → 188/212; full-suite 742 → 749 passing (target test + 6 new unit tests). **OPTIMIZER.md audit verdict**: no update needed — `path-utils.ts` and `output-assembly.ts` are on the trigger checklist but the changes are localized correctness fixes; no phase / marker / convention change. **Pattern observation**: OSS-406 + OSS-404 both shipped under the same playbook — deep pickup audit reveals broader scope than umbrella documented → update description + ship coherent fix + file siblings for adjacent bugs. The `feedback_parity_audit_multi_root_cause` memory is now validated across 4 PRs (OSS-400/401/406/404). The umbrella audit at filing-time produces useful but quick framing; the pickup audit produces real scope.
- **2026-05-21** — [OSS-406](https://linear.app/kunai/issue/OSS-406) merged via PR #134. **First F2 sub-issue closed** (under OSS-403 umbrella) — flips `example_optimization_issue_3542`; convergence 186 → 187/212 (88.2%). **What landed**: `promoteEventHandlerCaptures` at `src/optimizer/transform/event-capture-promotion.ts:474-491` now branches on a new `isInlineStrategy` field threaded through `EventCaptureContext`. Under `EntryStrategy::Inline`, non-loop eventHandler captures stay in `captureNames` (so the downstream pipeline `preConsolidateRawPropsCaptures` → `transformInlineSegmentBody` → `injectCapturesUnpacking` → `replacePropsFieldReferencesInBody` routes them through `_captures[N]` unpacking + `_rawProps.X` rewriting). Under `Hoist` and default `smart`/`segment`, existing segment-file-style positional-param padding (`['_', '_1', 'atom', 'ctx']`) keeps firing. **Key implementation moment**: initial gate `isInlineStrategy = type === 'inline' || type === 'hoist'` regressed `example_issue_33443` (Hoist, baseline-passing) which expects `(_, _1, counter) => counter.value++` padded paramNames. Narrowed to `inline` only — Hoist needs its existing padding. **3 focused regression tests** at `tests/optimizer/oss-406-rawprops-propagation.test.ts` cover the gate behavior: positive (Inline keeps captureNames + emits `_captures[0]` + `_rawProps.X` + `.w([_rawProps])`); negative-1 (Hoist still pads); negative-2 (default smart still pads). Net +183/-5 across 3 files. **Multi-root-cause audit confirmation in BOTH directions**: the OSS-403 umbrella creation incorrectly framed OSS-406 as "emit `q_X.s(body)` blocks" — pickup re-audit found TS WAS emitting them, just with wrong content (loop-cross-capture-style args, phantom `ev`, missing `.w([_rawProps])`). Re-audit correctly identified the real bug + scope. The audit-during-impl norm caught the over-broad Hoist gate before merge. **Pattern observation**: F* sub-issue audits at pickup are now established as load-bearing. Umbrella audits during sub-issue filing may have wrong framing (broad / quick survey); real scope only emerges during deep pickup audit. **Audit policy memorialized in STATE**: for ALL future F* sub-issues, pickup audit MUST re-verify the documented bug shape before coding. Convergence 186 → 187/212; full-suite 738 → 742 (target + 3 unit tests). **OPTIMIZER.md audit verdict**: no update needed — localized correctness fix adding one branch to an existing classifier; `event-capture-promotion.ts` is on the trigger checklist but no phase / marker / convention change. **F2 remaining**: 5 sub-issues (OSS-404 / 405 / 407 / 408 / 409). Some may now be subsumed by this fix — pickup audit on the next sub-issue checks first.
- **2026-05-21** — [OSS-402](https://linear.app/kunai/issue/OSS-402) merged via PR #132. **F8 LAST OPEN TEST FLIPPED** (`example_component_with_event_listeners_inside_loop`) — convergence 185 → 186/212 (87.7%). First parity flip since OSS-379 closed `example_qwik_react` (back during the code-health pass). Closes the 3-PR arc: OSS-400 (hoist target depth) + OSS-401 (scope-aware `classifyConstness`) + OSS-402 (this). **What landed**: new fourth branch in `analyzeMemberExpression` at `src/optimizer/signal-analysis.ts:868` handling computed-property MemberExpressions whose property is not a string literal (`obj[Identifier]` / `obj[NumericLit]`). Gate: `isKnownIdent && exprNode.computed && allDeps.length > 0 && no blockers`. The branch sits below the three existing classifications (`isSignalValueAccess` → `isDeepStoreAccess` → `isStoreFieldAccess`) so static / string-literal-computed access still routes to wrapProp via the fast path. Mirrors SWC's `convert_inlined_fn` path: SWC's `prop_to_string` returns None for `[Identifier]` / `[NumericLit]` so the `obj.prop` wrap branch is skipped; `ObjectUsageChecker.used_as_object` then matches when the object resolves to an in-scope `Var(_)` on `decl_stack`. TS replicates: gate on `isKnownIdent` (via `localNames.has(objIdent)`); collect deps via `collectSignalDeps` (alphabetically sorted, matching SWC's `scoped_idents` sort); route through `generateFnSignal` which already handles param substitution + string serialization. For the target test, 3 sites (`loopForI` `{results[i]}`, `loopForIn` `{results[key]}`, `loopWhile` `{results[i]}`) all dedupe to the same `_hf0 = (p0,p1)=>p1[p0]` via `SignalHoister.hoist`'s string-keyed dedup. 6 new unit tests at `tests/optimizer/signal-analysis.test.ts`: 3 positive (`results[i]` Identifier prop, `obj[key]` for-in pattern, `obj[42]` numeric literal — SWC also hoists this since `prop_to_string` only handles strings) + 3 negative (`obj["string"]` still wrapProp via fast-path, `unknownGlobal[i]` skipped since obj isn't local, `arr[foo()]` blocked by `containsUnknownCall`). Net +95/+0 across 2 files. Convergence 185 → 186/212; full-suite 731 → 738 passing (target test + 6 new unit tests). **OPTIMIZER.md audit verdict**: no update needed — localized correctness fix adding one branch to an existing classifier; no phase / marker / convention change; the Hoisted Expressions / `_fnSignal` section describes the conceptual contract which is unchanged (TS now matches SWC for one more shape). **Multi-root-cause audit confirmation**: unlike OSS-400 (which surfaced 2 hidden bugs during its audit), OSS-402's audit confirmed it was the single remaining blocker. The diff showed exactly the missing pieces — `_fnSignal` import + `_hf0`/`_hf0_str` decls + 3 call sites — all wired by the existing infrastructure once the new branch fires. The `feedback_parity_audit_multi_root_cause` memory pattern works in both directions: it CATCHES hidden bugs when they exist (OSS-400 case) AND confirms scope when the documented bug really is the only one (OSS-402 case). **F8 is now closed in CONVERGENCE_FAILURES.md** — all individual subfeatures (F8a/b/c/d) shipped; remaining open features (F2/F3/F5/F6/F7/F9/F10) are coordinated rewrites rather than scattered individual bugs.
- **2026-05-21** — [OSS-401](https://linear.app/kunai/issue/OSS-401) merged via PR #130. **Second consecutive F8 partial close** on `example_component_with_event_listeners_inside_loop` (target test didn't flip until OSS-402 also landed). **What landed**: scope-aware `classifyConstness` for shadowed identifiers. New `ScopeAwareBindings` interface + `ScopeAwareBindingsImpl` at `src/optimizer/transform/jsx.ts` exposing `classify(name, atPosition): 'const' | 'var' | undefined` + `addProgramScopeConst(name)`. Per-name index over scope ranges: `nameToScopes: Map<string, Array<{start, end, kind}>>`; lookup finds the smallest containing range. Walker rewritten from flat-Set `collectConstAndLocalNames` to `collectScopeAwareBindings` with a scope stack (push/pop on Function*/Arrow/Block/ForOf/ForIn/Catch). `classifyConstness` + `isConstBindingName` signatures take `atPosition: number`; 6 caller sites in `jsx-props.ts` / `jsx-children.ts` thread positions; `JsxTransformContext.constIdents` → `.bindings`. **Subtle bug surfaced + fixed during implementation**: initial impl broke `example_custom_inlined_functions` + `example_renamed_exports` because segment-codegen injects capture names like `state` as runtime-const but the segment body's AST `const state = _captures[0]` has a MemberExpression init that fails `isReturnStatic` → scope-inner var binding wins → `_wrapProp` child becomes dynamic → flag 3→1. Resolved with `alwaysConst: Set<string>` override field on `ScopeAwareBindingsImpl` that `classify()` checks first, bypassing scope walk for capture names. **12 new unit tests** at `tests/optimizer/jsx-transform.test.ts` cover arrow-param vs for-of shadowing (the core OSS-401 case), nested shadowing, catch params, block-scoped const with literal init (classifies as var since `isReturnStatic` only matches CallExpression with $/Qrl/use* callee), `const x = useStore()` (classifies as const), addProgramScopeConst override, for-in binding, compound destructure per-elem (`const [store, math] = [useStore(), Math.random()]` → store const, math var). Net +434/-120 across 6 files. **Effect on target test**: `loopArrowFn`'s arrow param `item` now correctly classifies as var (flag=4, matches SWC) where pre-fix it was wrongly flag=6. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (collector function signature + classifier signature change); transform/jsx.ts is on the trigger checklist but no phase/marker/convention change. **Workflow observation**: this is the **second consecutive partial-fix landing** following the OSS-400 pattern. The `feedback_parity_audit_multi_root_cause` memory (established during OSS-400 wrap-up) was directly used here — the audit explicitly checked whether OSS-401's documented bug was the only root cause (it was; no additional bugs surfaced) and traced through the full SWC algorithm (`is_const.rs` + `compute_scoped_idents` filter to `IdentType::Var(true)`) before writing code. The planning-first norm + multi-root-cause audit are now load-bearing for parity track work.
- **2026-05-21** — [OSS-400](https://linear.app/kunai/issue/OSS-400) merged via PR #128. **First parity-feature landing since the OSS-381 CBP umbrella substantively closed.** Partial F8 close: the documented "wrong hoist target depth" bug for `example_component_with_event_listeners_inside_loop` is fixed; the target test does NOT flip because the audit surfaced two additional bugs in the same fixture that are now filed as siblings. **What landed**: new `findComponentReturnPosition(bodyText)` helper at `src/optimizer/segment-codegen/body-transforms.ts:278` tracks brace depth and returns the LAST depth-1 `return ` token position. Replaces `bodyText.indexOf('return ')` (the FIRST `return ` anywhere — which lands inside the first nested function declaration, not the component's own return). For the target test, the component contains 5 nested loop helpers (`loopArrowFn`, `loopForI`, `loopForOf`, `loopForIn`, `loopWhile`); the buggy lookup was dumping all 6 `q_*.w([captures])` declarations inside `loopArrowFn` — broken at runtime. Post-fix: declarations land at component scope where every helper can reach them. Defensive fall-back to `-1` if no depth-1 return found (matching `indexOf`'s sentinel). **Two bugs surfaced during audit, filed as siblings**: **[OSS-401](https://linear.app/kunai/issue/OSS-401)** — scope-unaware `classifyConstness`: `constBindings` is a flat `Set<string>` with no scope info; same name (`item`) as arrow param vs for-of binding gets one classification globally (TS picks arbitrarily — current behavior: classifies-as-const → static children → flag=6; SWC's behavior: scope-aware → arrow gives flag=4, for-of gives flag=6). Fix requires scope-tracking refactor of `collectConstAndLocalNames` at `transform/jsx.ts:190`. **[OSS-402](https://linear.app/kunai/issue/OSS-402)** — `_fnSignal` hoisting not applied to non-trivial MemberExpression children in loop callbacks: SWC hoists `{results[i]}` / `{results[key]}` to `_fnSignal(_hf0, [i, results], _hf0_str)` per `signal-analysis.ts:346`; TS leaves them inline. Affects `loopForI` / `loopForIn` / `loopWhile`. All three (OSS-400 + OSS-401 + OSS-402) must land for the target test to flip. **Why partial-fix shipped**: per user-approved option, the documented F8 bug is real + the runtime-broken output is fixed; OSS-401/OSS-402 are scoped differently and warrant their own audits. Shipping in isolation closes OSS-400 cleanly without conflating three concerns in one PR. Net +47/-3 across 2 files (1 production + CONVERGENCE_FAILURES.md update). Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — `body-transforms.ts` is on the trigger checklist but the change is a localized correctness fix within an existing helper; no phase / marker / convention change. **Workflow observation**: this is the first ticket since the OSS-381 umbrella where the framing in CONVERGENCE_FAILURES.md was insufficient — the audit surfaced 2 additional bugs the original framing missed. New norm worth memory'ing: **for F* parity tickets, the audit step should explicitly check whether the test failure has multiple root causes**, not just whether the documented one is the only one. Original framing for F8 tests was often "verbatim body / mechanical printer issue" which is now known to be misleading (per the prior F8 closure observations).
- **2026-05-21** — [OSS-399](https://linear.app/kunai/issue/OSS-399) / OSS-390 Phase 2 merged via PR #126. **Phase 17 of the OSS-381 CBP rollout — TransformModule null-arm cleanup + consumer narrow sweep.** Phase 2 of OSS-390. **What landed**: removed `readonly segment: null` from `TransformModuleParent` and `readonly origPath: null` from `TransformModuleSegment` — the redundant null-arm fields Phase 1 (PR #107) retained for runtime-shape parity. JSDocs on the variant types + on the `TransformModule` union updated to remove the "kept for NAPI/consumer parity" justification (the parity concern was aspirational; no current consumer or convergence snap depended on the redundant fields — verified via `grep "\"segment\":\s*null" match-these-snaps/` returning empty). 4 construction sites updated to drop the redundant null-arm assignments (`transform/index.ts:547`, `module-cleanup.ts:584`, `segment-generation.ts:460` + `:1108`). 18 consumer narrow checks migrated across 8 test files: `transform.test.ts` (10×: `m.segment !== null` → `m.kind === 'segment'`), `snapshot-batch.test.ts` (3), `convergence.test.ts` (2: includes the snapshot-formatter `mod.segment != null` check), `segment-only-debug.test.ts` (1), `failure-families.test.ts` (2), `convergence-breakdown.test.ts` (1), `inlined-qrl-regressions.test.ts` (1). Migration patterns: `.segment !== null` → `.kind === 'segment'`; `.segment && .segment.X` → `.kind === 'segment' && .segment.X`; `.segment?.X === Y` → `.kind === 'segment' && .segment.X === Y`. 3 test cases in `transform.test.ts` (lines 25-67 + 782) refactored from `.segment!.X` non-null assertions to type-narrow locals (`const segment = result.modules[1]; if (segment.kind !== 'segment') throw ...; expect(segment.segment.X)...`). 1 test fixture in `tests/optimizer/types.test.ts` updated to drop the `segment: null` field literal + the `expect(mod.segment).toBeNull()` assertion; test name preserved per REGRESSION.md's renamed-tests caveat to keep the CI gate green. **The runtime shape of parent/segment objects changes slightly** (parent loses `.segment` property; segment loses `.origPath` property) — but no current consumer or convergence snap depends on these fields' presence. Net +56/-62 across 12 files. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (variant-field removal); 3 of 4 touched files on the trigger checklist; no phase / marker / convention change. **Closes the last unfiled Phase 2 sweep under the OSS-381 CBP umbrella** — with OSS-389 Phase 2 (PR #124) and this PR closing OSS-390 Phase 2, all Phase 2 follow-ups are now landed. Only OSS-389 Phase 3 candidates (per-variant field-presence via `?: never` pattern + construct-new at phase transitions) remain as deferred follow-ups across the entire umbrella. **Workflow observation**: this is the 9th consecutive Phase 1 / Phase 2 split landing (OSS-389 → OSS-390 → OSS-388 → OSS-394/OSS-395 → OSS-391 → OSS-396 → OSS-397 → OSS-398 → OSS-399). The pattern is now structurally definitive: every "Phase 1" ticket in this codebase ships principled foundation + defers consumer-side mechanical sweep to a sibling Phase 2 ticket filed when picked up.
- **2026-05-20** — [OSS-398](https://linear.app/kunai/issue/OSS-398) / OSS-389 Phase 2 merged via PR #124. **Phase 16 of the OSS-381 CBP rollout — ExtractionResult consumer narrowing.** Phase 2 of OSS-389. **Pragmatic scope per planning audit**: original 3-concern ticket (consumer narrowing + per-variant field-presence + construct-new at phase transitions) narrowed to consumer-narrowing only. The audit surfaced 54 phase-field mutation sites across 5+ files for construct-new, and per-variant field-presence is tightly coupled to construct-new. The CBP rule "Illegal states unrepresentable" already gets meaningful enforcement via the Phase 1 design (`Mutable<>` casts at documented FFI-boundary sites; truly-immutable fields readonly on `ExtractionBase`; discriminator narrows the union). This PR narrows consumers to capture the documentation/contract benefit while leaving the in-place mutation pattern intact. **What landed**: two new `Mutable<>` phase-flip loops at pipeline boundaries — Phase 2→3 (`'extracted' → 'captured'`) in `transform/index.ts` after capture analysis, Phase 4 (`'captured' → 'consolidated'`) moved INSIDE `rewriteParentModule` right after `resolveNesting()` (resolveNesting is the conceptual transition site — parent reference resolved). The discriminator now flips correctly across the pipeline where before it always stayed `'extracted'` from construction. `ParentRewriteResult.extractions` typed `ConsolidatedSegment[]` (was `ExtractionResult[]`); `SegmentGenerationContext.extractions` + `updatedExtractions` narrowed to `ConsolidatedSegment[]`; `extBySymbol` + `sortedExtractions` in `SegmentGenerationPrep` similarly narrowed. 5 functions in `segment-generation.ts` narrowed (`buildInlineStrategySegment` / `buildNestedQrlDeclarations` / `wireMigration` / `buildNestedCallSites` / `buildDefaultStrategySegment` + `tryBuildMarkerDeclMove`); 4 functions in `segment-codegen.ts` narrowed (Phase 5 entry points + internal helpers); stripped-segment loc reset cast at line 1145 updated from `Mutable<ExtractionResult>` to `Mutable<ConsolidatedSegment>`. 2 test fixtures updated to `phase: 'consolidated'`. **Audit revealed during implementation**: narrowing `rewrite/output-assembly` + `inline-body` helpers (originally in scope per the ticket) would cascade casts on iterators that read `ctx.extractions: ExtractionResult[]` mid-Phase-4. Reverted; kept on the union. Documented in the PR. **What stays on the union (intentional)**: `rewrite/predicates.ts` (called from multiple phases), `rewrite-context.ts` shared context fields (mid-Phase-4 state spans the union), `rewrite/output-assembly` + `inline-body` helpers (per audit), `extract.ts pendingClosures` (Phase 1/2 internal). **What's deferred (Phase 3 candidates)**: per-variant field-presence (`?: never` pattern), construct-new at all phase transitions (the 54 mutation sites stay), narrowing rewrite/output-assembly + inline-body helpers. Net +62/-27 across 5 files. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — type-internal refactor (interface narrowing + 2 new documented Mutable<> cast sites with FFI-boundary comments); no phase / marker / convention change. **Workflow observation**: the variants are structurally identical today, so narrowing changes ZERO runtime behavior — pure type-system contract change at consumer boundaries. The phase-flip Mutable<> loops DO change runtime behavior (discriminator now flips correctly), but no code branches on phase, so benign. Foundation for future per-variant field-presence work: consumer signatures already narrowed.
- **2026-05-20** — [OSS-397](https://linear.app/kunai/issue/OSS-397) / OSS-391 Phase 2b merged via PR #122. **Phase 15 of the OSS-381 CBP rollout — closes the OSS-391 walker enter/exit sweep across all 3 in-scope `oxc-walker` walks.** Applies the `walkWithProtocol` wrapper to the 517-line load-bearing extraction walker at `extract.ts:367` — the foundation every other phase reads from. **What landed**: new `ContextStackForEnter` (via `Pick<ContextStack, 'push' | 'pushDefaultExport' | 'getDisplayName' | 'getSymbolName' | 'peek'>`) restricts enter to push/peek/read API on the `ContextStack`. New `ExtractWalkEnterContext` (11 read-only inputs — `source`/`relPath`/`scope`/`transpileJsx`/`sourceExt`/`defaultExtension`/`fileStem`/`fileName`/`imports`/`customInlined`/`hasNonQwikJsxImportSource` — + 5 mutable buffers `activeSegmentBodies` as `readonly ActiveSegmentBody[]` / `pendingClosures` / `pushedNodes` / `parentMap` / `results` + `naming` narrowed to `ContextStackForEnter` + `pushActiveSegmentBody` mutator). New `ExtractWalkExitContext extends Omit<EnterContext, 'naming'>` that widens `naming` back to full `ContextStack` and adds `finaliseTopFrameIfMatches(node)` + `popContextStackForNode(node)` as exit-only act-helpers — the leave handler collapses from ~20 lines to two act-helper calls. Local `const ctx = new ContextStack(...)` renamed to `const naming = ...` so the wrapper parameter `ctx` doesn't shadow it (preferred reading: `ctx.naming.push(name)` vs `ctx.ctx.push(name)`). **Both compile-time probes verified** (then reverted): `ctx.naming.pop()` in enter → `tsc` reports `Property 'pop' does not exist on type 'ContextStackForEnter'`; `ctx.popContextStackForNode(node)` in enter → `tsc` reports `Property 'popContextStackForNode' does not exist on type 'ExtractWalkEnterContext'`. **Strongest type-level enforcement of any walker refactor in the sweep** — the Pick-based narrow gates 16 enter-side push sites AND the single leave-side `ctx.naming.pop()` at compile time. Phase 2a (event-capture) was modest by comparison; this is the bullseye. **Invariants preserved verbatim**: OSS-368 buffered-frame (`bodyIds.add` in enter via mutable Set on per-frame objects accessed through ReadonlyArray); OSS-353 closure threading (`pendingClosures.push` in enter, `closureNodesOut.set` after walk); JSX detection (`hasJsx = true` per-frame mutation in enter); inlinedQrl detection branch at `extract.ts:491-636`; `disambiguateExtractions` post-pass outside the wrapper; ContextStack push/pop discipline (16 enter pushes + 1 leave pop). **New design precedent**: the `Omit + redeclare` pattern for property-type widening in interface extension is now established — TS interface extension allows narrowing property types but not widening; `Omit<T, K>` + add-it-back-with-wider-type is the workaround. Net +153/-60 in single file. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md drift folded into the same docs PR alongside this state refresh** — `extract.ts` is on the trigger checklist and the ~60 lines of new interface definitions at the top of the file pushed 5 cites past threshold: `resolveCanonicalCalleeName` +56 (was :231, now :287), `getDirectWrapperContextName` +56 (was :243-259, now :299-...), `disambiguateExtractions` +93 (was :901-934, now :994-...), inlinedQrl detection +109 (was :491-636, now :600-...), marker-call extraction block + ExtractionResult fields-table cites +109 (was :716-751, now :825-...). All refreshed. **OSS-391 sweep structurally complete** — wrapper applied to all 3 in-scope enter+leave `oxc-walker` walks (JSX via Phase 1 PR #115 / buildExtractionLoopMap via Phase 2a PR #120 / extract.ts via Phase 2b PR #122).
- **2026-05-20** — [OSS-396](https://linear.app/kunai/issue/OSS-396) / OSS-391 Phase 2a merged via PR #120. **Phase 14 of the OSS-381 CBP rollout — walker enter/exit context split, Phase 2a.** Applies the `walkWithProtocol` wrapper introduced by OSS-391 Phase 1 (PR #115) to `transform/event-capture-promotion.ts:38` (`buildExtractionLoopMap`) — the smaller of the two remaining enter+leave walks. **What landed**: new `BuildExtractionLoopMapEnterContext` (3 read-only inputs + the 3 mutable buffers `extractionLoopMap`/`loopBodyVarDecls`/`loopStack` as `readonly LoopContext[]` + `pushLoop()` mutator) and `BuildExtractionLoopMapExitContext extends BuildExtractionLoopMapEnterContext` (adds `popTopLoopIfMatches(node)` as the exit-only act-helper). The walk migrated from `walk()` to `walkWithProtocol(...)`; handlers read state through `ctx` instead of closure-captured locals; the `loopStack.pop()` on a loop-boundary node moved behind the `popTopLoopIfMatches` act-helper. **Compile-time enforcement verified by probe**: temporarily inserted `ctx.popTopLoopIfMatches(node)` inside the enter handler and `tsc` reported `Property 'popTopLoopIfMatches' does not exist on type 'BuildExtractionLoopMapEnterContext'` — confirms the type-level enforcement. Modest safety value compared to the JSX walker pilot (no MagicString to gate), but the pattern uniformity matters — every non-trivial enter+leave `oxc-walker` call in the codebase is now consistent. **Workflow pattern**: filed Phase 2a as a NEW Linear ticket (OSS-396) rather than re-opening OSS-391; this matches the OSS-388/394/395 sibling-ticket precedent and keeps the ticket-board honest about scope. **Phase 2b unfiled** — applies same wrapper to `extract.ts:367` (the 517-line load-bearing extraction walker with OSS-368 buffered-frame + OSS-353 closure threading + JSX detection invariants). The Phase 2a/2b split was the user's choice during planning: smaller walker first validates the wrapper API on a 2nd non-MagicString walker before tackling the bigger refactor; ticket arc surfaced the type-level enforcement is partial (e.g. `ctx.pop()` on ContextStack stays convention-protected) so it's worth shipping incrementally. Net +64/-19 in a single file. Convergence 185/212 + full-suite 719/769 baselines unchanged. **OPTIMIZER.md audit verdict**: no update needed — `event-capture-promotion.ts` is on the trigger checklist but the doc has no internal citations of it (verified via `grep -n event-capture-promotion .claude/rules/OPTIMIZER.md` returning empty); type-internal refactor; no phase/marker/convention change.
- **2026-05-20** — PR #119 (no-ticket OPTIMIZER.md cumulative-drift refresh). **Sibling to PR #94 / OSS-393 territory.** The per-PR OPTIMIZER.md audit catches structural changes per-merge but not cumulative drift accumulated across many merges; this PR was a periodic refresh after the CBP rollout (OSS-356/357/358 v2 split + OSS-346 sequencer + OSS-389/390 discriminated unions + OSS-391 walkWithProtocol + OSS-392 mixin) pushed ~30 file:line citations past the doc's own ~30-line tolerance. **Audit findings**: built a 3-tier drift table — Tier 1 (>50 lines, 16 cites including the famous `segment-generation.ts:761 → :1048` flagged in PR #118 wrap-up as the worst drift +287), Tier 2 (ExtractionResult fields table whose individual line cites all drifted 200+ lines after the OSS-389 split + the interface itself was decomposed into 3 variants), Tier 3 (under-threshold drift at +1 to +28 lines). User opted for all three tiers swept for completeness — every cite in the doc now matches current source. **Key updates**: postProcess call cite refreshed; `SegmentMetadataInternal` block now correctly cited at both `:436` (inline-strategy) AND `:1083` (default-strategy) sites — a concept-shift the v2 split introduced; `.w([...])` capture emission now cites both passes (`rewriteCallSites:453` inline + `addCaptureWrapping:612-649` after-the-fact); ExtractionResult fields table re-anchored at `extract.ts:63-186` for the discriminated-union variants with the marker-call extraction block at `:716-751` as the per-field anchor; branded types (Origin/SymbolName/Hash/CanonicalFilename/DisplayName/CtxName/ByteOffset) substituted for the old `string` types per OSS-384/385/386 brand propagation. Net +73/-71 across OPTIMIZER.md alone. **Workflow pattern**: doc-only-of-docs PR; no Linear ticket because the doc-refresh policy is a meta-workstream (similar to how PR #93 widened the auto-merge carve-out without a ticket). Took the auto-merge carve-out path; landed instantly because the baseline workflow was already current.
## What to do next

Natural next pickups (smallest to largest scope, per `CONVERGENCE_FAILURES.md`):

- **CBP rollout substantively complete + ALL Phase 2 sweeps shipped — [OSS-381](https://linear.app/kunai/issue/OSS-381) umbrella, all 11 numbered sub-issues + OSS-394/395/396/397/398/399 siblings landed**. Phases 1-17 across PRs #87 / #89 / #91 / #97 / #100 / #103 / #105 / #107 / #109 / #111 / #113 / #115 / #117 / #120 / #122 / #124 / #126 (+ #119 doc-only drift refresh): OSS-382 (double-casts + exhaustiveness), OSS-383 (brand-types foundation), OSS-384 (symbol-family brand propagation), OSS-385 (path-family + CtxName), OSS-386 (position + source-text), OSS-387 (`readonly` discipline), OSS-389 Phase 1 (`ExtractionResult` discriminated union), OSS-390 Phase 1 (`TransformModule` discriminated union), OSS-388 Phase 1 (AST iterator visitor types), OSS-394 / OSS-388 Phase 2 (utils/simplify.ts casts), OSS-395 / OSS-388 Phase 3 (closes cast trajectory), OSS-391 Phase 1 (`walkWithProtocol` wrapper + JSX walker), OSS-392 (`EntryStrategy` mixin + predicate; CBP catch-all polish), OSS-396 / OSS-391 Phase 2a (`walkWithProtocol` applied to `buildExtractionLoopMap`), OSS-397 / OSS-391 Phase 2b (`walkWithProtocol` applied to `extract.ts:367`), OSS-398 / OSS-389 Phase 2 (`ExtractionResult` consumer narrowing — ~12 signatures + 2 phase-flip Mutable<> loops), OSS-399 / OSS-390 Phase 2 (TransformModule null-arm cleanup + 18-site consumer narrow sweep). **Brand-propagation complete; readonly-discipline complete; both load-bearing discriminated unions have Phase 1 + Phase 2 narrowing surface AND the null-arm cleanup; AST iterator + simplify boundaries tightened; OSS-388 cast-reduction closed across Phases 1-3; OSS-391 walker enter/exit sweep STRUCTURALLY COMPLETE — wrapper applied to all 3 in-scope `oxc-walker` enter+leave walks; EntryStrategy mixin consolidates the last identified parallel-declaration anti-pattern; OPTIMIZER.md cumulative drift swept (PR #119); ExtractionResult phase discriminator now flips correctly across the pipeline; TransformModule variants accurately model their distinct shapes (no redundant null-arm fields).** **Every Phase 2 sweep originally deferred from Phase 1 work has now shipped.** Only OSS-389 Phase 3 candidates (per-variant field-presence via `?: never` + construct-new at phase transitions) remain as deferred follow-ups across the entire umbrella, and those were explicitly classified as low-value/high-risk during the OSS-398 audit. The natural pivots are: (a) **resume parity feature work** (1 remaining F8 test `example_component_with_event_listeners_inside_loop`, F2 cluster rebase, F3 lightweight-inline coordinated rewrite, F5/F6/F7/F9/F10 still open per `CONVERGENCE_FAILURES.md`) — the most direct path to convergence 186+, or (b) OSS-389 Phase 3 if specific safety-bar gaps surface in practice. **Workflow pattern definitive**: Phase 1 / Phase 2 split is now **9 consecutive landings** (OSS-389 Phase 1 → OSS-390 Phase 1 → OSS-388 Phase 1 → OSS-394 / 395 → OSS-391 Phase 1 → OSS-396 / 397 → OSS-398 → OSS-399). Sibling-ticket-when-picked-up is the structurally definitive workflow norm for this codebase.
- **Code health pass complete** (14 landings: OSS-364 through OSS-377). Both major sub-threads exhausted: back-to-back-walk (closed with OSS-373) and "Reduce function parameters" (closed with OSS-377). The segment-body 5–7× re-parse opportunity from OSS-367 recon remains documented as a separate workstream once a perf signal motivates it.
- **`example_qwik_react` flipped** via the OSS-378 → OSS-379 → OSS-380 arc (convergence 184 → 185). The `ts-output/` untrack chore (OSS-380) + a 2-line `ci-full.json`/`local.json` gitignore follow-up (PR #84) both shipped alongside. No open follow-ups from this arc.
- **F8 individual fixes — CLOSED.** All five F8 subfeatures shipped (F8a OSS-361, F8b OSS-362, F8c OSS-363, F8d via 3-PR arc OSS-400/401/402). `example_qwik_react` remains failing on a different root cause (migration `_auto_filterProps`); it sits adjacent to F8 in CONVERGENCE_FAILURES.md but is its own one-off.
- **Broader `simplify` coverage**. PR #48 added `src/optimizer/utils/simplify.ts` scoped to JSX prop values only. SWC's `simplify::simplifier` runs program-wide. Likely candidate sites for the same module: parent-rewrite expression positions, segment-codegen body transforms, signal-analysis hoisted expressions. File a ticket and expand once a second test demands it.
- **F4/F1b-adjacent edge cases**. The compareAst additions across PR #42 (`stripFrameworkHelperImports`, broader `isReorderableDeclaration`) and PR #46 (`stripBareQrlPreloadCalls`) are general — they may unblock other tests too. Worth scanning the 30 failing list to see if any previously-blocked tests now flip cleanly with a small targeted optimizer fix.
- **De-any follow-ups (PR #44)**: ~150 instances in `src/testing/ast-compare.ts` (testing util — biggest single concentration; possible 2–3h slog) and ~22 in `tests/optimizer/`. Convention is locked in; mechanical apply.
- **F2 cluster — umbrella + 4 remaining sub-issues + 1 sibling.** [OSS-403](https://linear.app/kunai/issue/OSS-403) is the umbrella; OSS-406 (PR #134) + OSS-404 (PR #136) closed the first two sub-issues. Remaining: OSS-405 (`useLexicalScope()` bodies), OSS-407 (Hoist + regCtxName → `_regSymbol`), OSS-408 (peer-tool `inlinedQrl(...)` re-emission), OSS-409 (`_fnSignal` dedup), OSS-410 (JSX dev-info `lineNumber:` offset — new sibling). Stale `ast-parity/F2` branch fully superseded (F1c port landed in OSS-404). Pickup audit must check whether OSS-406+OSS-404's landings subsumed any of the remaining target tests before re-investigating.
- **Perf follow-ups**. BENCH-01 at **2.66×** (cap 1.15×), BENCH-02 at **4.67×** (cap 1.5×). File a Linear ticket per workstream; append BENCHMARKS.md row before/after.
- **Two OSS-347 backlog candidates**: per-iteration `ext` mutation in segment-generation; split `SegmentGenerationContext`'s 28 fields. File when picked up.
- **Rust/OXC port** (`oxc-port` branch, long-lived). Scaffolding-only at this point — workspace + napi crate + 4K LOC of dormant Rust at oxc 0.129 / napi 3. Resume when ready; not blocking TS parity work.

**If you're between sessions and need to pick up cold:** read `OPTIMIZER.md` first (Two-namespaces section + Phase pipeline table + marker catalog), then `BENCHMARKS.md` for perf, then `CONVERGENCE_FAILURES.md` for remaining parity work.

## Maintenance

**You are expected to update this file actively.** Not a passive snapshot — a working artifact.

### Branch scoping

Unlike other files in `.claude/rules/` (CONSTRAINTS, REGRESSION, METHODOLOGIES, CONVERGENCE_FAILURES, LINEAR, OPTIMIZER) which are project-wide rules edited and reviewed in isolation, **STATE.md is branch-scoped**. It reflects the active workstream of whatever branch it's committed on:

- **Edit/commit on feature & working branches only.** Never edit STATE.md directly on `main` as part of a standalone changeset. Never cherry-pick a STATE.md change to `main`.
- **It travels with merges.** When a feature branch merges into `main`, its STATE.md comes along naturally. After the merge, `main` carries that branch's final state until the next branch merges and overwrites it.
- **A branch without STATE.md is fine.** Branches that predate this convention or haven't started a workstream may not have one. Create one when meaningful work begins.
- **Refresh on new branches.** When branching off `main` for a new feature, the inherited STATE.md belongs to a different workstream. Update it to reflect the new branch's goal, branches-in-flight, feature focus, and progress log — don't carry over stale state from unrelated work.

In short: STATE.md is *authored* on feature branches, *propagates* through merges, and is *refreshed* (not deleted) on new branches.

### When to update

Update when:

- A test flips status (passing ↔ failing).
- A feature's status changes (OPEN → PARTIAL → CLOSED).
- A branch is created, merged, abandoned, or force-pushed.
- A Linear ticket's status changes (Backlog → In Progress → In Review → Done) for tickets the active workstream tracks.
- A feature description is materially corrected.
- A new substantial discovery refines scope.

Don't update for:

- Every commit (commit messages are the source of truth there).
- Mid-investigation debugging.
- Speculation about future features.

Each update:

1. Bump the "Last updated" date.
2. Add an entry at the top of "Most recent meaningful progress" with the date and a one-line summary.
3. Trim entries older than ~10 to keep this file from bloating.
4. Update the "Current measurements" table if test counts changed.
5. Update the "Branches in flight", "Refactor track v2", and "Parity feature status" tables if any changed.

## Where to look for more

- `OPTIMIZER.md` — end-to-end pipeline walkthrough with deep dives on capture analysis, migration policy, JSX rewrite, and segment metadata. Read this when onboarding into a new optimizer area.
- [`BENCHMARKS.md`](../../BENCHMARKS.md) (repo root) — perf-history doc tracking BENCH-01 / BENCH-02 wall-time vs the SWC reference, with two Mermaid charts and a runbook for adding new data points.
- `CONVERGENCE_FAILURES.md` — feature breakdown with per-test root causes
- `CONSTRAINTS.md` — hard rules (read-only directories)
- `REGRESSION.md` — regression invariants (now CI-enforced)
- `METHODOLOGIES.md` — process / workflow rules including the Refactoring section
- `LINEAR.md` — ticket management conventions including state UUIDs and auto-assignment
- `PROJECT.md` — portable-skill bindings (tracker, state file, stand-up + wrap-up config) consumed by `~/.claude/skills/{daily-stand-up,wrap-up,bootstrap-project}/SKILL.md`
- `.github/workflows/README.md` — CI workflow documentation
- Linear OSS-343 — refactor track v2 parent (rolls up sub-issue completion)
- `pnpm vitest convergence --run` — current parity measurement
- `pnpm ci:baseline:check <vitest-json>` — local regression check against stored baseline
- `tests/optimizer/failure-families.test.ts` — secondary signal (broader, less strict than convergence)
