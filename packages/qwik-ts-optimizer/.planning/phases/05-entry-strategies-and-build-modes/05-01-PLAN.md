---
phase: 05-entry-strategies-and-build-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/optimizer/entry-strategy.ts
  - src/optimizer/dev-mode.ts
  - src/optimizer/transform.ts
  - src/optimizer/rewrite-parent.ts
  - src/optimizer/rewrite-calls.ts
  - tests/optimizer/entry-strategy.test.ts
  - tests/optimizer/dev-mode.test.ts
autonomous: true
requirements: [ENT-01, ENT-03, ENT-04, MODE-01, MODE-02, MODE-03]

must_haves:
  truths:
    - "Smart/segment/hook entry strategy sets entry field to null in segment metadata"
    - "Component entry strategy sets entry field to parent component's symbol name"
    - "Manual entry strategy sets entry field from manual map lookup"
    - "Dev mode emits qrlDEV() with file/lo/hi/displayName metadata instead of qrl()"
    - "Dev mode JSX emits source info object as trailing _jsxSorted argument"
    - "Dev mode component segments include _useHmr() call"
  artifacts:
    - path: "src/optimizer/entry-strategy.ts"
      provides: "Entry strategy resolution and metadata population"
      exports: ["resolveEntryField"]
    - path: "src/optimizer/dev-mode.ts"
      provides: "Dev mode QRL declaration builders and JSX source info"
      exports: ["buildQrlDevDeclaration", "buildJsxSourceInfo"]
    - path: "tests/optimizer/entry-strategy.test.ts"
      provides: "Entry strategy unit tests"
    - path: "tests/optimizer/dev-mode.test.ts"
      provides: "Dev mode unit tests"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "src/optimizer/entry-strategy.ts"
      via: "resolveEntryField() call for each segment"
      pattern: "resolveEntryField"
    - from: "src/optimizer/rewrite-parent.ts"
      to: "src/optimizer/dev-mode.ts"
      via: "qrlDEV builder when mode=dev"
      pattern: "buildQrlDevDeclaration"
---

<objective>
Entry strategy metadata resolution and dev mode QRL/JSX generation.

Purpose: Wire the `entry` field in segment metadata based on entryStrategy config, and implement dev mode transforms (qrlDEV, JSX source info, _useHmr).

Output: entry-strategy.ts module, dev-mode.ts module, updated transform.ts and rewrite-parent.ts to use them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entry-strategies-and-build-modes/05-RESEARCH.md

@src/optimizer/types.ts
@src/optimizer/transform.ts
@src/optimizer/rewrite-parent.ts
@src/optimizer/rewrite-calls.ts
@src/optimizer/segment-codegen.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entry strategy resolution and dev mode builders</name>
  <files>src/optimizer/entry-strategy.ts, src/optimizer/dev-mode.ts, tests/optimizer/entry-strategy.test.ts, tests/optimizer/dev-mode.test.ts</files>
  <read_first>
    - src/optimizer/types.ts (EntryStrategy, EmitMode, SegmentAnalysis types)
    - src/optimizer/rewrite-calls.ts (existing buildQrlDeclaration pattern)
    - .planning/phases/05-entry-strategies-and-build-modes/05-RESEARCH.md (Pattern 1-5)
  </read_first>
  <behavior>
    - resolveEntryField("smart", symbolName, ctxName, parentSymbol, manual) returns null
    - resolveEntryField("segment", ...) returns null (same as smart)
    - resolveEntryField("hook", ...) returns null (same as smart)
    - resolveEntryField("component", symbolName, ctxName, parentComponentSymbol, manual) returns parentComponentSymbol when ctxName is not "component", null when ctxName is "component"
    - resolveEntryField("single", ...) returns a fixed entry name
    - resolveEntryField with manual map returns manual[symbolName] if present, null otherwise
    - buildQrlDevDeclaration(symbolName, canonicalFilename, devFile, lo, hi, displayName) returns `const q_{symbolName} = /*#__PURE__*/ qrlDEV(()=>import("./{canonicalFilename}"), "{symbolName}", { file: "{devFile}", lo: {lo}, hi: {hi}, displayName: "{displayName}" });`
    - buildJsxSourceInfo(fileName, lineNumber, columnNumber) returns `{ fileName: "{fileName}", lineNumber: {lineNumber}, columnNumber: {columnNumber} }`
    - buildUseHmrInjection(filePath) returns `_useHmr(import.meta.hot, "{filePath}");` (or similar based on snapshot evidence)
  </behavior>
  <action>
Create src/optimizer/entry-strategy.ts with:
- `resolveEntryField(strategyType, symbolName, ctxName, parentComponentSymbol, manual)` function
- Smart/segment/hook: return null (current behavior, segments as separate files)
- Component: return the parent component's symbol name for non-component segments. For component segments themselves, return null.
- Manual: look up symbolName in the manual map; return the mapped value or null
- Single: return a fixed entry grouping name (all segments grouped together)

Create src/optimizer/dev-mode.ts with:
- `buildQrlDevDeclaration(symbolName, canonicalFilename, devFile, lo, hi, displayName)` returns the qrlDEV const declaration string matching snapshot format from example_dev_mode
- `buildDevFilePath(inputPath, srcDir, devPath?)` returns absolute dev file path: devPath if provided, otherwise srcDir + "/" + inputPath
- `buildJsxSourceInfo(fileName, lineNumber, columnNumber)` returns the source info object literal string for JSX dev mode (trailing arg to _jsxSorted)
- `buildUseHmrCall(filePath)` returns `_useHmr(import.meta.hot, "${filePath}")` string for component segment injection. Note: if no snapshot evidence exists for _useHmr, implement best-effort from Qwik source and mark with TODO for verification.

Create tests for both modules with the behavior cases above.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run tests/optimizer/entry-strategy.test.ts tests/optimizer/dev-mode.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - resolveEntryField returns correct entry values for all strategy types
    - buildQrlDevDeclaration output matches example_dev_mode snapshot format exactly
    - buildJsxSourceInfo output matches example_jsx_keyed_dev format
    - All new tests pass
  </acceptance_criteria>
  <done>Entry strategy resolution and dev mode builder functions exist with passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire entry strategy and dev mode into pipeline</name>
  <files>src/optimizer/transform.ts, src/optimizer/rewrite-parent.ts, src/optimizer/rewrite-calls.ts, src/optimizer/jsx-transform.ts</files>
  <read_first>
    - src/optimizer/transform.ts (current pipeline, segment metadata assembly at line ~316)
    - src/optimizer/rewrite-parent.ts (QRL declaration generation at step 5)
    - src/optimizer/jsx-transform.ts (transformAllJsx signature for dev mode JSX source info)
    - src/optimizer/entry-strategy.ts (just created in Task 1)
    - src/optimizer/dev-mode.ts (just created in Task 1)
  </read_first>
  <action>
Wire entry strategy into transform.ts:
1. Import resolveEntryField from entry-strategy.ts
2. In the segment metadata assembly loop (~line 316), replace `entry: null` with `resolveEntryField(entryStrategy.type, ext.symbolName, ext.ctxName, parentComponentSymbol, entryStrategy.manual)`. To find parentComponentSymbol: walk up the parent chain from the extraction to find the nearest extraction with ctxName === "component".
3. Pass `options.entryStrategy` (default to `{ type: 'smart' }` if not provided) into the resolution.

Wire dev mode into rewrite-parent.ts:
1. Add `mode` and `devFilePath` parameters to `rewriteParentModule()` signature (optional, defaults to 'prod' behavior)
2. In Step 5 (QRL declarations), when mode === 'dev': use buildQrlDevDeclaration instead of buildQrlDeclaration. This requires passing extraction loc, displayName, and devFile path.
3. When mode === 'dev': add 'qrlDEV' to neededImports instead of 'qrl'
4. Update transform.ts to pass mode and computed devFilePath to rewriteParentModule

Wire dev mode JSX source info:
1. Pass a `devMode` flag through to transformAllJsx (and down to the JSX element transform)
2. When devMode is true, append the source info object `{ fileName, lineNumber, columnNumber }` as a trailing argument to each _jsxSorted call. Use the AST node's loc to get line/column. fileName is the relPath.
3. This requires modifying the JSX transform to accept and propagate the dev flag.

Wire _useHmr for component segments:
1. In segment-codegen.ts (or transform.ts), when mode === 'dev' and the extraction's ctxName === 'component': inject `_useHmr(import.meta.hot, "{devFilePath}")` into the segment body after _captures unpacking (if any). Add `_useHmr` to the segment's imports from `@qwik.dev/core`.

Update rewrite-calls.ts:
1. Export `buildQrlDeclaration` unchanged (still used for non-dev mode and segment nested QRLs)

Ensure all existing 381 tests still pass (dev mode and entry strategy only activate when options specify them; default behavior unchanged).
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Entry metadata populated correctly based on entryStrategy option
    - Dev mode produces qrlDEV declarations with correct metadata object
    - Dev mode JSX includes source info trailing argument
    - Component segments in dev mode include _useHmr call
    - All 381+ existing tests pass (no regressions)
  </acceptance_criteria>
  <done>Entry strategy and dev mode wired into pipeline, all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. The optimizer processes trusted source code from the development build system.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | dev-mode.ts | accept | Dev file paths exposed in dev builds only, not shipped to production |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes all existing + new tests
- Entry strategy unit tests cover smart, component, manual, single strategy types
- Dev mode unit tests verify qrlDEV format, JSX source info format
</verification>

<success_criteria>
- ENT-01: Smart mode segments have `entry: null` in metadata
- ENT-03: Component strategy sets entry to parent component symbol
- ENT-04: Manual strategy uses manual map for entry field
- MODE-01: Dev mode emits qrlDEV with file/lo/hi/displayName
- MODE-02: Dev mode JSX has trailing source info argument
- MODE-03: Component segments in dev mode have _useHmr call
</success_criteria>

<output>
After completion, create `.planning/phases/05-entry-strategies-and-build-modes/05-01-SUMMARY.md`
</output>
