---
phase: 05-entry-strategies-and-build-modes
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/optimizer/strip-exports.ts
  - src/optimizer/const-replacement.ts
  - src/optimizer/transform.ts
  - src/optimizer/rewrite-parent.ts
  - tests/optimizer/strip-exports.test.ts
  - tests/optimizer/const-replacement.test.ts
autonomous: true
requirements: [MODE-06, MODE-07]

must_haves:
  truths:
    - "Strip exports replaces specified export bodies with throw statements"
    - "Strip exports removes imports that become unused after stripping"
    - "isServer=true replaces isServer with true, isBrowser with false"
    - "isServer=false replaces isServer with false, isBrowser with true"
    - "isDev replaced with true in dev mode, false in prod mode"
    - "Const replacement only applies to identifiers imported from @qwik.dev/core or @qwik.dev/core/build"
  artifacts:
    - path: "src/optimizer/strip-exports.ts"
      provides: "Export stripping with throw replacement and import cleanup"
      exports: ["stripExportDeclarations"]
    - path: "src/optimizer/const-replacement.ts"
      provides: "isServer/isBrowser/isDev identifier replacement"
      exports: ["replaceConstants"]
    - path: "tests/optimizer/strip-exports.test.ts"
      provides: "Strip exports unit tests"
    - path: "tests/optimizer/const-replacement.test.ts"
      provides: "Const replacement unit tests"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "src/optimizer/strip-exports.ts"
      via: "stripExportDeclarations() before parent rewrite"
      pattern: "stripExportDeclarations"
    - from: "src/optimizer/rewrite-parent.ts"
      to: "src/optimizer/const-replacement.ts"
      via: "replaceConstants() during parent assembly"
      pattern: "replaceConstants"
---

<objective>
Strip exports mode and isServer/isBrowser/isDev const replacement.

Purpose: Implement the stripExports option that replaces specified exports with throw statements (removing their now-unused imports), and implement const replacement for build-time boolean constants.

Output: strip-exports.ts module, const-replacement.ts module, updated pipeline to apply both transforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entry-strategies-and-build-modes/05-RESEARCH.md
@.planning/phases/05-entry-strategies-and-build-modes/05-01-SUMMARY.md

@src/optimizer/types.ts
@src/optimizer/transform.ts
@src/optimizer/rewrite-parent.ts
@src/optimizer/marker-detection.ts

<interfaces>
From src/optimizer/types.ts:
```typescript
export interface TransformModulesOptions {
  stripExports?: string[];
  isServer?: boolean;
  mode?: EmitMode;  // 'dev' | 'prod' | 'lib'
}
```

From src/optimizer/marker-detection.ts:
```typescript
export function collectImports(program: any): Map<string, ImportInfo>;
export interface ImportInfo {
  localName: string;
  importedName: string;
  source: string;
  isQwikCore: boolean;
  specifierStart: number;
  specifierEnd: number;
  declarationStart: number;
  declarationEnd: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip exports and const replacement modules</name>
  <files>src/optimizer/strip-exports.ts, src/optimizer/const-replacement.ts, tests/optimizer/strip-exports.test.ts, tests/optimizer/const-replacement.test.ts</files>
  <read_first>
    - .planning/phases/05-entry-strategies-and-build-modes/05-RESEARCH.md (Patterns 8, 9)
    - src/optimizer/marker-detection.ts (collectImports, ImportInfo -- for understanding import tracking)
    - src/optimizer/rewrite-parent.ts (how magic-string is used for overwrites)
  </read_first>
  <behavior>
    - stripExportDeclarations on `export const onGet = () => { mongodb(); };` with stripExports=["onGet"] produces `export const onGet = ()=>{\n    throw "Symbol removed by Qwik Optimizer, it can not be called from current platform";\n};` and removes the mongodb import if unused elsewhere
    - stripExportDeclarations preserves exports NOT in the strip list
    - stripExportDeclarations keeps imports used by both stripped and non-stripped code
    - replaceConstants with isServer=true replaces `isServer` identifiers (from @qwik.dev/core import) with `true` and `isBrowser` with `false`
    - replaceConstants with isServer=false does the inverse
    - replaceConstants with isServer=undefined does nothing
    - replaceConstants replaces isDev with true when mode='dev', false when mode='prod'
    - replaceConstants does NOT replace `isServer` if it's a user-defined variable (not imported from qwik)
    - replaceConstants removes the import statement for isServer/isBrowser/isDev from @qwik.dev/core/build after replacement
  </behavior>
  <action>
Create src/optimizer/strip-exports.ts:
- `stripExportDeclarations(source: string, s: MagicString, program: AST, stripExports: string[], importMap: Map<string, ImportInfo>)`: Walk program.body for ExportNamedDeclaration nodes. For each whose exported name is in stripExports, replace the initializer with `()=>{\n    throw "Symbol removed by Qwik Optimizer, it can not be called from current platform";\n}`. After all replacements, scan remaining code for import usage. Remove import specifiers that are no longer referenced by any non-stripped code. If an entire import declaration becomes empty, remove the whole statement. Returns `{ strippedNames: string[] }`.
- The throw string must match exactly: `"Symbol removed by Qwik Optimizer, it can not be called from current platform"` (verified from snapshot).

Create src/optimizer/const-replacement.ts:
- `replaceConstants(source: string, s: MagicString, program: AST, importMap: Map<string, ImportInfo>, isServer?: boolean, isDev?: boolean)`: Find identifiers named `isServer`, `isBrowser`, `isDev` that trace to imports from `@qwik.dev/core`, `@qwik.dev/core/build`, `@builder.io/qwik/build`, or `@builder.io/qwik-city/build`. Replace each reference with the boolean literal. Remove the import binding for replaced identifiers. Use oxc-walker to walk the AST and find Identifier nodes, checking against the import map to ensure only imported instances are replaced (not user-defined variables with the same name). Returns `{ replacedCount: number }`.
- Note: Do NOT implement dead code elimination (if(false) branch removal) in this plan. That is a separate concern handled by downstream bundlers. The snapshot example_build_server shows DCE but the research notes say to keep it simple.

Create thorough tests for both modules.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run tests/optimizer/strip-exports.test.ts tests/optimizer/const-replacement.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Strip exports produces exact throw statement format from snapshots
    - Strip exports removes unused imports, keeps shared imports
    - Const replacement only targets imported identifiers, not user variables
    - Const replacement removes the build import statement after replacement
    - All new tests pass
  </acceptance_criteria>
  <done>Strip exports and const replacement modules exist with passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire strip exports and const replacement into pipeline</name>
  <files>src/optimizer/transform.ts, src/optimizer/rewrite-parent.ts</files>
  <read_first>
    - src/optimizer/transform.ts (full pipeline -- need to find correct insertion points)
    - src/optimizer/rewrite-parent.ts (magic-string flow)
    - src/optimizer/strip-exports.ts (just created)
    - src/optimizer/const-replacement.ts (just created)
  </read_first>
  <action>
Wire strip exports into transform.ts/rewrite-parent.ts:
1. When `options.stripExports` is non-empty, call `stripExportDeclarations()` on the magic-string instance during parent rewriting. This should happen AFTER import rewriting (Step 1-2 in rewrite-parent.ts) but BEFORE extraction call site rewriting (Step 4). This ensures the import map is already built and import source paths are rewritten.
2. Pass `options.stripExports` and the import map to the strip function.
3. The function modifies the magic-string in place (overwrites export bodies, removes unused imports).

Wire const replacement into transform.ts/rewrite-parent.ts:
1. When `options.isServer` is defined OR mode is 'dev'/'prod', call `replaceConstants()` on the magic-string during parent rewriting.
2. Apply const replacement AFTER all other rewrites (Step 6 in rewrite-parent.ts, before final assembly) so it doesn't conflict with other magic-string edits.
3. Compute isDev: `options.mode === 'dev'`.
4. Pass isServer and isDev to replaceConstants.

Also apply const replacement in segment bodies:
1. In segment-codegen.ts, after generating the segment body text, if isServer is defined or isDev is relevant, apply const replacement to the segment code. This can be done by parsing the segment code with oxc-parser and running replaceConstants on it.
2. This ensures segments also get isServer/isBrowser/isDev replaced.

Ensure all existing 381 tests still pass (strip exports and const replacement only activate when options specify them).
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - stripExports option produces throw statements for specified exports
    - isServer option replaces boolean constants in both parent and segment code
    - mode option controls isDev replacement
    - Default behavior (no stripExports, no isServer) unchanged
    - All 381+ existing tests pass
  </acceptance_criteria>
  <done>Strip exports and const replacement wired into pipeline, all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Build configuration is trusted input from the development environment.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | strip-exports.ts | accept | stripExports list from build config, not user input |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes all existing + new tests
- Strip exports tests verify throw format and import cleanup
- Const replacement tests verify import-awareness and boolean substitution
</verification>

<success_criteria>
- MODE-06: Specified exports replaced with throw statements, unused imports removed
- MODE-07: isServer/isBrowser/isDev replaced with correct boolean values based on config
</success_criteria>

<output>
After completion, create `.planning/phases/05-entry-strategies-and-build-modes/05-03-SUMMARY.md`
</output>
