---
phase: 06-diagnostics-and-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/optimizer/diagnostics.ts
  - src/optimizer/transform.ts
  - src/optimizer/types.ts
  - tests/optimizer/diagnostics.test.ts
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "C02 FunctionReference error is emitted when a function or class declaration is captured across a $() boundary"
    - "C03 CanNotCapture error is emitted when a $() argument is not a function expression but captures local identifiers"
    - "C05 MissingQrlImplementation error is emitted when foo$ is called but fooQrl is not exported in the same file"
    - "preventdefault-passive-check warning is emitted when both passive:event and preventdefault:event exist on same element"
    - "@qwik-disable-next-line comment directive suppresses matching diagnostics on the next source line"
    - "Diagnostics do NOT prevent code generation -- output is always produced alongside diagnostics"
  artifacts:
    - path: "src/optimizer/diagnostics.ts"
      provides: "Diagnostic emission functions and @qwik-disable-next-line suppression"
    - path: "tests/optimizer/diagnostics.test.ts"
      provides: "Unit tests for all diagnostic codes and suppression directive"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "src/optimizer/diagnostics.ts"
      via: "import and call diagnostic detection functions during pipeline"
      pattern: "emitC02|emitC03|emitC05|filterSuppressed"
---

<objective>
Implement diagnostic emission (C02, C03, C05, preventdefault-passive-check) and the @qwik-disable-next-line suppression directive.

Purpose: The optimizer must report invalid code patterns (functions crossing $() boundaries, invalid captures, missing Qrl exports) as diagnostics in the TransformOutput, matching the snapshot format exactly. The suppression directive allows users to silence specific diagnostics per-line.

Output: `src/optimizer/diagnostics.ts` module with detection + suppression logic, wired into `transform.ts` pipeline, with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-diagnostics-and-convergence/06-RESEARCH.md

@src/optimizer/types.ts
@src/optimizer/transform.ts
@src/optimizer/capture-analysis.ts
@src/optimizer/extract.ts
@src/optimizer/marker-detection.ts
@src/testing/snapshot-parser.ts

<interfaces>
<!-- From src/optimizer/types.ts -->
```typescript
export interface Diagnostic {
  severity: 'error' | 'warning';
  code: string;
  message: string;
  file: string;
  highlights: DiagnosticHighlight[];
}

export interface DiagnosticHighlight {
  message: string | null;
  loc: SourceLocation;
}

export interface SourceLocation {
  start_line: number;
  start_col: number;
  end_line: number;
  end_col: number;
}
```

<!-- From src/testing/snapshot-parser.ts (snapshot format differs!) -->
```typescript
export interface Diagnostic {
  category: string;  // not "severity"
  code: string;
  file: string;
  message: string;
  highlights: Array<{
    lo: number;     // flat, not nested SourceLocation
    hi: number;
    startLine: number;
    startCol: number;
    endLine: number;
    endCol: number;
  }> | null;
  suggestions: null;
  scope: string;    // not in optimizer type
}
```

<!-- From src/optimizer/extract.ts -->
```typescript
export interface ExtractionResult {
  symbolName: string;
  displayName: string;
  calleeName: string;
  isBare: boolean;
  isSync: boolean;
  bodyText: string;
  ctxKind: 'function' | 'eventHandler' | 'jSXProp';
  ctxName: string;
  origin: string;
  captureNames: string[];
  captures: boolean;
  // ... position fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create diagnostics module with C02/C03 detection and suppression</name>
  <files>src/optimizer/diagnostics.ts, tests/optimizer/diagnostics.test.ts</files>
  <read_first>
    - src/optimizer/types.ts (Diagnostic type -- needs updating to match snapshot format)
    - src/optimizer/capture-analysis.ts (capture detection -- C02 needs to classify fn/class captures)
    - src/optimizer/extract.ts (ExtractionResult shape, calleeName for C03)
    - src/testing/snapshot-parser.ts (snapshot Diagnostic format with category/scope/flat highlights)
    - match-these-snaps/qwik_core__test__example_capturing_fn_class.snap (C02 expected format)
    - match-these-snaps/qwik_core__test__should_disable_multiple_rules_from_single_directive.snap (C03 + suppression)
  </read_first>
  <behavior>
    - Test: emitC02 produces error with code "C02", message containing identifier name and "it's a function"/"it's a class"
    - Test: C02 is emitted for each fn/class captured (not for uncaptured ones like class Other in snapshot)
    - Test: emitC03 produces error with code "C03" when $() arg is not ArrowFunction/FunctionExpression but captures locals
    - Test: C03 message lists the captured local identifiers comma-separated
    - Test: parseDisableDirectives extracts codes from @qwik-disable-next-line comments (single code, multiple comma-separated codes)
    - Test: filterSuppressedDiagnostics removes diagnostics whose code matches a directive on the previous line
    - Test: suppression only affects the NEXT line, not subsequent lines
    - Test: diagnostics have category field (not severity) matching snapshot format
  </behavior>
  <action>
    1. Update `src/optimizer/types.ts` Diagnostic interface to match snapshot format:
       - Rename `severity` to `category` (values remain 'error' | 'warning')
       - Add `scope: string` field (always "optimizer")
       - Add `suggestions: null` field
       - Change `highlights` to use flat format: `Array<{ lo: number; hi: number; startLine: number; startCol: number; endLine: number; endCol: number }> | null`
       - Remove DiagnosticHighlight and SourceLocation interfaces (no longer needed)

    2. Create `src/optimizer/diagnostics.ts` with:
       - `emitC02(identName: string, file: string, isClass: boolean): Diagnostic` -- creates C02 FunctionReference error. Message: "Reference to identifier '{name}' can not be used inside a Qrl($) scope because it's a function" (for classes: "because it's a class"). Highlights: null.
       - `emitC03(identNames: string[], file: string, highlightSpan?: {lo, hi, startLine, startCol, endLine, endCol}): Diagnostic` -- creates C03 CanNotCapture error. Message: "Qrl($) scope is not a function, but it's capturing local identifiers: {comma-separated}".
       - `parseDisableDirectives(sourceCode: string): Map<number, Set<string>>` -- scans source lines for `@qwik-disable-next-line` followed by comma-separated codes. Returns map of (lineNumber+1) -> set of suppressed codes. Works for both `/* @qwik-disable-next-line CODE */` and `{/* @qwik-disable-next-line CODE */}` forms.
       - `filterSuppressedDiagnostics(diagnostics: Diagnostic[], directives: Map<number, Set<string>>): Diagnostic[]` -- removes diagnostics whose code appears in the directive map for their source line.

    3. Create `tests/optimizer/diagnostics.test.ts` with unit tests for all the above behaviors.

    To detect C02 conditions during transform: the capture analysis already finds captured identifiers. C02 needs additional classification of whether each captured name was declared as a function/class in the enclosing scope. This classification will be wired in Task 2 -- this task creates the emission functions and tests them in isolation.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/diagnostics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - emitC02 and emitC03 produce Diagnostic objects matching snapshot format exactly (category not severity, scope: "optimizer", highlights as flat array or null)
    - parseDisableDirectives correctly parses single and multi-code directives
    - filterSuppressedDiagnostics removes only matching codes on the correct line
    - All tests pass
  </acceptance_criteria>
  <done>C02, C03 emission functions and suppression directive parsing work in isolation with full test coverage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add C05/preventdefault-passive-check and wire all diagnostics into transform pipeline</name>
  <files>src/optimizer/diagnostics.ts, src/optimizer/transform.ts, src/optimizer/types.ts, tests/optimizer/diagnostics.test.ts</files>
  <read_first>
    - src/optimizer/diagnostics.ts (from Task 1)
    - src/optimizer/transform.ts (pipeline -- where to hook diagnostic detection)
    - src/optimizer/extract.ts (extractSegments returns calleeName, isBare etc.)
    - src/optimizer/marker-detection.ts (collectCustomInlined, isMarkerCall -- for C05 detection)
    - match-these-snaps/qwik_core__test__example_missing_custom_inlined_functions.snap (C05 format)
    - match-these-snaps/qwik_core__test__should_ignore_preventdefault_with_passive.snap (passive warning)
    - match-these-snaps/qwik_core__test__should_disable_passive_warning_with_qwik_disable_next_line.snap (suppressed passive)
    - match-these-snaps/qwik_core__test__should_disable_qwik_transform_error_by_code.snap (suppressed C05)
    - match-these-snaps/qwik_core__test__should_only_disable_the_next_line.snap (line-scoped suppression)
  </read_first>
  <behavior>
    - Test: emitC05 produces error with code "C05" when foo$ called but fooQrl not exported
    - Test: emitPreventdefaultPassiveCheck produces warning when both passive:event and preventdefault:event exist
    - Test: transform pipeline emits C02 for function/class captures in example_capturing_fn_class
    - Test: transform pipeline emits C05 for missing custom inlined Qrl in example_missing_custom_inlined_functions
    - Test: transform pipeline still produces valid code output alongside diagnostics (non-blocking)
    - Test: @qwik-disable-next-line suppresses matching diagnostics in pipeline output
  </behavior>
  <action>
    1. Add to `src/optimizer/diagnostics.ts`:
       - `emitC05(calleeName: string, qrlName: string, file: string, highlightSpan?: {...}): Diagnostic` -- C05 MissingQrlImplementation. Message: "Found '{calleeName}' but did not find the corresponding '{qrlName}' exported in the same file. Please check that it is exported and spelled correctly".
       - `emitPreventdefaultPassiveCheck(eventName: string, file: string): Diagnostic` -- warning with code "preventdefault-passive-check". Message: "preventdefault:{event} has no effect when passive:{event} is also set; passive event listeners cannot call preventDefault()".
       - `classifyDeclarationType(program: any, sourceCode: string, identName: string): 'var' | 'fn' | 'class'` -- walks AST to find the declaration node for identName and classify it. FunctionDeclaration -> 'fn', ClassDeclaration -> 'class', everything else -> 'var'.
       - `detectC05(extractions: ExtractionResult[], moduleExports: Set<string>, file: string): Diagnostic[]` -- for each extraction where calleeName is a custom $-suffixed function (not a known Qwik core function), check if the corresponding Qrl export exists. If not, emit C05.

    2. Wire diagnostics into `src/optimizer/transform.ts` pipeline:
       - After capture analysis: for each extraction with captures, call classifyDeclarationType for each captured name. If fn/class, call emitC02 and add to diagnostics array.
       - After capture analysis: for each extraction where the $() argument is NOT an ArrowFunction/FunctionExpression but has captures, call emitC03.
       - After extraction: detect C05 conditions by checking custom inlined functions against module exports.
       - During JSX transform (or after): check for contradictory passive/preventdefault attributes and emit warning.
       - Before returning: parse disable directives from original source code, filter suppressed diagnostics.
       - CRITICAL: Diagnostics must NOT prevent code generation. The pipeline runs to completion regardless.

    3. Add integration tests to `tests/optimizer/diagnostics.test.ts`:
       - Test transformModule with example_capturing_fn_class input produces C02 diagnostics
       - Test transformModule with example_missing_custom_inlined_functions input produces C05 diagnostic
       - Test transformModule with preventdefault+passive input produces warning
       - Test transformModule with @qwik-disable-next-line suppresses the correct diagnostic
       - Test that code output is still produced when diagnostics fire

    4. Update any tests in other test files if the Diagnostic type change (category vs severity) breaks them.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/diagnostics.test.ts && npx vitest run tests/optimizer/transform.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - C02, C03, C05, and preventdefault-passive-check all emit correct diagnostics through the transform pipeline
    - @qwik-disable-next-line suppresses matching codes
    - Code generation continues even when diagnostics are present
    - Existing transform tests still pass (Diagnostic type change is backward-compatible in tests)
  </acceptance_criteria>
  <done>All 4 diagnostic codes emit correctly through transformModule(), suppression directive works, no regressions in existing tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> Diagnostics | User source code is parsed and analyzed; diagnostic messages include identifier names from source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I (Information Disclosure) | Diagnostic messages | accept | Build-time only tool; diagnostic messages expose identifier names which are already in source code |
</threat_model>

<verification>
- `npx vitest run tests/optimizer/diagnostics.test.ts` -- all diagnostic unit + integration tests pass
- `npx vitest run` -- full suite green, no regressions from Diagnostic type change
</verification>

<success_criteria>
- C02 emitted for fn/class captures, C03 for non-function $() args with captures, C05 for missing Qrl exports
- preventdefault-passive-check warning emitted for contradictory attributes
- @qwik-disable-next-line suppresses matching codes on next line only
- Diagnostics are non-blocking (code output always produced)
- Diagnostic format matches snapshot expectations (category/scope/flat highlights)
</success_criteria>

<output>
After completion, create `.planning/phases/06-diagnostics-and-convergence/06-01-SUMMARY.md`
</output>
