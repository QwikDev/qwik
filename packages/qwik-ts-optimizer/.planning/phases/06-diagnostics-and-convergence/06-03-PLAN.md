---
phase: 06-diagnostics-and-convergence
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/optimizer/transform.ts
  - src/optimizer/rewrite-parent.ts
  - src/optimizer/segment-codegen.ts
  - src/optimizer/extract.ts
  - tests/optimizer/convergence.test.ts
  - tests/optimizer/snapshot-batch.test.ts
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "All 209 snapshot tests pass via AST-based comparison with correct per-snapshot options"
    - "The 5 diagnostic snapshots produce correct diagnostics AND correct code output"
    - "No regressions from previously locked snapshot batches"
    - "Convergence test confirms 209/209 (or near-complete with documented exceptions)"
  artifacts:
    - path: "tests/optimizer/convergence.test.ts"
      provides: "Full convergence test passing all 209 snapshots"
    - path: "tests/optimizer/snapshot-batch.test.ts"
      provides: "Updated locked snapshot batches including diagnostic snapshots"
  key_links:
    - from: "tests/optimizer/convergence.test.ts"
      to: "src/optimizer/transform.ts"
      via: "transformModule() called with per-snapshot options"
      pattern: "transformModule"
---

<objective>
Fix remaining convergence failures to reach 209/209 snapshot pass rate. This involves systematic category-by-category fixes for the root causes identified in research: TS stripping, extraction edge cases, preserveFilenames, and diagnostic snapshot validation.

Purpose: This is the final convergence push. After Plans 01 and 02 handle diagnostics and the highest-impact fixes (import cleanup + explicitExtensions), this plan addresses the remaining root causes to achieve full snapshot parity with the Rust optimizer.

Output: All 209 snapshots passing, convergence test green, phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-diagnostics-and-convergence/06-RESEARCH.md
@.planning/phases/06-diagnostics-and-convergence/06-01-SUMMARY.md
@.planning/phases/06-diagnostics-and-convergence/06-02-SUMMARY.md

@src/optimizer/transform.ts
@src/optimizer/extract.ts
@src/optimizer/rewrite-parent.ts
@src/optimizer/segment-codegen.ts
@src/optimizer/types.ts
@tests/optimizer/convergence.test.ts
@tests/optimizer/snapshot-options.ts

<interfaces>
<!-- Key interfaces from prior plans will be available in SUMMARY files -->
<!-- The convergence test from Plan 02 shows exactly which snapshots still fail -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining convergence failures by root cause category</name>
  <files>src/optimizer/transform.ts, src/optimizer/extract.ts, src/optimizer/rewrite-parent.ts, src/optimizer/segment-codegen.ts</files>
  <read_first>
    - .planning/phases/06-diagnostics-and-convergence/06-02-SUMMARY.md (convergence pass rate after Plan 02, which categories remain)
    - tests/optimizer/convergence.test.ts (run to see current failures -- examine output)
    - src/optimizer/transform.ts (pipeline -- where TS stripping / preserveFilenames would go)
    - src/optimizer/extract.ts (extraction edge cases -- $() inside non-exported functions)
  </read_first>
  <action>
    Run the convergence test from Plan 02 and categorize remaining failures. Then fix each category in priority order:

    **Category A: TS type stripping** (if many failures caused by TypeScript types remaining in output):
    - In `src/optimizer/transform.ts`, apply `oxc-transform` TypeScript stripping to the input code BEFORE extraction when the file extension is .ts or .tsx (matching Rust default behavior of transpileTs=true).
    - Use `import { transform } from 'oxc-transform'` with `{ typescript: { onlyRemoveTypeImports: false } }` to strip all TS syntax.
    - Apply stripping to source before parseSync + extraction.
    - Also strip TS from segment body text before codegen if segments contain TS types.

    **Category B: Extraction edge cases** (if $() calls inside non-exported function bodies aren't found):
    - The extraction walker should already handle nested $() inside any function body. If specific patterns are missed (e.g., $() inside an arrow function assigned to a const), verify the walker's enter conditions handle these cases.
    - Check specific failing snapshots like `example_3.snap` to identify the exact pattern.

    **Category C: preserveFilenames option** (2 snapshots):
    - When `options.preserveFilenames` is true, use the original file stem in segment filenames instead of the hash-based canonical name.
    - Thread through extract.ts or segment-codegen.ts.

    **Category D: Segment-level fixes** (24 snapshots from research):
    - Examine the 24 segment-only failures from the convergence test
    - Common causes: metadata mismatches (captures, paramNames), JSX in segment bodies, self-referential QRL patterns
    - Fix each sub-category as encountered

    **Category E: Remaining individual edge cases**:
    - Each remaining failure after categories A-D gets individual investigation
    - Fix the most common patterns first (highest leverage)
    - Document any snapshots that require features explicitly out of scope (minify/simplify) as known exceptions

    CRITICAL: After each fix, re-run the convergence test to confirm the fix increases pass count and doesn't regress previously passing tests. Work iteratively.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts --reporter=verbose 2>&1 | grep -E "passed|failed" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All or nearly all 209 snapshots pass (target: 209/209 or documented exceptions for out-of-scope features)
    - Previously passing snapshots in snapshot-batch.test.ts still pass
    - Each fix category is addressed systematically, not as whack-a-mole
  </acceptance_criteria>
  <done>Convergence test shows 209/209 passing (or near-complete with documented exceptions for minify/simplify)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify final convergence and diagnostic correctness</name>
  <files>tests/optimizer/convergence.test.ts</files>
  <action>
    Human verification checkpoint for complete Phase 6. Run the following commands and confirm results:

    1. Run full test suite: `npx vitest run`
       - Expected: ALL tests pass (diagnostics, convergence, existing unit tests)
    2. Run convergence test specifically: `npx vitest run tests/optimizer/convergence.test.ts --reporter=verbose`
       - Expected: 209/209 passing (or near-complete with documented exceptions)
    3. Spot-check diagnostic snapshots:
       - `npx vitest run tests/optimizer/convergence.test.ts -t "example_capturing_fn_class"` -- should show C02 diagnostics
       - `npx vitest run tests/optimizer/convergence.test.ts -t "example_missing_custom_inlined"` -- should show C05 diagnostic
       - `npx vitest run tests/optimizer/convergence.test.ts -t "should_disable"` -- suppression tests
    4. Verify no regressions: `npx vitest run tests/optimizer/snapshot-batch.test.ts`
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Human confirms convergence is complete, diagnostics are correct, and no regressions exist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Build-time code transformation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T (Tampering) | TS stripping | mitigate | Use oxc-transform (battle-tested Rust tool) not custom stripping; verify output preserves runtime semantics |
</threat_model>

<verification>
- `npx vitest run` -- full test suite green
- `npx vitest run tests/optimizer/convergence.test.ts` -- 209/209 passing
- `npx vitest run tests/optimizer/snapshot-batch.test.ts` -- no regressions in locked batches
</verification>

<success_criteria>
- 209/209 snapshots pass (or documented near-complete with specific exceptions)
- Diagnostic snapshots produce correct diagnostics AND correct code
- Full test suite green with no regressions
- Human verification confirms convergence is complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-diagnostics-and-convergence/06-03-SUMMARY.md`
</output>
