---
phase: 08-parent-rewrite-batch-2
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - src/optimizer/transform.ts
  - src/optimizer/rewrite-parent.ts
  - src/optimizer/strip-ctx.ts
  - src/optimizer/inline-strategy.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3]

must_haves:
  truths:
    - "All 24 Phase 8 target snapshots pass parent + segment AST comparison"
    - "All previously-passing snapshots still pass (21 baseline + any gained in Plans 01-04)"
    - "Zero regressions in unit tests"
  artifacts:
    - path: "src/optimizer/transform.ts"
      provides: "Final wiring fixes for remaining Phase 8 snapshots"
    - path: "src/optimizer/strip-ctx.ts"
      provides: "Side-effect import preservation under strip + event handler metadata fix"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "all optimizer modules"
      via: "final integration sweep"
      pattern: "transformModule"
---

<objective>
Fix remaining Phase 8 snapshots: example_qwik_react_inline, example_strip_client_code, root_level_self_referential_qrl_inline, example_use_optimization, should_not_move_over_side_effects, and any others still failing after Plans 01-04.

Purpose: This is the final sweep plan that addresses the remaining failure categories (Categories 8 and 9 from research) plus any residual issues. It depends on all prior plans so it can see the cumulative state.

Output: All 24 Phase 8 target snapshots passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-parent-rewrite-batch-2/08-RESEARCH.md
@.planning/phases/08-parent-rewrite-batch-2/08-01-SUMMARY.md
@.planning/phases/08-parent-rewrite-batch-2/08-02-SUMMARY.md
@.planning/phases/08-parent-rewrite-batch-2/08-03-SUMMARY.md
@.planning/phases/08-parent-rewrite-batch-2/08-04-SUMMARY.md

@src/optimizer/transform.ts
@src/optimizer/rewrite-parent.ts
@src/optimizer/strip-ctx.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining Phase 8 snapshot failures</name>
  <files>src/optimizer/transform.ts, src/optimizer/rewrite-parent.ts, src/optimizer/strip-ctx.ts, src/optimizer/inline-strategy.ts</files>
  <read_first>
    - .planning/phases/08-parent-rewrite-batch-2/08-01-SUMMARY.md (what was fixed in Plan 01)
    - .planning/phases/08-parent-rewrite-batch-2/08-02-SUMMARY.md (what was fixed in Plan 02)
    - .planning/phases/08-parent-rewrite-batch-2/08-03-SUMMARY.md (what was fixed in Plan 03)
    - .planning/phases/08-parent-rewrite-batch-2/08-04-SUMMARY.md (what was fixed in Plan 04)
    - src/optimizer/transform.ts
    - src/optimizer/strip-ctx.ts
  </read_first>
  <action>
**Step 1: Run convergence tests for all 24 Phase 8 targets and identify which still fail.**

```bash
npx vitest run tests/optimizer/convergence.test.ts -t "example_props_wrapping|example_qwik_react_inline|example_reg_ctx_name|example_strip_client_code|example_transpile_ts_only|example_use_optimization|fun_with_scopes|inlined_qrl_uses|issue_476|root_level_self_referential_qrl_inline|should_ignore_null|should_keep_|should_not_generate|should_not_move"
```

**Step 2: For each still-failing snapshot, diagnose and fix.**

Known remaining categories from research:

**Category 8: example_qwik_react_inline** -- Uses entry=inline, transpileJsx=false. Expected output uses _noopQrl + .s() pattern. Debug why inline strategy is not applied -- likely an options threading issue where transpileJsx=false causes a code path skip. Check transform.ts for conditional logic that gates inline strategy on transpileJsx.

**Category 9: example_strip_client_code** -- Two issues:
1. Side-effect import preservation: `import "./keep"` and `import "../keep2"` should be preserved, `import "../../remove"` should be stripped. In rewrite-parent.ts import stripping or transform.ts removeUnusedImports, add logic to preserve side-effect imports (no specifiers) unless they match a strip condition. Study the expected output to determine the exact strip rule.
2. Segment metadata: `Parent_component_div_shouldRemove_EBj69wTX1do` may have metadata differences. Check stripCtxName and stripEventHandlers interaction.

**root_level_self_referential_qrl_inline**: Self-referential QRL in inline mode. The QRL variable references itself in its body. Check if the inline strategy handles self-references correctly.

**example_use_optimization, should_not_move_over_side_effects**: May need TS stripping (from Plan 01) + other fixes. Diagnose from diff output.

**Step 3: Fix each remaining issue with targeted patches.**

For each fix, run the specific snapshot test to verify before moving on. After all fixes, run the full convergence suite to confirm zero regressions.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "passed|failed" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 24 Phase 8 target snapshots pass convergence tests
    - All 21+ previously-passing snapshots still pass
    - Full unit test suite passes with zero regressions
    - npx vitest run reports no new failures
  </acceptance_criteria>
  <done>
    - 24/24 Phase 8 target snapshots pass
    - Zero regressions in all tests
    - Phase 8 success criteria fully met
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression sweep and lock verification</name>
  <files></files>
  <read_first>
    - src/optimizer/transform.ts (verify final state)
    - tests/optimizer/convergence.test.ts (understand test structure)
  </read_first>
  <action>
Run the full test suite to confirm all Phase 8 success criteria are met:

1. **Full convergence test**: `npx vitest run tests/optimizer/convergence.test.ts`
   - Count Phase 8 target passes (must be 24/24)
   - Count total passes (should be at least 45 = 21 baseline + 24 Phase 8)
   - Verify no previously-passing test regressed

2. **Full unit test suite**: `npx vitest run`
   - All unit test files pass
   - No new failures

3. **TypeScript check**: `npx tsc --noEmit`
   - No type errors

Document the final convergence count and any bonus passes (snapshots not targeted by Phase 8 that now also pass due to the infrastructure improvements).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shows all unit test files passing
    - npx vitest run tests/optimizer/convergence.test.ts shows 24 Phase 8 targets passing
    - npx tsc --noEmit exits cleanly
  </acceptance_criteria>
  <done>
    - Phase 8 fully verified: 24/24 targets + 21+ baseline all passing
    - Zero regressions in unit tests
    - TypeScript compilation clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- build-time code transformation tool.

## STRIDE Threat Register

No threats identified.
</threat_model>

<verification>
Final phase verification:
1. All 24 Phase 8 snapshots pass: example_props_wrapping, example_props_wrapping2, example_props_wrapping_children, example_props_wrapping_children2, example_qwik_react_inline, example_reg_ctx_name_segments_hoisted, example_reg_ctx_name_segments_inlined, example_strip_client_code, example_transpile_ts_only, example_use_optimization, fun_with_scopes, inlined_qrl_uses_identifier_reference_when_hoisted_snapshot, issue_476, root_level_self_referential_qrl_inline, should_ignore_null_inlined_qrl, should_keep_module_level_var_used_in_both_main_and_qrl, should_keep_non_migrated_binding_from_shared_array_destructuring_declarator, should_keep_non_migrated_binding_from_shared_destructuring_declarator, should_keep_non_migrated_binding_from_shared_destructuring_with_default, should_keep_non_migrated_binding_from_shared_destructuring_with_rest, should_keep_root_var_used_by_export_decl_and_qrl, should_keep_root_var_used_by_exported_function_and_qrl, should_not_generate_conflicting_props_identifiers, should_not_move_over_side_effects
2. All previously-passing snapshots still pass
3. Zero unit test regressions
4. TypeScript clean
</verification>

<success_criteria>
- 24/24 Phase 8 target snapshots passing
- All baseline snapshots still passing
- Full unit test suite green
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-parent-rewrite-batch-2/08-05-SUMMARY.md`
</output>
