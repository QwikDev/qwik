---
phase: 10-segment-identity-batch-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/optimizer/transform.ts
autonomous: true
requirements:
  - SEG-02
  - SEG-03
  - SEG-05

must_haves:
  truths:
    - "Prod mode segments use s_{hash} for symbolName"
    - "Prod mode displayName and canonicalFilename remain full human-readable form"
    - "Import-source naming uses basename of import path for bare identifier args"
    - "captures flag is false when all captures are delivered via _auto_ params"
  artifacts:
    - path: "src/optimizer/transform.ts"
      provides: "Prod mode s_ naming, captures metadata correction"
      contains: "s_"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "src/optimizer/extract.ts"
      via: "ExtractionResult.symbolName mutation for prod mode"
      pattern: "symbolName.*=.*s_"
---

<objective>
Fix three small edge-case segment identity issues: prod mode `s_` prefix naming, import-source naming for bare identifier arguments, and captures metadata correctness.

Purpose: Two snapshots fail because prod mode should use `s_{hash}` as the segment name. One snapshot fails because a bare imported identifier arg should derive its display name from the import source path. One snapshot has wrong `captures` metadata.

Output: Corrections in transform.ts that handle these three cases, passing 4 additional snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-segment-identity-batch-1/10-RESEARCH.md

@src/optimizer/transform.ts
@src/optimizer/extract.ts
@src/hashing/naming.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prod mode s_ naming and captures metadata fix</name>
  <files>src/optimizer/transform.ts</files>
  <read_first>
    - src/optimizer/transform.ts (lines 600-900 -- extraction processing, segment metadata assembly)
    - .planning/phases/10-segment-identity-batch-1/10-RESEARCH.md (Pattern 2: Prod Mode s_ Prefix, Pattern 5: Captures Metadata)
    - match-these-snaps/example_build_server/ (expected segment metadata for prod mode)
    - match-these-snaps/example_prod_node/ (expected segment metadata for prod mode)
    - match-these-snaps/example_functional_component_2/ (expected captures metadata)
  </read_first>
  <action>
    **Prod mode `s_` naming (Pattern 2):**

    In `transform.ts`, after extractions are finalized (after `extractSegments()` call and any disambiguation), but BEFORE segment modules are built and BEFORE parent rewriting:

    1. Add a block that checks `emitMode === 'prod'`:
       ```typescript
       if (emitMode === 'prod') {
         for (const ext of extractions) {
           if (ext.isInlinedQrl) continue; // inlinedQrl has its own naming
           ext.symbolName = 's_' + ext.hash;
           // displayName and canonicalFilename remain unchanged
         }
       }
       ```

    2. This MUST happen AFTER disambiguation (Plan 01) so hashes are final, but BEFORE `rewriteParentModule()` so parent QRL declarations use the `s_` names.

    **Captures metadata fix (Pattern 5):**

    In the segment metadata assembly section (around line 780-855), find where `ext.captures` is set. Currently `ext.captures = ext.captureNames.length > 0` after migration filtering.

    The fix: after filtering captureNames for migrated vars, if the remaining captures are ALL delivered via `_auto_` parameter injection (i.e., `ext.paramNames.length > 0` and `ext.captureNames.length === 0` after filtering), ensure `captures = false`. This should already be the case since `ext.captures = ext.captureNames.length > 0`, but verify. If `paramNames` are populated from captures that didn't get filtered, the captures flag should still be false when those captures become params.

    Check the specific case in example_functional_component_2: if a segment has `paramNames: ['state']` and the capture 'state' was migrated, `captures` should be `false`.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts -t "example_build_server|example_prod_node|example_functional_component_2" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "s_.*ext.hash" src/optimizer/transform.ts shows prod mode naming logic
    - grep -n "emitMode.*prod" src/optimizer/transform.ts shows the prod mode guard
    - npx vitest run tests/optimizer/convergence.test.ts -t "example_prod_node" passes
    - npx vitest run tests/optimizer/convergence.test.ts -t "example_functional_component_2" passes
  </acceptance_criteria>
  <done>
    Prod mode segments use `s_{hash}` naming. Captures metadata is correct. example_build_server, example_prod_node, and example_functional_component_2 pass convergence tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Import-source naming for bare identifier arguments</name>
  <files>src/optimizer/extract.ts</files>
  <read_first>
    - src/optimizer/extract.ts (lines 400-620 -- marker call extraction, argument handling)
    - .planning/phases/10-segment-identity-batch-1/10-RESEARCH.md (Pattern 3: Import-Source Naming)
    - match-these-snaps/example_capture_imports/ (expected segment names with import-source derived display names)
    - src/optimizer/context-stack.ts (how context stack builds display names)
    - src/hashing/naming.ts (escapeSym for path escaping)
  </read_first>
  <action>
    In `extract.ts`, in the section where marker call arguments are processed (around line 470-620), find where the argument type is checked for ArrowFunctionExpression/FunctionExpression.

    When the argument is a bare `Identifier` (not a function expression):
    1. Look up the identifier name in the `imports` map.
    2. If found, extract the import source path: `importInfo.source` (e.g., `"./style.css"`).
    3. Compute the display name from the import source: take the basename (after last `/`), run `escapeSym()` on it. For `./style.css` this produces `style_css`.
    4. Use this escaped import basename as the context portion for naming, instead of the context stack.

    Specifically, in the extraction result construction for bare identifier args:
    - Override the `displayName` to be `fileStem + '_' + escapedImportBasename`
    - Recompute `symbolName` and `hash` using the new context portion
    - Update `canonicalFilename` accordingly

    Import `escapeSym` from `../hashing/naming.js` if not already imported.

    NOTE: This only affects marker calls with bare identifier args that are imported bindings. Local variables or function expressions are unaffected.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts -t "example_capture_imports" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "escapeSym" src/optimizer/extract.ts shows import and usage for import-source naming
    - npx vitest run tests/optimizer/convergence.test.ts -t "example_capture_imports" passes
    - npx vitest run tests/optimizer/ shows zero regressions
  </acceptance_criteria>
  <done>
    Bare imported identifier arguments derive segment display names from import source path. example_capture_imports passes convergence test. No regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- internal algorithm changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | prod mode naming | accept | Internal transform, no external input |
</threat_model>

<verification>
1. Prod mode snapshots pass: `npx vitest run tests/optimizer/convergence.test.ts -t "example_build_server|example_prod_node"`
2. Captures metadata correct: `npx vitest run tests/optimizer/convergence.test.ts -t "example_functional_component_2"`
3. Import-source naming: `npx vitest run tests/optimizer/convergence.test.ts -t "example_capture_imports"`
4. Full regression: `npx vitest run tests/optimizer/`
</verification>

<success_criteria>
- Prod mode uses s_{hash} for symbolName while keeping full displayName/canonicalFilename
- Import-source naming works for bare imported identifier args
- Captures metadata is false when all captures are _auto_ params
- 4 additional snapshots pass (example_build_server, example_prod_node, example_capture_imports, example_functional_component_2)
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-segment-identity-batch-1/10-02-SUMMARY.md`
</output>
