---
phase: 10-segment-identity-batch-1
plan: 02
subsystem: optimizer
tags: [prod-mode, s_-naming, segment-identity, emit-mode]

# Dependency graph
requires:
  - phase: 10-segment-identity-batch-1
    provides: disambiguateExtractions for duplicate display names
provides:
  - Prod mode s_{hash} symbolName in transform.ts
affects: [segment-identity-batch-2, parent-rewrite]

# Tech tracking
tech-stack:
  added: []
  patterns: [prod mode naming applied after disambiguation but before parent rewrite]

key-files:
  created: []
  modified:
    - src/optimizer/transform.ts
    - tests/optimizer/transform.test.ts
    - tests/optimizer/snapshot-batch.test.ts

key-decisions:
  - "Prod mode s_ naming inserted after disambiguation and before rewriteParentModule so parent QRL refs use s_ names"
  - "Unit tests updated to set mode: 'lib' explicitly (matching Rust test harness default EmitMode::Test)"

patterns-established:
  - "Mode-dependent naming: check emitMode before symbolName mutation"

requirements-completed: [SEG-02]

# Metrics
duration: 25min
completed: 2026-04-11
---

# Phase 10 Plan 02: Prod Mode s_ Naming Summary

**Prod mode segments use s_{hash} symbolName while preserving full human-readable displayName/canonicalFilename; import-source naming and captures metadata deferred due to hash reproducibility blocker**

## Performance

- **Duration:** 25 min
- **Started:** 2026-04-11T08:28:33Z
- **Completed:** 2026-04-11T08:53:43Z
- **Tasks:** 1 of 2 completed
- **Files modified:** 3

## Accomplishments
- Prod mode `s_{hash}` symbolName correctly applied for all non-inlinedQrl extractions
- All segment identity metadata (name, displayName, hash, canonicalFilename, parent) verified correct for both example_prod_node and example_build_server
- Unit tests updated to explicitly set mode: 'lib' to match Rust test harness defaults
- Zero regressions: 175 failed | 449 passed (unchanged from baseline)

## Task Commits

Each task was committed atomically:

1. **Task 1: Prod mode s_ naming** - `c0c9955` (feat)

## Files Created/Modified
- `src/optimizer/transform.ts` - Added prod mode s_{hash} symbolName mutation block after disambiguation
- `tests/optimizer/transform.test.ts` - Added mode: 'lib' to 2 migration tests that relied on implicit default
- `tests/optimizer/snapshot-batch.test.ts` - Added mode: 'lib' to all 3 transformModule calls (replace_all)

## Decisions Made
- Prod mode s_ naming inserted between disambiguation (plan 10-01) and rewriteParentModule so parent QRL declarations automatically use s_ names via ext.symbolName reference
- Unit tests that don't specify mode now explicitly set mode: 'lib' to match Rust test harness EmitMode::Test default, preventing false regressions when mode-dependent features are added

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed unit test mode defaults**
- **Found during:** Task 1
- **Issue:** 2 unit tests in transform.test.ts and 3 test calls in snapshot-batch.test.ts did not set mode, defaulting to 'prod', which caused regressions when s_ naming was added
- **Fix:** Added `mode: 'lib'` to all affected test calls to match Rust test harness default
- **Files modified:** tests/optimizer/transform.test.ts, tests/optimizer/snapshot-batch.test.ts
- **Verification:** All previously-passing tests still pass (175 failed | 449 passed)
- **Committed in:** c0c9955

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Test mode defaults corrected. No scope creep.

## Deferred Issues

### Task 2: Import-source naming (BLOCKED)

**Issue:** Cannot reproduce the expected hash `TRu1FaIoUM0` for the import-source naming case (example_capture_imports). Investigated the Rust transform.rs `register_context_name` function extensively -- it uses `stack_ctxt.join("_")` for ALL cases with no special import-source path visible in the code. Tried all plausible combinations of `qwikHash(scope, relPath, contextPortion)` inputs; none produce the expected hash.

**Root cause:** Either the snapshot was generated by a different version of the Rust optimizer that had import-source naming logic not present in the current main branch, or there is an intermediate transform step in the Rust that modifies the context stack based on argument type that was not visible in the code review.

**Recommendation:** This requires running the Rust optimizer locally on this specific input to trace the actual hash computation, or deeper investigation of the Rust SWC fold visitor chain.

### Captures metadata (example_functional_component_2)

**Issue:** The captures metadata fix requires q:ps (passive capture) injection for non-loop event handlers on HTML elements. The Rust optimizer injects q:ps for event handler captures, converting them to function parameters. The TS optimizer does not implement non-loop q:ps injection yet, so the convergence test fails on parent module mismatch before reaching captures comparison.

**Recommendation:** Implement non-loop q:ps injection as part of a later phase focused on capture routing.

## Issues Encountered
- Prod mode convergence tests (example_prod_node, example_build_server) still fail at the convergence level due to segment CODE differences (body ordering, parent module code), not segment identity. Segment identity metadata matches correctly.
- The default mode in transform.ts (`options.mode ?? 'prod'`) is correct for the production API but caused regressions in tests that didn't explicitly set mode.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Prod mode s_ naming complete
- Import-source naming deferred (hash reproducibility investigation needed)
- Captures metadata deferred (requires q:ps injection feature)
- Ready for 10-03 (remaining edge cases)

---
*Phase: 10-segment-identity-batch-1*
*Completed: 2026-04-11*

## Self-Check: PASSED
- src/optimizer/transform.ts: FOUND
- tests/optimizer/transform.test.ts: FOUND
- tests/optimizer/snapshot-batch.test.ts: FOUND
- Commit c0c9955: FOUND
