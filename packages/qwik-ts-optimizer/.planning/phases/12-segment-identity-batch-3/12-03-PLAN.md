---
phase: 12-segment-identity-batch-3
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/optimizer/transform.ts
  - src/optimizer/segment-codegen.ts
  - src/optimizer/jsx-transform.ts
  - src/optimizer/rewrite-parent.ts
autonomous: true
requirements: [P12-04, P12-05]
must_haves:
  truths:
    - "All 21 Phase 12 target snapshots pass both parent and segment AST comparison"
    - "All previously-locked snapshots (34 passing) still pass"
    - "All unit tests pass with zero regressions"
  artifacts:
    - path: "src/optimizer/transform.ts"
      provides: "Complete loop-aware pipeline for all 21 snapshots"
    - path: "src/optimizer/segment-codegen.ts"
      provides: "Correct segment output for all loop handler patterns"
  key_links:
    - from: "convergence.test.ts"
      to: "all 21 Phase 12 snapshots"
      via: "AST comparison"
      pattern: "should_transform.*loop|should_not_wrap|should_ignore_prevent"
---

<objective>
Final convergence sweep for Phase 12: fix remaining snapshot failures from the 21 targets, verify all pass, and confirm zero regressions.

Purpose: Plans 01 and 02 implement the core infrastructure. This plan handles edge cases, remaining failures, and the hard gate verification.

Output: All 21 Phase 12 target snapshots pass. All 34 previously-locked snapshots still pass. All unit tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-segment-identity-batch-3/12-RESEARCH.md
@.planning/phases/12-segment-identity-batch-3/12-01-SUMMARY.md
@.planning/phases/12-segment-identity-batch-3/12-02-SUMMARY.md
@src/optimizer/transform.ts
@src/optimizer/segment-codegen.ts
@src/optimizer/jsx-transform.ts
@src/optimizer/rewrite-parent.ts
@src/optimizer/loop-hoisting.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnostic sweep and targeted fixes for remaining Phase 12 failures</name>
  <files>src/optimizer/transform.ts, src/optimizer/segment-codegen.ts, src/optimizer/jsx-transform.ts, src/optimizer/rewrite-parent.ts</files>
  <read_first>
    - .planning/phases/12-segment-identity-batch-3/12-01-SUMMARY.md
    - .planning/phases/12-segment-identity-batch-3/12-02-SUMMARY.md
    - src/optimizer/transform.ts (loop classification section added in Plan 01)
    - src/optimizer/segment-codegen.ts (signature rewriting added in Plan 01)
    - src/optimizer/rewrite-parent.ts (.w() hoisting added in Plan 02)
    - src/optimizer/jsx-transform.ts (q:p placement changes from Plan 02)
  </read_first>
  <action>
Run the full convergence test and identify which of the 21 Phase 12 targets still fail:

```bash
npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "should_ignore_preventdefault|should_not_transform_events|should_not_wrap_fn|should_only_disable|should_transform_block_scoped|should_transform_component|should_transform_event_names|should_transform_handlers|should_transform_loop|should_transform_multiple_block|should_transform_multiple_event|should_transform_nested|should_transform_passive|should_transform_same_element|should_transform_three|should_transform_two|should_wrap_prop"
```

For each remaining failure:
1. Read the snapshot file from `match-these-snaps/` to get expected output
2. Run the optimizer on the snapshot's input and compare actual vs expected
3. Identify the specific delta (segment code, parent code, metadata)
4. Apply targeted fix

Common edge cases to check:
- **Multiple handlers on same element**: `should_transform_multiple_event_handlers` — multiple event handlers with different captures on the same JSX element
- **Three nested loops**: `should_transform_three_nested_loops_handler_captures_outer_only` — handler in 3rd-level loop capturing from 1st-level loop
- **Mixed capture/no-capture on same element**: `should_transform_same_element_one_handler_with_captures_one_without` — one handler has captures, other doesn't
- **Preventdefault with passive**: `should_ignore_preventdefault_with_passive` — diagnostic suppression + parent codegen
- **Only disable next line**: `should_only_disable_the_next_line` — diagnostic line scoping

For each fix, verify with `npx vitest run tests/optimizer/convergence.test.ts -t "{snapshot_name}"` before moving to the next.

Do NOT scope-creep into fixing snapshots outside the 21 Phase 12 targets. If a fix for a Phase 12 snapshot incidentally fixes other snapshots, that's fine, but don't chase fixes for non-Phase-12 snapshots.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts -t "should_ignore_preventdefault_with_passive|should_not_transform_events_on_non_elements|should_not_wrap_fn|should_only_disable_the_next_line|should_transform_block_scoped_variables_and_item_index_in_loop|should_transform_block_scoped_variables_in_loop|should_transform_component_with_normal_function|should_transform_event_names_without_jsx_transpile|should_transform_handlers_capturing_cross_scope_in_nested_loops|should_transform_loop_multiple_handler_with_different_captures|should_transform_multiple_block_scoped_variables_and_item_index_in_loop|should_transform_multiple_block_scoped_variables_in_loop|should_transform_multiple_event_handlers|should_transform_multiple_event_handlers_case2|should_transform_nested_loops|should_transform_nested_loops_handler_captures_only_inner_scope|should_transform_passive_event_names_without_jsx_transpile|should_transform_same_element_one_handler_with_captures_one_without|should_transform_three_nested_loops_handler_captures_outer_only|should_transform_two_handlers_capturing_different_block_scope_in_loop|should_wrap_prop_from_destructured_array" 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/optimizer/convergence.test.ts reports all 21 Phase 12 target tests as passed
    - No new test failures introduced
  </acceptance_criteria>
  <done>All 21 Phase 12 target snapshots pass convergence test (both parent and segment AST comparison match expected output).</done>
</task>

<task type="auto">
  <name>Task 2: Phase 12 regression gate verification</name>
  <files></files>
  <read_first>
    - (no files to read — this is a verification-only task)
  </read_first>
  <action>
Run the full test suite to verify zero regressions:

1. `npx vitest run` — all unit tests must pass
2. `npx vitest run tests/optimizer/convergence.test.ts` — count total passing, verify >= 55 (34 baseline + 21 new)
3. Verify previously-locked snapshots still pass by checking the test output for any snapshot that was passing before Phase 12

Report:
- Total convergence tests passing: X/210
- Phase 12 targets passing: 21/21
- Previously-locked snapshots: all still passing
- Unit tests: all passing
- Any regressions: none

If any regression found, fix it before completing this task. Regressions in previously-passing snapshots are BLOCKERS.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shows 0 failed tests
    - npx vitest run tests/optimizer/convergence.test.ts shows >= 55 passed (34 baseline + 21 new)
    - grep "failed" output from full vitest run returns 0 test failures
  </acceptance_criteria>
  <done>Phase 12 convergence gate passed: all 21 targets pass, all previously-locked snapshots pass, zero regressions in unit tests. Ready for Phase 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Convergence sweep — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | I (Information) | snapshot comparison | accept | Test infrastructure only, no production risk |
</threat_model>

<verification>
1. `npx vitest run tests/optimizer/convergence.test.ts` shows >= 55 passed
2. `npx vitest run` shows all tests pass with zero failures
3. All 21 Phase 12 target snapshot names appear in passed list
</verification>

<success_criteria>
- 21/21 Phase 12 target snapshots pass
- 34+ previously-locked snapshots still pass
- All unit tests pass
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-segment-identity-batch-3/12-03-SUMMARY.md`
</output>
