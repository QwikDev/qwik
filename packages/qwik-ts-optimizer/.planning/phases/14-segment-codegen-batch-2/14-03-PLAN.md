---
phase: 14-segment-codegen-batch-2
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - src/optimizer/segment-codegen.ts
  - src/optimizer/transform.ts
  - src/optimizer/jsx-transform.ts
  - src/optimizer/signal-analysis.ts
  - src/optimizer/bind-transform.ts
autonomous: true
requirements: [P14-04, P14-05]

must_haves:
  truths:
    - "bind:value/bind:checked merged with onInput$ handlers into q-e:input arrays"
    - "_fnSignal suppressed for component-element (uppercase tag) prop accesses"
    - "All 25 Phase 14 target snapshots pass parent + segment AST comparison"
    - "All previously-locked snapshots still pass (49+ baseline, zero regressions)"
  artifacts:
    - path: "src/optimizer/bind-transform.ts"
      provides: "Bind handler merging into event handler arrays"
    - path: "src/optimizer/signal-analysis.ts"
      provides: "Component-element _fnSignal suppression"
    - path: "src/optimizer/jsx-transform.ts"
      provides: "Spread JSX and passive event fixes"
  key_links:
    - from: "src/optimizer/bind-transform.ts"
      to: "src/optimizer/jsx-transform.ts"
      via: "merged q-e:input entries"
      pattern: "q-e:input"
    - from: "src/optimizer/signal-analysis.ts"
      to: "src/optimizer/jsx-transform.ts"
      via: "component element detection"
      pattern: "_fnSignal"
---

<objective>
Fix remaining Phase 14 segment codegen issues: bind:value/bind:checked merging with event handlers, _fnSignal suppression for component elements, HMR codegen, q:ps unified slot allocation, spread JSX patterns, passive event flags, and hoisted_fn_signal_in_loop edge cases. Sweep all 25 target snapshots to convergence.

Purpose: This is the convergence sweep plan that resolves all remaining Phase 14 failures. Prior plans fixed the high-impact nested call rewriting and TS enum/rawProps issues. This plan addresses the tail of smaller issues across multiple subsystems.

Output: All 25 Phase 14 snapshots passing with zero regressions. Target: 74+/209 convergence tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-segment-codegen-batch-2/14-RESEARCH.md
@.planning/phases/14-segment-codegen-batch-2/14-01-SUMMARY.md
@.planning/phases/14-segment-codegen-batch-2/14-02-SUMMARY.md
@src/optimizer/segment-codegen.ts
@src/optimizer/jsx-transform.ts
@src/optimizer/signal-analysis.ts
@src/optimizer/bind-transform.ts
@src/optimizer/transform.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind merging, _fnSignal suppression, and targeted segment fixes</name>
  <files>src/optimizer/bind-transform.ts, src/optimizer/signal-analysis.ts, src/optimizer/jsx-transform.ts, src/optimizer/segment-codegen.ts</files>
  <read_first>
    - src/optimizer/bind-transform.ts (full file -- understand how bind:value generates event entries)
    - src/optimizer/signal-analysis.ts (full file -- understand _fnSignal wrapping logic, find where component vs HTML distinction could be added)
    - src/optimizer/jsx-transform.ts (search for "spread" and "passive" to understand current handling)
    - tests/optimizer/snapshots/should_merge_bind_value_and_on_input/ (expected output showing array merge pattern)
    - tests/optimizer/snapshots/should_mark_props_as_var_props_for_inner_cmp/ (expected output showing no _fnSignal on component props)
    - tests/optimizer/snapshots/hmr/ (expected output showing _useHmr and qrlDEV)
    - tests/optimizer/snapshots/moves_captures_when_possible/ (expected output showing unified q:ps)
  </read_first>
  <action>
1. **Bind merging (4 segments, 2-3 tests):** In `bind-transform.ts` or wherever bind:value/bind:checked event entries are generated, when a `q-e:input` (or `q-e:change`) entry already exists from an explicit `onInput$` handler, merge the bind-generated handler into an array with the explicit handler:
   - Expected: `"q-e:input": [q_explicitHandler, inlinedQrl(_val, "_val", [localValue])]`
   - Current: separate entries in varProps and constProps
   - The explicit handler QRL must come first in the array, then the bind-generated inlinedQrl

2. **_fnSignal suppression for component elements (3 segments, 3 tests):** In `signal-analysis.ts`, when determining whether to wrap a prop access with `_fnSignal`, check if the containing JSX element is a component (uppercase first letter tag). If so, do NOT wrap with `_fnSignal` -- pass the prop value directly. HTML elements (lowercase) continue to get `_fnSignal` wrapping.

3. **Remaining targeted fixes** based on research findings:
   - **should_ignore_passive_jsx_events_without_handlers:** Fix static children flag (flags should be 3, not 1) for div containing multiple static children
   - **hoisted_fn_signal_in_loop:** Fix _hf numbering order and flags (5 vs 3/7)
   - **example_spread_jsx:** Fix createElement vs _jsxSplit selection for spread patterns
   - **issue_7216_add_test:** Fix spread with interleaved event handlers ordering
   - **HMR (hmr test):** Add `_useHmr()` injection to segment bodies when dev mode + HMR enabled; add `qrlDEV()` to parent when dev mode. If this requires extensive infrastructure, document the gap and move on to other fixes.
   - **q:ps unified slot allocation (moves_captures_when_possible):** If feasible, implement cross-handler unified param slot allocation. If too complex for this plan, document the gap.

4. Prioritize fixes by impact (bind merging and _fnSignal fix more tests than HMR/q:ps). For complex single-test fixes (HMR, q:ps), implement if time allows but don't let them block convergence of simpler fixes.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts -t "should_merge_bind_value_and_on_input|should_merge_bind_checked_and_on_input|should_mark_props_as_var_props_for_inner_cmp|should_merge_attributes_with_spread_props" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "q-e:input\|merge\|array" src/optimizer/bind-transform.ts returns matches showing bind merging logic
    - grep -n "component\|uppercase\|HTML\|_fnSignal" src/optimizer/signal-analysis.ts returns matches showing component element detection
    - At least 3 of the 4 tested snapshots pass
  </acceptance_criteria>
  <done>Bind:value/bind:checked merged with explicit event handlers into arrays. _fnSignal suppressed for component-element props. Targeted segment fixes applied for remaining issues.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 14 convergence sweep and regression gate</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/transform.ts, src/optimizer/jsx-transform.ts</files>
  <read_first>
    - Convergence test output from full suite run (identify which of the 25 Phase 14 targets still fail)
    - For each still-failing snapshot: compare actual vs expected output to identify remaining issue
  </read_first>
  <action>
1. Run the full convergence suite and identify all Phase 14 target snapshots that still fail.

2. For each failing snapshot, debug by running the individual test and examining actual vs expected output. Classify remaining issues:
   - Quick fix (typo, missing import, wrong flag value) -> fix immediately
   - Medium fix (logic gap in existing code) -> fix if within plan budget
   - Complex fix (new feature/infrastructure needed) -> document as gap for Phase 15

3. Apply fixes iteratively, running regression checks after each batch. The hard gate is: all 49+ previously-passing tests must still pass after every change.

4. After all feasible fixes applied, run the final convergence gate:
   - `npx vitest run tests/optimizer/convergence.test.ts` must show 74+ passing (49 baseline + 25 Phase 14 targets, minus any documented gaps)
   - `npx vitest run` (full unit test suite) must show zero new failures
   - `npx tsc --noEmit` must be clean

5. Document any Phase 14 targets that could not be fixed in this plan as gaps for Phase 15 or gap closure.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/optimizer/convergence.test.ts reports at least 65 passed (49 baseline + 16+ new Phase 14 fixes)
    - All 49 previously-passing tests still pass (zero regressions)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Phase 14 convergence sweep complete. Maximum feasible Phase 14 snapshots passing. Zero regressions. Any remaining failures documented with root cause for gap closure or Phase 15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | jsx-transform.ts | accept | Internal build tool; all output validated by snapshot convergence tests |
</threat_model>

<verification>
- All 25 Phase 14 target snapshots tested individually
- Full convergence suite confirms 65+ passing with zero regressions
- TypeScript compilation clean
- Any unfixed gaps documented with classification
</verification>

<success_criteria>
- 65+/209 convergence tests passing (target: 74 if all 25 pass)
- Zero regressions in all previously-passing tests
- Clean TypeScript compilation
- Remaining gaps (if any) documented for Phase 15
</success_criteria>

<output>
After completion, create `.planning/phases/14-segment-codegen-batch-2/14-03-SUMMARY.md`
</output>
