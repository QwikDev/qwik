---
phase: 15-segment-codegen-batch-3
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/optimizer/jsx-transform.ts
  - src/optimizer/signal-analysis.ts
autonomous: true
requirements:
  - P15-03
must_haves:
  truths:
    - "Flags bitmask matches Rust optimizer output for all JSX elements"
    - "Store-field _wrapProp goes to constEntries for component elements"
    - "ObjectExpression values are NOT wrapped in _fnSignal (classified as var instead)"
    - "_fnSignal-wrapped props go to varEntries for component elements when they depend on iteration variables"
  artifacts:
    - path: "src/optimizer/jsx-transform.ts"
      provides: "Corrected flags bitmask and prop classification"
      contains: "computeFlags"
    - path: "src/optimizer/signal-analysis.ts"
      provides: "ObjectExpression exclusion from _fnSignal wrapping"
      contains: "ObjectExpression"
  key_links:
    - from: "src/optimizer/jsx-transform.ts"
      to: "computeFlags"
      via: "flags bit assignment matches Rust semantics"
      pattern: "computeFlags"
    - from: "src/optimizer/signal-analysis.ts"
      to: "src/optimizer/jsx-transform.ts"
      via: "signal analysis result drives prop placement"
      pattern: "analyzeSignal"
---

<objective>
Fix the flags bitmask computation and prop classification to match Rust optimizer semantics. This affects 10+ snapshots that have correct prop content but wrong flags values or wrong varProps/constProps placement.

Purpose: The flags bitmask and prop classification are the most pervasive remaining issues (10+ snapshots). Getting these right unblocks the majority of Phase 15.
Output: Correct flags on all JSX elements, correct prop placement for _wrapProp/_fnSignal on component elements, no _fnSignal on ObjectExpression values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-segment-codegen-batch-3/15-RESEARCH.md
@.planning/phases/15-segment-codegen-batch-3/15-01-SUMMARY.md
@src/optimizer/jsx-transform.ts
@src/optimizer/signal-analysis.ts
</context>

<interfaces>
<!-- Key functions the executor must understand and modify -->

From src/optimizer/jsx-transform.ts:
```typescript
export function computeFlags(
  hasVarProps: boolean,
  childrenType: 'none' | 'static' | 'dynamic',
  inLoop: boolean = false,
): number;

export function classifyProp(
  expr: any,
  importedNames: Set<string>,
  localNames?: Set<string>,
): 'const' | 'var';

function processProps(
  attributes: any[],
  source: string,
  importedNames: Set<string>,
  tagIsHtml: boolean,
  passiveEvents: Set<string>,
  signalHoister: SignalHoister,
  inLoop?: boolean,
  qrlsWithCaptures?: Set<string>,
  paramNames?: Set<string>,
): { varEntries, constEntries, key, hasVarProps, hasSpread, neededImports };
```

From src/optimizer/signal-analysis.ts:
```typescript
export function analyzeSignalExpression(
  node: any,
  source: string,
  importedNames: Set<string>,
): SignalAnalysisResult;
// Returns { type: 'none' | 'wrapProp' | 'fnSignal', ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix flags bitmask and prop classification for component elements</name>
  <files>src/optimizer/jsx-transform.ts, src/optimizer/signal-analysis.ts</files>
  <read_first>
    - src/optimizer/jsx-transform.ts (lines 265-320 -- computeFlags, lines 700-770 -- signal/wrapProp prop placement, lines 860-930 -- flags computation call site)
    - src/optimizer/signal-analysis.ts (full file -- 766 lines, focus on analyzeSignalExpression and ObjectExpression handling)
    - Run debug comparison for key failing snapshots to understand exact expected vs actual flags values:
      npx vitest run tests/optimizer/convergence.test.ts -t "should_not_wrap_ternary_function_operator_with_fn" 2>&1 | head -80
      npx vitest run tests/optimizer/convergence.test.ts -t "should_wrap_inner_inline_component_prop" 2>&1 | head -80
      npx vitest run tests/optimizer/convergence.test.ts -t "should_wrap_object_with_fn_signal" 2>&1 | head -80
      npx vitest run tests/optimizer/convergence.test.ts -t "should_wrap_store_expression" 2>&1 | head -80
      npx vitest run tests/optimizer/convergence.test.ts -t "should_move_props_related_to_iteration_variables_to_var_props" 2>&1 | head -80
  </read_first>
  <action>
This task requires careful investigation before making changes, as flags changes caused regressions in Phase 14. The approach:

**Step 1: Collect evidence from snapshot corpus.**
For each of these failing snapshots, compare the expected flags value against what our optimizer produces. Also note the varProps/constProps/children state. Build a truth table:
- should_not_wrap_ternary_function_operator_with_fn: expected flags, actual flags, varProps?, constProps?, children?
- should_wrap_inner_inline_component_prop: same
- should_wrap_object_with_fn_signal: same
- should_not_wrap_var_template_string: same
- should_move_props_related_to_iteration_variables_to_var_props: same

Also check the ALREADY PASSING snapshots to make sure any changes don't regress them. Run the full convergence suite to get baseline count (should be >= 57 from Plan 01).

**Step 2: Fix computeFlags based on evidence.**
The current implementation (bit 0 = no varProps, bit 1 = static children) may have the bits swapped or the semantics wrong. The research suggests:
- bit 0 (1) = children are static (null, string, no dynamic content)  
- bit 1 (2) = no var props (all immutable)
- bit 2 (4) = loop context

BUT Phase 14 found that simply swapping bits caused regressions. The issue may be more nuanced -- "has varProps" might not be the right signal. Study the failing AND passing snapshot flags to determine the exact rule.

If the evidence confirms bits are swapped, swap them. If the evidence shows a different pattern (e.g., "has signal-reactive props" vs "has ANY varProps"), implement that pattern.

CRITICAL: After ANY change to computeFlags, run the FULL convergence suite immediately. If regressions appear, analyze which snapshots regressed and why. Do NOT proceed if net regressions occur.

**Step 3: Fix prop classification for component elements.**

a) Store-field _wrapProp on component elements: In processProps, when `tagIsHtml === false` and a prop's signal analysis returns `wrapProp` (2-arg store-field form), place it in constEntries instead of varEntries. Look at the signal analysis result handling (~line 730-750) and add a condition: if `!tagIsHtml && signalResult.type === 'wrapProp'`, push to constEntries.

b) _fnSignal placement for component elements with iteration vars: For `should_move_props_related_to_iteration_variables_to_var_props`, the expected output shows `_fnSignal` in varProps for a component element. This contradicts the general rule that _fnSignal goes to constEntries. Check if this is specific to props that depend on iteration variables (captured in paramNames). If so, add a condition: if the _fnSignal accesses a paramName variable, place in varEntries.

c) ObjectExpression _fnSignal suppression: In signal-analysis.ts, find where ObjectExpression is handled in analyzeSignalExpression. When the node is an ObjectExpression, return `{ type: 'none' }` -- don't wrap objects in _fnSignal. This fixes should_wrap_store_expression where `class={{ ... }}` should be a var prop, not _fnSignal-wrapped.

**Step 4: Run full convergence and fix regressions.**
After all changes, run full suite. Target: net positive improvement. If specific regressions appear, analyze and fix or revert the specific change that caused them.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run tests/optimizer/convergence.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/optimizer/convergence.test.ts shows >= 65 passing (Plan 01 baseline + ~8 new from flags/classification fixes)
    - Zero net regressions from Plan 01 baseline
    - grep "ObjectExpression" in signal-analysis.ts shows early return for object literals
    - should_not_wrap_ternary_function_operator_with_fn, should_wrap_inner_inline_component_prop, should_wrap_object_with_fn_signal show improved output
  </acceptance_criteria>
  <done>Flags bitmask matches Rust semantics for all tested snapshots. Prop classification correct for component elements. ObjectExpression not wrapped in _fnSignal. Net positive convergence improvement with zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AST node -> classification | Signal analysis classifies untrusted AST patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | T (Tampering) | computeFlags | accept | Flags are internal optimization hints; incorrect flags cause rendering bugs not security issues |
| T-15-04 | I (Info Disclosure) | signal-analysis | accept | No user data flows through signal classification |
</threat_model>

<verification>
1. `npx vitest run tests/optimizer/convergence.test.ts` -- count >= 65 passing, zero regressions
2. `npx vitest run` -- all unit tests still green
3. Spot-check flags values in output for key snapshots
</verification>

<success_criteria>
- Flags bitmask correct for all Phase 15 target snapshots that had flags mismatches
- Store-field _wrapProp in constEntries for component elements
- ObjectExpression values classified as var (no _fnSignal wrapping)
- Net convergence improvement of 8+ new passes from Plan 01 baseline
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-segment-codegen-batch-3/15-02-SUMMARY.md`
</output>
