---
phase: 16-final-convergence
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/optimizer/rewrite-parent.ts
  - src/optimizer/jsx-transform.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Parent module output contains only rewritten imports, no leaked user imports"
    - "Event handler props (q-d:*, q-w:*) placed in constProps arg of _jsxSorted, not mutableProps"
  artifacts:
    - path: "src/optimizer/rewrite-parent.ts"
      provides: "Correct user import stripping in parent output"
    - path: "src/optimizer/jsx-transform.ts"
      provides: "Correct event prop argument position in _jsxSorted"
  key_links:
    - from: "src/optimizer/rewrite-parent.ts"
      to: "parent module output"
      via: "import cleanup/replacement"
      pattern: "import.*from"
    - from: "src/optimizer/jsx-transform.ts"
      to: "_jsxSorted call arguments"
      via: "prop classification"
      pattern: "_jsxSorted"
---

<objective>
Fix Pattern 3 (user imports leaking into parent output) and Pattern 6 (event handler prop
argument position in _jsxSorted). These patterns together account for an estimated 30-45
of the remaining failing snapshots.

Purpose: Correct parent module import output and JSX event prop classification.
Output: Clean parent imports + correct _jsxSorted argument layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-final-convergence/16-RESEARCH.md
@.planning/phases/16-final-convergence/16-01-SUMMARY.md
@src/optimizer/rewrite-parent.ts
@src/optimizer/jsx-transform.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip leaked user imports from parent module output</name>
  <files>src/optimizer/rewrite-parent.ts</files>
  <read_first>
    - src/optimizer/rewrite-parent.ts (full file -- understand import rewriting pipeline)
    - src/optimizer/rewrite-imports.ts (understand import source rewriting)
    - Run debug-diff on example_build_server to see the leaked imports pattern
  </read_first>
  <action>
The parent module retains original user-level imports alongside the optimizer-generated imports.
For example, `import { component$, $, useSignal } from '@qwik.dev/core'` remains when it should
be replaced by `import { componentQrl, _noopQrl, ... } from '@qwik.dev/core'`.

1. Debug the issue by running: set TARGET='example_build_server' in debug-diff.test.ts and
   examine the parent diff to identify which imports are leaking.
2. In rewrite-parent.ts, trace the import rewriting logic. Find where original import
   declarations are supposed to be replaced/removed.
3. The fix likely involves ensuring that when the optimizer generates replacement imports,
   the original import statements for Qwik packages are fully removed (not just augmented).
4. Check all Qwik package prefixes (QWIK_CORE_PREFIXES) to ensure imports from any of them
   are properly replaced.
5. Be careful not to strip non-Qwik user imports (e.g., `import { useState } from 'react'`
   should remain).

Target snapshots: example_build_server, example_drop_side_effects, ternary_prop, and other
parent-only failures from the breakdown.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
Parent-only failures decrease. Pass count increases by 10+ from import cleanup.
No regressions in previously passing tests.
  </acceptance_criteria>
  <done>Parent module output contains only rewritten imports for Qwik packages. User imports for non-Qwik packages preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix event handler prop classification in _jsxSorted arguments</name>
  <files>src/optimizer/jsx-transform.ts</files>
  <read_first>
    - src/optimizer/jsx-transform.ts (search for _jsxSorted, constProps, mutableProps, and the prop classification logic)
    - Run debug-diff on should_convert_jsx_events to see the arg position mismatch
  </read_first>
  <action>
The SWC optimizer places all event attrs (q-e:*, q-d:*, q-w:*) in the constProps argument
(arg 3) of _jsxSorted(), but our optimizer puts q-d:* and q-w:* in mutableProps (arg 1).

1. In jsx-transform.ts, find the prop classification logic that determines which props go
   to constProps vs mutableProps in the _jsxSorted call.
2. Look for where q-e:* props are classified as const and ensure q-d:* (default prevent)
   and q-w:* (stop propagation) follow the same classification.
3. The fix should be in the prop classification switch/if-else that handles event-related
   attribute prefixes.
4. Verify with should_convert_jsx_events and should_convert_passive_jsx_events snapshots.

Expected output:
- All q-e:*, q-d:*, q-w:* attrs appear in constProps (arg 3) of _jsxSorted
- mutableProps (arg 1) contains only genuinely mutable props
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
Event handler snapshots (should_convert_jsx_events, should_convert_passive_jsx_events) pass or get closer.
Pass count increases from combined Pattern 3+6 fixes.
  </acceptance_criteria>
  <done>Event handler props correctly classified in _jsxSorted constProps. Pass count increases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | T (Tampering) | import stripping | accept | Build-time code generation, not user-facing runtime |
</threat_model>

<verification>
Run full convergence suite: `npx vitest run tests/optimizer/convergence.test.ts`
Run unit tests: `npx vitest run --exclude '**/convergence*' --exclude '**/snapshot-batch*' --exclude '**/failure-families*'`
</verification>

<success_criteria>
- Convergence test pass count >= 100 (up from ~85 after Plan 01)
- Zero regressions in previously passing tests
- Zero regressions in unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-final-convergence/16-02-SUMMARY.md`
</output>
