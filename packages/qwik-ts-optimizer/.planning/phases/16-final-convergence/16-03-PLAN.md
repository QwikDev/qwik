---
phase: 16-final-convergence
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/optimizer/segment-codegen.ts
  - src/optimizer/transform.ts
  - src/optimizer/strip-exports.ts
  - src/optimizer/dev-mode.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Segment bodies have dead isServer/isBrowser branches eliminated based on build target"
    - "Dev mode segments emit qrlDEV() with location metadata instead of qrl()"
    - "Unused variable bindings stripped from segment bodies (side-effect simplification)"
  artifacts:
    - path: "src/optimizer/segment-codegen.ts"
      provides: "Dead code elimination + side-effect simplification in segment bodies"
    - path: "src/optimizer/transform.ts"
      provides: "Dev mode flag threading to segment codegen"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "src/optimizer/segment-codegen.ts"
      via: "isServer/mode options threading"
      pattern: "isServer|qrlDEV|mode"
---

<objective>
Fix Patterns 4, 5, and 7: (4) dead code elimination for isServer/isBrowser in segment
bodies, (5) dev mode qrlDEV() format emission in segments, and (7) unused variable binding
stripping in segment bodies. These three patterns together account for an estimated 20-40
of the remaining failing snapshots.

Purpose: Complete segment body transformation pipeline with dead code, dev mode, and simplification passes.
Output: Segments with correct dead code elimination, dev mode QRL format, and simplified side-effect statements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-final-convergence/16-RESEARCH.md
@.planning/phases/16-final-convergence/16-01-SUMMARY.md
@.planning/phases/16-final-convergence/16-02-SUMMARY.md
@src/optimizer/segment-codegen.ts
@src/optimizer/transform.ts
@src/optimizer/strip-exports.ts
@src/optimizer/dev-mode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dead code elimination and side-effect simplification in segment bodies</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/strip-exports.ts</files>
  <read_first>
    - src/optimizer/segment-codegen.ts (search for "dead code" and "isServer" to see existing handling)
    - src/optimizer/strip-exports.ts (full file -- understand existing dead code logic)
    - src/optimizer/strip-ctx.ts (may have isServer/isBrowser stripping logic)
    - Run debug-diff on example_build_server and example_strip_server_code to see dead branch patterns
  </read_first>
  <action>
Two issues to fix:

**Pattern 4 -- Dead code elimination:**
Segment bodies retain `if (isServer) { ... }` / `if (isBrowser) { ... }` blocks instead of
eliminating dead branches based on the `isServer` build option.

1. Check if strip-ctx.ts or strip-exports.ts already has dead code elimination logic for
   isServer/isBrowser.
2. In segment-codegen.ts, after body text construction but before final assembly, apply the
   same dead code elimination that the parent module uses.
3. Thread the `isServer` option from transform options through to segment codegen.
4. Remove imports that are only used in eliminated dead branches.

**Pattern 7 -- Side-effect simplification:**
Segment bodies retain `const hola = ident1.no;` when the binding `hola` is unused; should
become just `ident1.no;` (expression statement).

1. After body transforms, scan for variable declarations where the binding name is not
   referenced elsewhere in the body.
2. Convert `const x = expr;` to `expr;` when x is unused.
3. Be conservative -- only apply to simple expression statements (member access, function calls),
   not complex expressions with side effects that might be affected by removing the binding.

Target snapshots: example_build_server, example_strip_server_code, example_10, example_exports
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
Dead code and side-effect snapshots pass or get closer. Pass count increases by 5+.
No regressions in previously passing tests.
  </acceptance_criteria>
  <done>Dead code elimination and side-effect simplification applied in segment bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Emit qrlDEV() format in dev mode segments</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/transform.ts, src/optimizer/dev-mode.ts</files>
  <read_first>
    - src/optimizer/dev-mode.ts (full file -- understand dev mode QRL format)
    - src/optimizer/segment-codegen.ts (search for "qrl(" to find where QRL declarations are emitted in segments)
    - src/optimizer/rewrite-calls.ts (search for "qrlDEV" to see format)
    - Run debug-diff on example_dev_mode and example_noop_dev_mode to see expected qrlDEV format
  </read_first>
  <action>
Dev mode segments should emit `qrlDEV()` with location metadata instead of plain `qrl()`.

1. Check how the parent module switches between qrl() and qrlDEV() based on mode ('lib' or
   explicit dev mode flag).
2. In segment-codegen.ts, where `const q_symbolName = qrl(...)` declarations are built,
   switch to `qrlDEV()` format when in dev mode:
   `const q_name = qrlDEV(() => import("./file"), "name", { file: "path", lo: N, hi: N, displayName: "Name" })`
3. Thread the mode/dev flag from transform options through to the segment codegen QRL builder.
4. The location metadata (file, lo, hi, displayName) should be available from the extraction
   result metadata.
5. Also ensure the qrlDEV import is added to segment imports when in dev mode.

Target snapshots: example_dev_mode, example_dev_mode_inlined, example_noop_dev_mode,
example_jsx_keyed_dev, example_drop_side_effects (dev mode aspects)
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
Dev mode snapshots (example_dev_mode, example_noop_dev_mode) pass or get significantly closer.
Pass count increases by 5+.
  </acceptance_criteria>
  <done>Dev mode segments emit qrlDEV() with correct location metadata. Pass count increases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | I (Info Disclosure) | dev-mode location metadata | accept | File paths in dev mode are expected behavior, stripped in prod |
</threat_model>

<verification>
Run full convergence suite: `npx vitest run tests/optimizer/convergence.test.ts`
Run unit tests: `npx vitest run --exclude '**/convergence*' --exclude '**/snapshot-batch*' --exclude '**/failure-families*'`
</verification>

<success_criteria>
- Convergence test pass count >= 115 (up from ~100 after Plan 02)
- Zero regressions in previously passing tests
- Zero regressions in unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-final-convergence/16-03-SUMMARY.md`
</output>
