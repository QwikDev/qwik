---
phase: 16-final-convergence
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - src/optimizer/segment-codegen.ts
  - src/optimizer/variable-migration.ts
  - src/optimizer/transform.ts
  - tests/optimizer/extract.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Segment import paths use correct relative resolution (./test not ./project/test)"
    - "_auto_ prefix re-exports use correct import alias pairing"
    - "Separator comments (//) present between import and body sections in all segments"
    - "Captured variables not double-imported in segments"
    - "QRL declaration ordering matches Rust optimizer output"
    - "tsc --noEmit reports zero errors"
    - "All unit tests pass (zero failures)"
  artifacts:
    - path: "src/optimizer/segment-codegen.ts"
      provides: "Fixed import paths, separators, capture imports, QRL ordering"
    - path: "src/optimizer/variable-migration.ts"
      provides: "Fixed _auto_ import path normalization"
    - path: "src/optimizer/transform.ts"
      provides: "Fixed type errors (walker signatures, ModuleExportName, assertions)"
    - path: "tests/optimizer/extract.test.ts"
      provides: "Fixed missing isComponentEvent property in test fixtures"
  key_links:
    - from: "src/optimizer/segment-codegen.ts"
      to: "segment output"
      via: "import resolution + separator insertion"
      pattern: "import.*from"
---

<objective>
Fix remaining lower-blast-radius patterns (8-12) plus all tsc type errors and unit test
failures. These are the long-tail edge cases: _auto_ import paths, separator comments,
capture variable double-imports, QRL declaration ordering, and flags bitmask edge cases.
Also fix 12 tsc errors and 3 failing unit tests.

Purpose: Clean up all remaining systemic issues and bring the codebase to a type-clean, unit-test-green state.
Output: All type errors resolved, all unit tests passing, many remaining edge case snapshots fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-final-convergence/16-RESEARCH.md
@.planning/phases/16-final-convergence/16-03-SUMMARY.md
@src/optimizer/segment-codegen.ts
@src/optimizer/variable-migration.ts
@src/optimizer/transform.ts
@tests/optimizer/extract.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining segment edge cases (Patterns 8-12)</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/variable-migration.ts</files>
  <read_first>
    - src/optimizer/segment-codegen.ts (search for "_auto_", "//", separator, import path resolution)
    - src/optimizer/variable-migration.ts (understand _auto_ prefix and import path handling)
    - Run debug-diff on example_exports and should_not_auto_export_var_shadowed_in_catch to see _auto_ path issues
    - Run debug-diff on should_preserve_non_ident_explicit_captures to see separator + double-import issues
  </read_first>
  <action>
Fix the following remaining patterns:

**Pattern 8 -- _auto_ import path issues:**
Segments import from "./project/test" instead of "./test". Fix the import source path
computation in segment codegen to use the same relative path the parent module uses.
Check variable-migration.ts for the path normalization that creates _auto_ re-exports.

**Pattern 9 -- Missing separator comments:**
Some segments are missing the `//` separator between imports and body code. Ensure the
separator insertion logic in generateSegmentCode handles all edge cases (e.g., when there
are no QRL declarations, when there are only imports).

**Pattern 10 -- Captured variables double-imported:**
Segments that use _captures[N] still have direct imports for the captured variables.
After _captures unpacking is injected, remove any import statements for variables that
appear in the captureNames array.

**Pattern 11 -- QRL declaration ordering:**
QRL `const q_xxx = ...` declarations in segments should match the order the Rust optimizer
emits them. Check if this is declaration order from the source or alphabetical.

**Pattern 12 -- Flags bitmask edge cases:**
Scattered edge cases in static/dynamic child classification for _jsxSorted FLAGS argument.
Fix any remaining misclassifications found in failing snapshots.

Approach: After fixing each pattern, run convergence to measure impact. Commit each fix
separately for easy rollback if regressions occur.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
Pass count increases by 10+ from edge case fixes. Separator, _auto_, and capture import issues resolved.
  </acceptance_criteria>
  <done>Remaining edge case patterns resolved. Separator comments, _auto_ paths, capture imports, QRL ordering all correct.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all tsc type errors and failing unit tests</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/transform.ts, tests/optimizer/extract.test.ts</files>
  <read_first>
    - Run `npx tsc --noEmit 2>&1` to get current error list
    - src/optimizer/transform.ts lines 571, 590, 1196, 1214, 1218 (type error locations)
    - src/optimizer/segment-codegen.ts lines 644, 735 (type error locations)
    - tests/optimizer/extract.test.ts lines 198, 239 (missing isComponentEvent)
    - Run `npx vitest run --exclude '**/convergence*' --exclude '**/snapshot-batch*' --exclude '**/failure-families*' 2>&1 | grep "FAIL"` to see failing unit tests
  </read_first>
  <action>
Fix all 12 tsc errors:

**segment-codegen.ts (3 errors):**
1. Line 644: Add null check for `nestedCallSites` (possibly undefined)
2. Line 735: Change `findLastIndex` -- either add 'es2023' to tsconfig lib or use a manual
   reverse loop. Prefer adding es2023 to lib in tsconfig.json since findLastIndex is standard.
3. Line 735: Add type annotation for parameter `p` (implicit any)

**transform.ts (7 errors):**
1. Lines 571, 590: Fix walker enter/leave type signatures. The oxc-walker callback params are
   `(node, parent, key, index)` not `(node, key, index)`. Update the parameter types to match
   the WalkerEnter/WalkerLeave type from oxc-walker.
2. Line 1196: Narrow ModuleExportName type before accessing .name -- check `node.type === 'Identifier'`
3. Line 1214: Use `attributes` instead of `assertions` (ImportDeclaration property name changed in ESTree spec)
4. Line 1218: Narrow ImportAttributeKey type before accessing .name/.value

**extract.test.ts (2 errors):**
Add `isComponentEvent: false` (or appropriate value) to the two test fixture objects at lines 198 and 239.

**Failing unit tests (3):**
1. rewrite-parent.test.ts bare `$()` test -- investigate and fix
2. transform.test.ts BIND-01, BIND-02 bind desugaring tests -- investigate and fix
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1; npx vitest run --exclude '**/convergence*' --exclude '**/snapshot-batch*' --exclude '**/failure-families*' 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
`npx tsc --noEmit` reports zero errors.
All unit tests pass (465/465 or better, zero failures).
  </acceptance_criteria>
  <done>Zero tsc errors. Zero unit test failures. Codebase is type-clean and unit-test-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | T (Tampering) | type safety | mitigate | tsc --noEmit enforces type correctness at build time |
</threat_model>

<verification>
Run `npx tsc --noEmit` -- zero errors.
Run `npx vitest run` -- all tests pass (unit + convergence improvements from earlier plans).
</verification>

<success_criteria>
- Zero tsc errors
- Zero unit test failures
- Convergence pass count >= 125 (up from ~115 after Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/16-final-convergence/16-04-SUMMARY.md`
</output>
