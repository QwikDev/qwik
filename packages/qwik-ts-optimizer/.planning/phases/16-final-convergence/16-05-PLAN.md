---
phase: 16-final-convergence
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - src/optimizer/segment-codegen.ts
  - src/optimizer/rewrite-parent.ts
  - src/optimizer/jsx-transform.ts
  - src/optimizer/transform.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "convergence.test.ts reports 209/209 (or 208/208 excluding no-input relative_paths)"
    - "All unit tests pass with zero failures"
    - "tsc --noEmit reports zero errors"
  artifacts:
    - path: "tests/optimizer/convergence.test.ts"
      provides: "Full convergence validation"
  key_links:
    - from: "convergence.test.ts"
      to: "all optimizer modules"
      via: "snapshot comparison"
      pattern: "209.*passed|208.*passed"
---

<objective>
Final convergence sweep: diagnose and fix all remaining failing snapshots to achieve the
209/209 (or 208/208) target. This plan handles the long tail of unique, one-off issues
that were not covered by the systemic pattern fixes in Plans 01-04.

Purpose: Achieve full convergence -- every snapshot test passes.
Output: 209/209 (or 208/208) convergence, zero tsc errors, zero unit test failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-final-convergence/16-RESEARCH.md
@.planning/phases/16-final-convergence/16-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and categorize all remaining failures</name>
  <files>tests/optimizer/debug-diff.test.ts</files>
  <read_first>
    - Run `npx vitest run tests/optimizer/convergence.test.ts --reporter=verbose 2>&1 | grep "FAIL"` to get current failure list
    - tests/optimizer/debug-diff.test.ts (understand how to debug individual snapshots)
    - tests/optimizer/convergence-breakdown.test.ts (understand categorization)
  </read_first>
  <action>
After Plans 01-04 have landed, the remaining failures will be a mix of unique edge cases.

1. Run the full convergence suite and collect the list of all still-failing snapshot names.
2. For each failing snapshot (or batches of similar ones), use debug-diff.test.ts to examine
   the actual vs expected diff.
3. Categorize remaining failures into groups:
   - Off-by-1-stmt: Nearly matching, one statement different
   - Import ordering: Only import order differs (may need AST comparison fix)
   - Unique codegen issues: One-off problems in specific snapshots
   - Options mismatch: Snapshot options (mode, transpileJsx, etc.) may be wrong
4. For each category, identify the minimal fix needed.
5. For snapshot-options mismatches, update tests/optimizer/snapshot-options.ts.
6. Apply fixes from highest-impact categories first.

This is an investigative task -- the exact fixes cannot be predetermined since they depend
on what remains after Plans 01-04. Allocate time for diagnosis (reading diffs) and targeted
fixes.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"</automated>
  </verify>
  <acceptance_criteria>
All remaining failures diagnosed and categorized. At least half of remaining failures fixed.
  </acceptance_criteria>
  <done>Remaining failures categorized. Targeted fixes applied. Pass count increases significantly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix final remaining failures and validate 209/209 convergence</name>
  <files>src/optimizer/segment-codegen.ts, src/optimizer/rewrite-parent.ts, src/optimizer/jsx-transform.ts, src/optimizer/transform.ts</files>
  <read_first>
    - Run `npx vitest run tests/optimizer/convergence.test.ts --reporter=verbose 2>&1 | grep "FAIL"` for remaining failures
    - For each remaining failure, run debug-diff to understand the exact diff
  </read_first>
  <action>
Fix all remaining failures identified in Task 1. This task is the final push to 209/209.

For each remaining failure:
1. Read the debug-diff output
2. Identify the exact code path producing wrong output
3. Apply the minimal, targeted fix
4. Verify no regressions after each fix

After all fixes:
1. Run full convergence: `npx vitest run tests/optimizer/convergence.test.ts`
   -- Must show 209 passed (or 208 if relative_paths excluded) and 0 failed
2. Run full unit tests: `npx vitest run`
   -- Must show 0 failed
3. Run type check: `npx tsc --noEmit`
   -- Must show 0 errors

If any snapshot proves impossible to fix without causing regressions, document it and
explain why. The target is 209/209 but 208/208 (excluding no-input relative_paths) is
the minimum acceptable gate.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep -E "Tests.*failed.*passed"; npx vitest run 2>&1 | grep -E "Tests.*failed.*passed"; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
convergence.test.ts: 209 passed, 0 failed (or 208 passed with 1 skipped for no-input).
Full vitest run: 0 failed.
tsc --noEmit: 0 errors.
  </acceptance_criteria>
  <done>209/209 convergence achieved (or 208/208 excluding no-input). Zero tsc errors. Zero unit test failures. Phase 16 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | T (Tampering) | full convergence | mitigate | 209/209 snapshot comparison validates output matches Rust optimizer exactly |
</threat_model>

<verification>
Full convergence gate:
1. `npx vitest run tests/optimizer/convergence.test.ts` -- 209/209 (or 208/208)
2. `npx vitest run` -- all tests pass
3. `npx tsc --noEmit` -- zero errors
</verification>

<success_criteria>
- convergence.test.ts: 209/209 passed (or 208/208 excluding relative_paths)
- Full test suite: zero failures
- tsc --noEmit: zero errors
- Phase 16 final convergence achieved
</success_criteria>

<output>
After completion, create `.planning/phases/16-final-convergence/16-05-SUMMARY.md`
</output>
