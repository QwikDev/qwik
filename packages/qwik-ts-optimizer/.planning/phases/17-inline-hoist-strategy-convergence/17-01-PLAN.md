---
phase: 17-inline-hoist-strategy-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/optimizer/rewrite-parent.ts
autonomous: true
requirements:
  - IHS-01
  - IHS-02
  - IHS-03

must_haves:
  truths:
    - "Synthetic imports in parent module output use Map insertion order (discovery order), not alphabetical sort"
    - "All 73 previously-passing convergence tests still pass after import reordering"
    - "Import order in inline/hoist snapshot output matches SWC expected output"
  artifacts:
    - path: "src/optimizer/rewrite-parent.ts"
      provides: "Import ordering fix -- Map insertion order instead of alphabetical sort"
      contains: "Array.from(neededImports.entries())"
  key_links:
    - from: "src/optimizer/rewrite-parent.ts"
      to: "neededImports Map"
      via: "Array.from(neededImports.entries()) without .sort()"
      pattern: "Array\\.from\\(neededImports\\.entries\\(\\)\\)"
---

<objective>
Fix synthetic import ordering in parent module output to use Map insertion order (matching SWC discovery order) instead of alphabetical sorting.

Purpose: Import ordering is the widest-blast-radius root cause -- every single inline/hoist snapshot (36 tests) fails partly due to alphabetical import sorting. SWC uses Vec insertion order (discovery order during AST traversal). Our neededImports Map already preserves insertion order; we just need to stop sorting it.

Output: Updated rewrite-parent.ts with import ordering fix, verified by zero regressions in 73 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-inline-hoist-strategy-convergence/17-RESEARCH.md

<interfaces>
From src/optimizer/rewrite-parent.ts (line 1540):
```typescript
// CURRENT (alphabetical sort -- WRONG for AST comparison):
const sortedImports = Array.from(neededImports.entries()).sort((a, b) =>
  a[0].localeCompare(b[0]),
);
// neededImports is a Map<string, string> -- Map preserves insertion order
```

From swc-reference-only/collector.rs (line 46):
```rust
pub synthetic: Vec<(Id, Import)>  // Vec preserves insertion order
```

From swc-reference-only/transform.rs (lines 3411-3418):
```rust
// synthetic imports emitted in their Vec order (insertion order)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove alphabetical import sort and use Map insertion order</name>
  <files>src/optimizer/rewrite-parent.ts</files>
  <read_first>
    - src/optimizer/rewrite-parent.ts (lines 1536-1546 -- the import sorting code)
    - .planning/phases/17-inline-hoist-strategy-convergence/17-RESEARCH.md (Root Cause 1)
  </read_first>
  <action>
In `src/optimizer/rewrite-parent.ts`, at approximately line 1540, change the import ordering from alphabetical sort to plain Map insertion order:

BEFORE (line 1540-1542):
```typescript
const sortedImports = Array.from(neededImports.entries()).sort((a, b) =>
  a[0].localeCompare(b[0]),
);
```

AFTER:
```typescript
const sortedImports = Array.from(neededImports.entries());
```

This single change removes the `.sort((a, b) => a[0].localeCompare(b[0]))` call. The `neededImports` Map already preserves insertion order (JavaScript Map guarantee), which matches SWC's Vec insertion order for synthetic imports.

Do NOT rename the variable `sortedImports` -- it's used downstream at line 1544 and renaming would be a larger diff for no benefit.

IMPORTANT: After making this change, also update the comment at line 1536-1539 to reflect that imports are no longer sorted:

BEFORE:
```
// Step 5d: Build import statements (deferred from Step 5 to include
// additional imports discovered during Step 5c body transformation)
```

AFTER:
```
// Step 5d: Build import statements (deferred from Step 5 to include
// additional imports discovered during Step 5c body transformation)
// NOTE: Import order follows Map insertion order (discovery order),
// matching SWC's Vec<(Id, Import)> insertion ordering.
```
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run tests/optimizer/convergence.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/optimizer/rewrite-parent.ts` does NOT contain `.sort((a, b) => a[0].localeCompare(b[0]))` on the neededImports line
    - `src/optimizer/rewrite-parent.ts` contains `const sortedImports = Array.from(neededImports.entries());` (no sort call)
    - `src/optimizer/rewrite-parent.ts` contains `// matching SWC's Vec<(Id, Import)> insertion ordering`
    - All 73 previously-passing convergence tests still pass (zero regressions)
    - `npx vitest run tests/optimizer/convergence.test.ts` reports `73 passed` (not fewer)
  </acceptance_criteria>
  <done>Import ordering uses Map insertion order; 73 previously-passing tests have zero regressions</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify no unit test regressions</name>
  <files>src/optimizer/rewrite-parent.ts</files>
  <read_first>
    - src/optimizer/rewrite-parent.ts (verify the sort removal from Task 1 is in place)
  </read_first>
  <action>
Run the full test suite (not just convergence tests) to verify the import ordering change causes no regressions in unit tests or other test files.

Execute: `npx vitest run`

If any previously-passing test now fails, investigate:
1. Check if the failure is in a non-inline/hoist test (regression from import reorder)
2. If so, the import discovery order for that test's neededImports Map may differ from what its snapshot expects
3. Document which tests regressed and what the import order difference is
4. Do NOT revert the change -- flag the regression for investigation

If all non-convergence tests pass, the import reorder is safe.

Also run `npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>cd /Users/jackshelton/dev/open-source/qwik-optimizer-ts && npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` does not show any new failures in non-convergence test files
    - `npx tsc --noEmit` exits with 0 errors
    - No unit test regressions from the import ordering change
  </acceptance_criteria>
  <done>Full test suite passes with no regressions from import reorder; TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source input -> AST parse | Untrusted user source code parsed by oxc-parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T (Tampering) | Import ordering | accept | Import ordering is deterministic (Map insertion order); no user-controlled input affects symbol discovery sequence. Low risk -- ordering difference is cosmetic for runtime correctness. |
</threat_model>

<verification>
1. `npx vitest run tests/optimizer/convergence.test.ts` -- 73+ passed (zero regressions)
2. `npx vitest run` -- all unit tests pass
3. `npx tsc --noEmit` -- zero type errors
4. `grep -n "localeCompare" src/optimizer/rewrite-parent.ts` -- should NOT match on the neededImports sort line
</verification>

<success_criteria>
- Import ordering changed from alphabetical to insertion order
- Zero regressions in 73 previously-passing convergence tests
- Zero regressions in unit tests
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/17-inline-hoist-strategy-convergence/17-01-SUMMARY.md`
</output>
