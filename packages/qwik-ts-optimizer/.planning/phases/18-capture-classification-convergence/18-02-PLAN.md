---
phase: 18-capture-classification-convergence
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/optimizer/transform.ts
  - src/optimizer/segment-codegen.ts
autonomous: true
requirements:
  - CAP-03

must_haves:
  truths:
    - "Segment metadata (captures boolean, function signatures embedding paramNames, _captures unpacking embedding captureNames) matches snapshot expected output for all capture-related tests"
    - "All 73 previously-passing convergence tests still pass"
    - "Full unit test suite passes with zero regressions"
  artifacts:
    - path: "src/optimizer/transform.ts"
      provides: "Correct captures boolean and captureNames/paramNames arrays in extraction metadata"
    - path: "src/optimizer/segment-codegen.ts"
      provides: "Segment code correctly embeds paramNames in function signature and captureNames in _captures unpacking"
  key_links:
    - from: "src/optimizer/transform.ts"
      to: "tests/optimizer/convergence.test.ts"
      via: "extraction metadata flows through segment codegen to convergence comparison"
      pattern: "captures.*captureNames.*paramNames"
---

<objective>
Validate and fix remaining capture metadata mismatches after Plan 01's sort-order and .w() delivery fixes. Run full convergence suite, identify any remaining capture-related failures, apply targeted fixes, and confirm zero regressions.

Purpose: CAP-03 requires segment metadata (captures, captureNames, paramNames) to match snapshot expected metadata exactly. Plan 01 fixes the core sort-order and delivery mechanism issues; this plan catches any remaining edge cases and validates the full convergence count.

Output: All capture-related convergence tests passing. Convergence count reported for phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-capture-classification-convergence/18-RESEARCH.md
@.planning/phases/18-capture-classification-convergence/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix remaining capture metadata mismatches</name>
  <files>src/optimizer/transform.ts, src/optimizer/segment-codegen.ts</files>
  <read_first>
    - src/optimizer/transform.ts (current state after Plan 01 modifications)
    - src/optimizer/segment-codegen.ts (current state after Plan 01 modifications)
    - .planning/phases/18-capture-classification-convergence/18-01-SUMMARY.md (what Plan 01 changed and what still fails)
  </read_first>
  <action>
**Step 1: Run full convergence suite and capture results**
Run `npx vitest run tests/optimizer/convergence.test.ts` and record:
- Total passing count (must be >= 73)
- List of still-failing capture-related tests (grep for "capture", "multi_capture", "moves_captures", "loop" in test names)

**Step 2: For each remaining capture-related failure, diagnose the specific mismatch**
Write a quick diagnostic script or modify debug-diff.test.ts TARGET to inspect actual vs expected for each failing test. Focus on:
- Function signature mismatches (paramNames wrong order or missing padding)
- _captures unpacking line differences (captureNames wrong order or count)
- .w() wrapping format differences (indentation, newlines, variable order)
- captures boolean metadata (true when should be false, or vice versa)

**Step 3: Apply targeted fixes**
For each diagnosed issue, apply the minimal fix. Common patterns:
- If paramNames order is wrong: verify alphabetical sort was applied correctly
- If _captures unpacking order is wrong: verify captureNames is sorted alphabetically
- If .w() format doesn't match: adjust whitespace/newline formatting in rewrite-parent.ts or segment-codegen.ts
- If captures boolean is wrong: verify the partitioning logic correctly sets captures = crossScopeCaptures.length > 0

**Step 4: Run full test suite to confirm zero regressions**
Run `npx vitest run` (all tests) and verify:
- All unit tests pass
- Convergence count >= 73 (no regressions)
- TypeScript compiles cleanly: `npx tsc --noEmit`

**Step 5: Report convergence count**
Record the final convergence pass count for the phase summary. This count will be the input for Phase 21's convergence gate.

NOTE: Some capture-related tests may also have JSX or migration issues (Phase 19/20). If a test fails for reasons clearly unrelated to capture classification (e.g., _jsxSorted flags, variable migration), document it as a known cross-phase issue rather than trying to fix it here.
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/convergence.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/optimizer/convergence.test.ts 2>&1 | grep "passed"` shows >= 73 passed
    - `npx vitest run 2>&1 | grep "Tests"` shows zero test failures in unit tests
    - `npx tsc --noEmit` produces zero errors
    - Any still-failing capture tests are documented with root cause (JSX/migration/sync, not capture classification)
  </acceptance_criteria>
  <done>Full convergence suite run. All capture-classification issues fixed. Remaining failures documented as cross-phase (JSX/migration/sync). Zero regressions from 73 baseline. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source input -> optimizer | Untrusted source code parsed and transformed; no user-facing network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | Metadata arrays | accept | Build-time transform; metadata correctness validated by convergence tests, not security-sensitive |
</threat_model>

<verification>
1. `npx vitest run tests/optimizer/convergence.test.ts` — convergence count >= 73
2. `npx vitest run` — full suite green
3. `npx tsc --noEmit` — zero errors
</verification>

<success_criteria>
- All capture-classification convergence tests pass
- Convergence count >= 73 (zero regressions)
- Full unit test suite passes
- TypeScript compiles cleanly
- Remaining failures (if any) documented as non-capture root causes
</success_criteria>

<output>
After completion, create `.planning/phases/18-capture-classification-convergence/18-02-SUMMARY.md`
</output>
