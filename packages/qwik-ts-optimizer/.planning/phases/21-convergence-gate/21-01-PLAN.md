---
phase: 21-convergence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/optimizer/jsx-transform.test.ts
  - .planning/phases/21-convergence-gate/21-VERIFICATION.md
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03]

must_haves:
  truths:
    - "Convergence test count is measured and documented accurately"
    - "All 73 v2.0 baseline tests still pass (zero regressions)"
    - "All unit tests pass with zero failures after expectation fixes"
    - "TypeScript compilation produces zero errors"
    - "Gate outcome (pass/fail) is recorded with gap analysis"
  artifacts:
    - path: "tests/optimizer/jsx-transform.test.ts"
      provides: "Updated classifyProp test expectations matching SWC-aligned behavior"
      contains: "toBe('var')"
    - path: ".planning/phases/21-convergence-gate/21-VERIFICATION.md"
      provides: "Gate report with convergence count, regression check, failure analysis"
      contains: "Gate Outcome"
  key_links:
    - from: "tests/optimizer/jsx-transform.test.ts"
      to: "src/optimizer/jsx-transform.ts"
      via: "classifyProp function under test"
      pattern: "classifyProp"
---

<objective>
Validate v3.0 milestone convergence, fix 2 known unit test expectations from Phase 19 SWC alignment, and produce the gate report documenting pass/fail with gap analysis.

Purpose: This is the convergence gate for v3.0. It measures whether the 70%+ target (147+/210) was achieved, verifies zero regressions from v2.0 baseline, and documents the actual state for milestone closure.

Output: Updated unit tests (2 expectations), gate verification report (21-VERIFICATION.md)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-convergence-gate/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Phase 19 unit test expectations and verify full test suite</name>
  <files>tests/optimizer/jsx-transform.test.ts</files>
  <read_first>
    - tests/optimizer/jsx-transform.test.ts (lines 72-85 for classifyProp member expression test, lines 265-283 for imported value constProps test)
    - .planning/phases/19-jsx-transform-convergence/19-VERIFICATION.md (confirmation that these changes are intentional SWC alignment)
  </read_first>
  <action>
Fix 2 unit test expectations in tests/optimizer/jsx-transform.test.ts that were intentionally broken by Phase 19's SWC-aligned classifyProp change:

1. **Line 79** — Test "returns const for member expression on imported value (styles.foo)":
   - Change `expect(classifyProp(node, importedNames)).toBe('const')` to `expect(classifyProp(node, importedNames)).toBe('var')`
   - Reason: Phase 19 aligned classifyProp with SWC is_const.rs which treats ALL member expressions as var regardless of import status

2. **Line 281** — Test "puts imported value props in constProps":
   - Change `expect(result!.constProps).toContain('class: styles.foo')` to `expect(result!.varProps).toContain('class: styles.foo')`
   - Change `expect(result!.varProps).toBeNull()` to `expect(result!.constProps).toBeNull()`
   - Update the test name from "puts imported value props in constProps" to "puts imported member expression props in varProps" to reflect SWC-aligned behavior

After edits, run the full test suite: `npx vitest run`

Verify: ALL non-convergence unit tests pass (the 2 previously-failing classifyProp tests now pass, the 3 pre-existing failures from v2.0 are noted but not addressed here as they predate v3.0).
  </action>
  <verify>
    <automated>npx vitest run tests/optimizer/jsx-transform.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/optimizer/jsx-transform.test.ts contains `toBe('var')` for the member expression on imported value test (line ~79)
    - tests/optimizer/jsx-transform.test.ts contains `varProps` (not constProps) for the imported member expression integration test (line ~281)
    - `npx vitest run tests/optimizer/jsx-transform.test.ts` exits with 0 failures
  </acceptance_criteria>
  <done>Both Phase 19 classifyProp test expectations updated to match SWC-aligned behavior; jsx-transform.test.ts passes fully</done>
</task>

<task type="auto">
  <name>Task 2: Run convergence gate checks and produce verification report</name>
  <files>.planning/phases/21-convergence-gate/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-convergence-gate/21-RESEARCH.md (baseline numbers: 76/210 pass, 74/209 real snapshots, failure categories)
    - .planning/ROADMAP.md (Phase 21 success criteria: 147+/210, zero regressions, zero unit test regressions, tsc clean)
  </read_first>
  <action>
Run all four gate validation checks and document results in 21-VERIFICATION.md:

**Check 1 — Convergence count (CONV-01):**
Run `npx vitest run tests/optimizer/convergence.test.ts` and record exact pass/fail count. The target is 147+/210 (70%). Research shows current state is 76/210 (36%). Record the actual number.

**Check 2 — Regression check (CONV-02):**
From the convergence test output, verify that ALL 73 v2.0 baseline tests still pass. The research confirms zero regressions + 3 new passes. Record which baseline tests pass.

**Check 3 — Unit test suite (CONV-03):**
Run `npx vitest run` (full suite). After Task 1 fixes, the 2 Phase 19 classifyProp tests should now pass. Record total pass/fail. Note the 3 pre-existing v2.0 failures separately (these are NOT v3.0 regressions).

**Check 4 — TypeScript compilation:**
Run `npx tsc --noEmit` and record result (should be 0 errors).

**Write 21-VERIFICATION.md** with this structure:
```
# Phase 21: Convergence Gate — Verification Report

## Gate Outcome: FAIL (below 70% target)

## Check Results
| Check | Target | Actual | Status |
|-------|--------|--------|--------|
| CONV-01: Convergence | 147+/210 (70%) | {actual}/210 ({pct}%) | FAIL |
| CONV-02: Regressions | 0 regressions | 0 regressions | PASS |
| CONV-03: Unit tests | 0 new failures | 0 new failures (3 pre-existing v2.0) | PASS |
| tsc --noEmit | 0 errors | 0 errors | PASS |

## v3.0 Milestone Summary
- Starting convergence: 73/210 (v2.0 baseline)
- Ending convergence: {actual}/210 (v3.0 final)
- Net gain: +{N} tests
- Infrastructure improvements: [list from research]

## Failure Gap Analysis
- Total failing: {N}
- Parent-only: {N}
- Segment-only: {N}
- Both: {N}
- Top root cause: const_idents tracking (estimated 25-35 test impact)

## Recommendation
Gate does NOT pass. Document v3.0 results and plan v4.0 milestone targeting const_idents tracking system.
```

The report must contain actual numbers from the test runs, not estimates from research.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/21-convergence-gate/21-VERIFICATION.md exists and contains "Gate Outcome"
    - .planning/phases/21-convergence-gate/21-VERIFICATION.md contains "CONV-01" and "CONV-02" and "CONV-03"
    - .planning/phases/21-convergence-gate/21-VERIFICATION.md contains actual pass/fail counts from test runs
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>Gate verification report written with actual test results; all 4 checks documented; gate outcome recorded as FAIL with gap analysis and v4.0 recommendation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test expectations vs runtime behavior | Updated test expectations must match actual classifyProp output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | jsx-transform.test.ts | accept | Test expectation changes are intentional SWC alignment; verified by running full test suite after changes |
| T-21-02 | Repudiation | 21-VERIFICATION.md | mitigate | Gate report numbers come from actual test execution output, not estimates; commands documented for reproducibility |
</threat_model>

<verification>
1. `npx vitest run tests/optimizer/jsx-transform.test.ts` — all classifyProp tests pass
2. `npx vitest run tests/optimizer/convergence.test.ts` — convergence count documented
3. `npx vitest run` — full suite, 0 new failures from v3.0
4. `npx tsc --noEmit` — 0 errors
5. 21-VERIFICATION.md contains gate outcome with actual numbers
</verification>

<success_criteria>
- Phase 19 unit test expectations updated and passing
- Convergence count accurately measured and documented
- Zero regressions from v2.0 baseline confirmed
- TypeScript compilation clean
- Gate report (21-VERIFICATION.md) written with pass/fail determination and gap analysis
</success_criteria>

<output>
After completion, create `.planning/phases/21-convergence-gate/21-01-SUMMARY.md`
</output>
