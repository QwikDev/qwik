==INPUT==



export const cache = patternCache[cacheKey] || (patternCache[cacheKey]={});
============================= project/test.tsx ==

//


export const cache = patternCache[cacheKey] || (patternCache[cacheKey]={});
== DIAGNOSTICS ==

[]
