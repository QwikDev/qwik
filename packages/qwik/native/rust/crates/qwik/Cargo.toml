[package]
name = "qwik"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
ryu-js = "1"
num-bigint = "0.4"

[dev-dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
