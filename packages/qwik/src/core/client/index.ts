/**
 * @file
 *
 *   This sub-package contains all of the code which is needed for deserialization of DOM and data.
 *
 *   It contains:
 *
 *   - DOM deserialization code (both vDOM as well as state)
 *   - Retrospection API which can be used by DevTools to figure out the state of the system
 *
 *   It DOES NOT contain:
 *
 *   - Serialization of code. (Once on client there is no need to serialize)
 *   - Streamable API for writing into the HTTP stream
 */
