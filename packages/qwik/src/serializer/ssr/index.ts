/**
 * @file
 *
 *   This sub-package contains all of the code which is needed for serialization of DOM and data.
 *
 *   It contains:
 *
 *   - Streamable API for writing into the HTTP stream
 *   - Serialization for both the vDOM as well as for state
 *
 *   It DOES NOT contain:
 *
 *   - Any DOM references (it is server)
 *   - Deserialization code
 */
