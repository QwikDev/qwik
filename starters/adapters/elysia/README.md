## Elysia on Bun

This starter site is configured to serve Qwik City through [Elysia](https://elysiajs.com/) on [Bun](https://bun.sh/). After running a full build, you can preview the build using the command:

```
bun run serve
```

Then visit [http://localhost:3000/](http://localhost:3000/)
