{
  "description": "Authentication plugin for using battle-tested Auth.js (aka NextAuth)",
  "devDependencies": {
    "@builder.io/qwik-auth": "0.0.4",
    "@auth/core": "^0.5.1"
  },
  "__qwik__": {
    "displayName": "Integration: Auth.js (authentication)",
    "priority": -10,
    "viteConfig": {
      "viteConfig": {
        "optimizeDeps": {
          "include": [
            "@auth/core"
          ]
        }
      }
    },
    "docs": [
      "https://authjs.dev/"
    ]
  }
}
